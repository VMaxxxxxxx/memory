- 掌握 C/C++，熟悉常用数据结构，了解面向对象及 C++11 新特性

- 熟悉 Linux 操作系统的基本使用（Linux 常用指令、Shell 脚本、vim 编辑等）

- 熟悉 Linux 应用编程，了解文件 IO、Socket 编程、进程线程模型、IPC 机制等

- 了解 i.MX6Q 平台，具备系统部署、驱动集成与基础调试能力，了解 UART/I2C/SPI 等通信协议

  > - 常用通信协议：
  >
  >   > ### 1. 串行通信（低速嵌入式片上总线）
  >   >
  >   > - **UART（Universal Asynchronous Receiver/Transmitter）**
  >   >   - 特点：异步，点对点，通常 2 根线（TX、RX），可加 GND。
  >   >   - 优点：简单、常用、开发调试方便（串口终端、日志输出）。
  >   >   - 缺点：不支持多机通信，速率有限（一般几十 Kbps ~ Mbps）。
  >   >   - 应用：**调试信息打印**，模块间简单通信（如 GPS、蓝牙模块）。
  >   > - **I²C（Inter-Integrated Circuit）**
  >   >   - 特点：双线总线（SCL、SDA），主从模式，支持挂多个从机（用 7 位/10 位地址区分）。
  >   >   - 优点：只需两根线，多从机扩展方便，硬件实现广泛。
  >   >   - 缺点：速率较低（标准 100 kbps，快速模式 400 kbps，最高 3.4 Mbps），线长受限。
  >   >   - 应用：**传感器读取**、配置寄存器、低速外设（RTC、EEPROM）。
  >   > - **SPI（Serial Peripheral Interface）**
  >   >   - 特点：全双工，通常 4 根线（MOSI、MISO、SCLK、CS），一主多从。
  >   >   - 优点：速度比 I²C 高得多（几十 Mbps），实现简单。
  >   >   - 缺点：需要的引脚数多（每个从机都要单独的片选 CS），没有统一标准。
  >   >   - 应用：**Flash 存储器、显示屏、音频编解码器、高速传感器**。
  >   > - **CAN（Controller Area Network）**
  >   >   - 特点：**多主机**总线，差分信号，两线（CAN_H、CAN_L）。
  >   >   - 优点：鲁棒性高，抗干扰强，支持仲裁，实时性好。
  >   >   - 缺点：速率相对有限（通常 ≤1 Mbps）。
  >   >   - 应用：**汽车电子**（ECU 之间通信）、**工业控制**。
  >   >
  >   > ------
  >   >
  >   > ### 2. 并行通信
  >   >
  >   > - **GPIO 并行接口**
  >   >   - 一次传输多位，速度高，但占用引脚多。
  >   >   - 应用：LCD 并口驱动、老式存储扩展。
  >   >
  >   > ------
  >   >
  >   > ### 3. 高速串行总线（系统总线接口、多媒体/高速数据传输）
  >   >
  >   > - **USB（Universal Serial Bus）**
  >   >   - 特点：主从结构，支持热插拔，速度分档（USB2.0 480 Mbps，USB3.0 5 Gbps+）。
  >   >   - 优点：通用性强，支持供电。
  >   >   - 应用：键盘鼠标、存储设备、摄像头。
  >   > - **Ethernet（以太网）**
  >   >   - 特点：基于包交换，速率常见 10M/100M/1G/10G。
  >   >   - 优点：远距离、高速，支持 TCP/IP 协议栈。
  >   >   - 应用：工业设备联网，远程通信。
  >   > - **PCIe（Peripheral Component Interconnect Express）**
  >   >   - 特点：点对点高速总线，通道可扩展（x1、x4、x16）。
  >   >   - 优点：超高速（几 GB/s），低延迟。
  >   >   - 应用：显卡、SSD、网卡、嵌入式扩展板卡。
  >   > - **MIPI（Mobile Industry Processor Interface）**
  >   >   - 应用：常用于手机、平板的摄像头（MIPI CSI）、显示屏（MIPI DSI）。
  >   >
  >   > ------
  >   >
  >   > ### 4. 无线通信协议（常见于嵌入式 IoT）
  >   >
  >   > - Wi-Fi：高速，适合联网大数据量传输。
  >   > - Bluetooth：低功耗，短距离。
  >   > - ZigBee：低速、低功耗、Mesh 网络。
  >   > - NFC：近场通信，支付、身份识别。
  >   >
  >   > ------
  >   >
  >   > | 协议         |      |          |            | 线数  | 速率                  | 主从关系    | 典型应用       |
  >   > | ------------ | ---- | -------- | ---------- | ----- | --------------------- | ----------- | -------------- |
  >   > | **UART**     | 串行 | 异步     | **全双工** | 2     | Kbps~Mbps（**低速**） | 点对点      | 调试、模块通信 |
  >   > | **I²C**      | 串行 | **同步** | 半双工     | 2     | ≤3.4 Mbps（**低速**） | 一主多从    | 传感器、EEPROM |
  >   > | **SPI**      | 串行 | **同步** | **全双工** | 4+    | 10~100 Mbps（高速）   | 一主多从    | Flash、显示屏  |
  >   > | **CAN**      |      |          |            | 2     | ≤1 Mbps               | 多主        | 汽车、工业     |
  >   > | **USB**      |      |          |            | 4     | ≤5 Gbps               | 主从        | 外设、存储     |
  >   > | **Ethernet** |      |          |            | 2/4/8 | 10 Mbps~10 Gbps       | 点对点/交换 | 网络通信       |
  >   >
  >   > ![image-20250907150445761](C:\Users\19855\AppData\Roaming\Typora\typora-user-images\image-20250907150445761.png)
  >
  > - uart、iic、spi的**语法、语义、时序**
  >
  >   > ### **UART（异步串口）**
  >   >
  >   > 1. **语法**：UART 通常只有两根主要信号线：TX（发送）、RX（接收），可选 RTS/CTS 做流控；数据按字节发送，每个字节通常包含 **起始位 1 位 + 数据位 5~8 位 + 可选奇偶校验位 + 停止位 1~2 位**。
  >   >
  >   > 2. **语义**：UART 是**异步、全双工、点对点**通信协议，字节为单位传输，不需要时钟信号，起始位标识帧开始，停止位标识帧结束，波特率必须一致。数据含义由上层约定（ASCII、二进制），可以带软件或硬件流控保证不丢失数据。
  >   >
  >   > 3. **时序**：时序按波特率决定，每一位的持续时间固定，数据按位串行发送，**起始位拉低，数据位低位先发，高位后发，可选校验位跟随，最后发送停止位拉高**。接收端用相同波特率计时采样，异步时序对两端必须严格匹配。
  >   >
  >   > 4. > uart发送数据时，先发低位，后发高位，当总线处于空闲时，线路保持高电平，发送数据时，会将总线变成低电平，提醒数据接收方准备，然后依次从低位到高位依次发送八位的数据位，数据位传输完成后，会发送一个1，让总线重新回到高电平的空闲状态；后续若要继续发送数据，需要重新发送起始位，重复改过程
  >   >    >
  >   >    > 板间通信：rs232、usb转串口、MCU主辅控制芯片
  >   >
  >   > ------
  >   >
  >   > ### **I²C（双线总线）**
  >   >
  >   > 1. **语法**：I²C 使用两根线：SCL（时钟）、SDA（数据），均需要上拉电阻；每次传输以 **起始信号（S） + 7/10 位地址 + 读/写位 + ACK/NACK + 数据 + 停止信号（P）** 构成。
  >   >
  >   > 2. **语义**：I²C 是基于**寻址和应答**机制的**多主多从**、**半双工**通信协议，SCL 由主机控制时钟，SDA 双向传输数据，每个字节传输后由接收方返回 ACK 表示接收成功。通过地址区分多个从设备，读写操作由 R/W 位指定。
  >   >
  >   > 3. **时序**：SDA 数据线上升/下降沿在 SCL 时钟的高低电平中变化；起始信号是 SDA 下降同时 SCL 高，停止信号是 SDA 上升同时 SCL 高；**每个数据位在 SCL 高电平时采样（因此数据位必须保持稳定）**，时序严格遵守时钟同步。
  >   >
  >   > 4. > iic可以挂载多个参与通信的器件，SCL线用于时钟同步，SDA用于传输数据，支持多主机模式，但同一时刻只能有一个主机，主机拥有该时刻下总线的控制权（发起和结束的权利）
  >   >    >
  >   >    > iic器件都有一个固定的地址，当主机要读取从机中的数据时，会向总线发出一个地址，找到从机
  >   >    >
  >   >    > iic从高到低依次发送数据，总线空闲时，SCL和SDA均保持高电平，当主机要发送数据时，会先将SDA数据线拉低，总线上这个从高电平到低电平的跳变是起始位，然后做**从机器件寻址**，在SCL低电平时，依次发送地址位，读写指示位。主机发送完后，**从机**如果成功接收，会发送一个**应答位**到总线上。然后主机开始以相同的流程**发送数据**，在一个8位数据发送后，必须有来自从机的应答位才能继续发送下一个字节。当所有数据发送完毕，主机需要将SCL时钟线拉到高电平，且SDA也从低电平到高电平（标识**停止位**）
  >   >
  >   > ------
  >   >
  >   > ### **SPI（串行外设接口）**
  >   >
  >   > 1. **语法**：SPI 常用四根线：SCLK（时钟）、MOSI（主出从入）、MISO（主入从出）、CS/SS（片选）；可配置模式（**CPOL、CPHA**）决定**时钟空闲电平**和**采样边沿**。
  >   >
  >   > 2. **语义**：SPI 是**无应答**的**一主多从**、**全双工**、**同步**协议，**主机拉低 CS 选择从机**，通过时钟同步数据传输，每个时钟周期传输 1 位数据；主机发送 MOSI，从机响应 MISO；传输速率由 SCLK 决定。
  >   >
  >   > 3. **时序**：数据在**时钟沿**采样和输出，CPOL/CPHA 决定上升/下降沿采样；CS 低有效，数据在 CS 拉低后连续传输，每传完 N 位数据，CS 拉高结束传输。主从完全同步，支持高速连续流传输。
  >   >
  >   > 4. > spi数据从高位到低位依次发送，spi的时钟线在空闲时，可以是高电平或低电平
  >   >    >
  >   >    > **空闲如果是高电平（取决于时钟极性）**，当SCLK出现下降沿跳变时，进行数据输出（**取决于时钟相位**），在上升沿出现时，进行数据采样（**取决于时钟相位**），没有开始位、停止位、应答位，**输出采样交替进行**
  >
  > - 硬件spi、软件spi
  >
  >   - 硬件spi：mcu/soc内部有专门的spi硬件控制器，直接配置，来进行高速传输，CPU消耗低，硬件完成大部分时序和数据搬运，直接DMA，
  >   - 软件spi：通过gpio在代码中按照时序手动控制SCLK、MOSI、CS，模拟spi信号，不依赖硬件控制器，随意选择引脚，速度比较慢，CPU消耗高
  >
  > - 触摸屏uart：
  >
  >   > 驱动软件的配置有接口选择，其实也只是线序的选择，rs232、ps2、usb，解决方案是从usb切换到rs232，去修改内核中GPIO引脚复用，根据我们底板的硬件连线，复用开启uart4，然后做编译烧录，使用`hexdump /dev/ttymxc3`进行测试
  >
  > - 串口概念
  >
  >   > 1. uart和usart都是串口的实现方式，是协议层
  >   > 2. rs232（短距离）和rs485（长距离、差分信号（只关心两根线的电压差，抗干扰））是接口层，规定的是电气特性，和uart结合使用
  >   > 3. 应用层设备文件通过/dev/ttymxcX读取和写入数据

- 熟练使用 Git、GDB、CMake、Makefile 等常用开发工具

  > - 补丁
  >
  >   > - 普通补丁：
  >   >   - 创建补丁：`diff -urN old/ new/ > feature.patch`
  >   >   - 应用补丁：`cd old/ && patch -p1 < ../feature.patch`
  >   >   - 回滚：`patch -R -p1 < ../feature.patch`
  >   >
  >   > - git补丁：
  >   >   - 生成补丁（本地工作区和暂存区对比）：`git diff > feature.patch`
  >   >   - 应用补丁：`git apply feature.patch && git add -A && git commit -m "Apply patch: XX`
  >
  > - git
  >
  >   > - 分布式版本控制系统，每个开发者本地都保存了完整的版本库，每个版本变更都是快照，由提交组成，分支的本质是使用指针或引用指向某个commit
  >   > - 代码管理：`git add/commit/log/show` 跟踪修改；
  >   > - 分支管理：`git branch/checkout/switch/merge/rebase`，常用 feature 分支开发，合并到 main/master；
  >   > - 远程协作：`git clone/fetch/pull/push`，熟悉 `origin` 与远程分支的关系；
  >   > - 回退和修复：比如 `git reset --hard` 回退到某个提交，`git revert` 生成反向提交，`git cherry-pick` 把别的分支的单独提交应用到当前分支；
  >   > - 冲突解决：在合并时用 `git mergetool` 或手工修改，确认后 `git add` 并完成合并。
  >   > - **补丁管理**：用 `git diff`、`git format-patch` 生成补丁，再用 `git apply` 或 `git am` 应用。
  >
  > - gdb
  >
  >   > - 编译的时候，添加-g调试信息
  >   >
  >   > - 启动gdb：`gdb ./app`
  >   >
  >   > - 设置断点（函数、行）：`break foo.c:25`、`b main`
  >   >
  >   > - 运行程序：`run`
  >   >
  >   > - 单步执行：`next`不进入函数、`step`进入函数、`continue`运行到下一个断点
  >   >
  >   > - 查看状态：`print var`、`backtrace`查看函数调用栈、`info locals`
  >   >
  >   > - > 程序崩溃，如segmentation fault、非法指令、除零，操作系统会把当前进程的内存镜像、寄存器状态、调用栈等环境信息，保存到一个文件，叫core dump核心转储文件
  >   >   >
  >   >   > 使用gdb调试器可以在程序结束后分析问题
  >   >   >
  >   >   > - 允许生成core dump：`ulimit -c unlimited`，得到一个core或core.pid
  >   >   > - 开始调试：`gdb ./app core.1234`
  >   >   > - `bt`、`info locals`、`frame 2`、`list`查看崩溃附近的代码
  >
  > - cmake
  >
  >   > - 跨平台的构建系统生成器，本身不编译代码，只是根据CMakeLists.txt生成对应平台的构建文件，如Makefile、VS工程
  >   > - **基本工程配置**：`cmake_minimum_required`、`project`、`add_executable`、`add_library`；
  >   > - **依赖与链接**：`find_package` 查找第三方库，`target_link_libraries` 把库与目标绑定；
  >   > - **编译选项**：用 `target_compile_options`、`target_include_directories` 配置不同目标的编译参数；
  >   > - **构建类型**：通过 `CMAKE_BUILD_TYPE=Debug/Release` 控制调试或发布；
  >   > - **跨平台管理**：例如在 Linux 下编译 ARM 平台代码时，通过 `toolchain.cmake` 文件配置交叉编译。
  >
  > - makefile
  >
  >   > - GNU Make的配置文件，**描述文件之间的依赖关系和构建规则**
  >   > - 目标：最终生产的文件，可执行文件或库
  >   > - 依赖：生成目标所需要的源文件或中间文件
  >   > - 命令：生成目标的操作，编译或链接
  >   > - **机制：如果目标的依赖文件修改过，才重新执行命令，否则直接跳过**，提高构建效率
  >   >
  >   > ```makefile
  >   > all: main
  >   > main: main.o foo.o
  >   >     gcc -o main main.o foo.o
  >   > %.o: %.c
  >   >     gcc -c $< -o $@
  >   > clean:
  >   >     rm -f *.o main
  >   > ```