# 1.操作系统接口

> > 操作系统的任务：
> >
> > 1. 管理硬件资源（CPU、内存、I/O 设备）。
> > 2. 提供进程/线程管理与调度。
> > 3. 实现存储管理（内存、文件系统）。
> > 4. 提供系统调用接口，方便应用运行。
> > 5. 保证安全与资源隔离。
>
> > | **系统调用**                              | **描述**                                                    |
> > | ----------------------------------------- | ----------------------------------------------------------- |
> > | **`int fork()`**                          | 创建一个进程，返回子进程的PID                               |
> > | **`int exit(int status)`**                | 终止当前进程，并将状态报告给wait()函数。无返回              |
> > | **`int wait(int *status)`**               | 等待一个子进程退出; 将退出状态存入*status; 返回子进程PID。  |
> > | **`int kill(int pid)`**                   | 终止对应PID的进程，返回0，或返回-1表示错误                  |
> > | **`int getpid()`**                        | 返回当前进程的PID                                           |
> > | `int sleep(int n)`                        | 暂停n个时钟节拍                                             |
> > | **`int exec(char *file, char *argv[])`**  | 加载一个文件并使用参数执行它; 只有在出错时才返回            |
> > | **`char *sbrk(int n)`**                   | 按n 字节增长进程的内存。返回新内存的开始                    |
> > | **`int open(char *file, int flags)`**     | 打开一个文件；flags表示read/write；返回一个fd（文件描述符） |
> > | **`int write(int fd, char *buf, int n)`** | 从buf 写n 个字节到文件描述符fd; 返回n                       |
> > | **`int read(int fd, char *buf, int n)`**  | 将n 个字节读入buf；返回读取的字节数；如果文件结束，返回0    |
> > | `int close(int fd)`                       | 释放打开的文件fd                                            |
> > | `int dup(int fd)`                         | 返回一个新的文件描述符，指向与fd 相同的文件                 |
> > | **`int pipe(int p[])`**                   | 创建一个管道，把read/write文件描述符放在p[0]和p[1]中        |
> > | `int chdir(char *dir)`                    | 改变当前的工作目录                                          |
> > | `int mkdir(char *dir)`                    | 创建一个新目录                                              |
> > | `int mknod(char *file, int, int)`         | 创建一个设备文件                                            |
> > | `int fstat(int fd, struct stat *st)`      | 将打开文件fd的信息放入*st                                   |
> > | `int stat(char *file, struct stat *st)`   | 将指定名称的文件信息放入*st                                 |
> > | `int link(char *file1, char *file2)`      | 为文件file1创建另一个名称(file2)                            |
> > | `int unlink(char *file)`                  | 删除一个文件                                                |

## 1.1进程和内存

> > 进程的组成：用户空间内存（指令、数据、堆栈）+对内核私有的进程状态
> >
> > 进程创建方式：fork出子进程
> >
> > 进程退出方式：exit使进程停止执行、释放资源（内存、打开的fd）
> >
> > 父子进程退出：`wait`系统调用返回当前进程的已退出(或已杀死)子进程的PID，并将子进程的退出状态复制到传递给`wait`的地址，如果调用方的子进程都没有退出，那么wait等待一个子进程退出
> >
> > 父子进程差别：在运行中拥有不同的内存空间和寄存器
> >
> > 进程空间替换：`exec`系统调用使用从文件系统中存储的文件所加载的新内存映像替换调用进程的内存。
>
> > fork之后父子进程间的差别：
> >
> > **独自占有**：寄存器（PC、SP 等）、内核栈、进程控制块（struct proc）。
> >
> > **共享**：代码段、数据段、堆、栈的物理内存（采用 **写时复制** COW 思想，xv6-riscv 实现了 COW）。
> >
> > **文件描述符表**：父子各自有表，但表项指向同一个打开的文件对象（**共享文件偏移**）。
> >
> > **空间分配**：fork 时不会立刻为子进程分配一份新的全部内存，先共享父进程的物理页，只有在写入时才会分配新页。
>
> > shell的执行原理：`shell` = **读命令 → fork 子进程 → exec 执行 → 父进程等子进程结束 → 循环**。
> >
> > 1. **shell 主循环**：读用户输入一行命令。
> > 2. **fork**：复制出一个子进程。
> >    - 父进程 → `wait()` 等待子进程结束。
> >    - 子进程 → `exec()` 替换自己为用户输入的程序。
> > 3. **程序执行**：比如 `echo hello`，此时子进程真正运行的是 `echo` 程序。
> > 4. **程序结束**：`echo` 运行完后调用 `exit()`。
> > 5. **父进程继续**：父进程的 `wait()` 返回，shell 回到循环，等待下一条命令。

## 1.2IO和文件描述符

> > 文件描述符FD：小整数，表示一个进程可以读写的、由内核管理的对象
> >
> > FD的获取：打开文件、目录、设备、创建管道、复制已存在的FD
> >
> > 进程FD：从FD0读取（标准输入），将输出写入FD1（标准输出），将错误消息写入FD2（标准错误）
> >
> > `read`和`write`系统调用，以字节为单位，从FD的偏移量开始，读取或写入已打开的以文件描述符命名的文件。
> >
> > FD分配规则：新分配的文件描述符总是当前进程中编号最小的未使用描述符。
>
> > cat实质：从FD0中read，往FD1中write，发生错误将消息写入标准错误FD2
> >
> > ```c
> > char buf[512];
> > int n;
> > for (;;) {
> >     n = read(0, buf, sizeof buf);
> >     if (n == 0)
> >         break;
> >     if (n < 0) {
> >         fprintf(2, "read error\n");
> >         exit(1);
> >     }
> >     if (write(1, buf, n) != n) {
> >         fprintf(2, "write error\n");
> >         exit(1);
> >     }
> > }
> > ```
>
> > shell实现IO重定向的原理：
> >
> > **fork 的作用**
> >
> > - 子进程会复制父进程的文件描述符表（fd 表）。
> > - 所以子进程一开始和父进程有完全相同的“打开文件”。
> >
> > **exec 的作用**
> >
> > - exec 只替换进程的代码和数据（内存），**不会**动文件描述符表。
> > - 所以重定向好的文件描述符在 exec 之后依然有效。
> >
> > **I/O 重定向的技巧**
> >
> > - shell 在子进程里把标准输入（fd=0）关掉。
> > - `open("input.txt")` 会返回最小可用的 fd（就是 0）。
> > - 所以新的 fd=0 指向 `input.txt`。
> > - 接着 exec 执行 `cat`，此时 `cat` 读标准输入，其实就是读 `input.txt`。
> >
> > **父进程不受影响**
> >
> > - 父进程的 fd 表没改，依然保持原状。
>
> > open标志：
> >
> > | `O_RDONLY` | 只读                     |
> > | ---------- | ------------------------ |
> > | `O_WRONLY` | 只写                     |
> > | `O_RDWR`   | 可读可写                 |
> > | `O_CREATE` | 如果文件不存在则创建文件 |
> > | `O_TRUNC`  | 将文件截断为零长度       |
> >
> > 两个进程分别调用 `open("x")`，虽然文件相同，但内核会创建两个不同的 `struct file`，**各自有独立的偏移量。**
>
> > fork和exec系统调用分离的原因：方便对子进程实现IO重定向而不干扰主shell的IO
>
> > dup复制一个现有的FD，返回引用自同一个底层IO对象的新文件描述符，共享偏移

## 1.3管道

> > 管道实质：小型内核环形缓冲区，一对读写的FD，单工，进程间通信
>
> > ```c
> > int p[2];
> > char *argv[2];
> > argv[0] = "wc";
> > argv[1] = 0;
> > pipe(p);
> > if (fork() == 0) {	// 子进程
> >     close(0);	// 关闭标准输入0，则FD0未分配
> >     dup(p[0]);	// 将FD0指向管道的读端p[0]，重定向
> >     close(p[0]);	// 关闭管道读写
> >     close(p[1]);
> >     exec("/bin/wc", argv); //等待管道中有读入的数据
> > } else {
> >     close(p[0]);	// 关闭读端，保留写端
> >     write(p[1], "hello world\n", 12);	// 往管道中写入，让子进程读
> >     close(p[1]);
> > }
> > ```
>
> > 管道和临时文件：
> >
> > `echo hello world | wc`与`echo hello world > /tmp/xyz; wc < /tmp/xyz`
> >
> > 管道优势：
> >
> > - 首先，管道会自动清理自己；在文件重定向时，shell使用完`/tmp/xyz`后必须小心删除
> > - 其次，管道可以任意传递长的数据流，文件重定向需要磁盘上足够的空闲空间来存储所有的数据。
> > - 第三，管道允许并行执行管道阶段，而文件方法要求第一个程序在第二个程序启动之前完成。
> > - 第四，如果实现进程间通讯，**管道的阻塞式读写**比文件的非阻塞语义更高效。

## 1.4文件系统

> > 文件系统提供数据文件+目录
>
> > 创建文件、目录：
> >
> > - mkdir创建目录项
> > - open使用O_CREATE创建数据文件
> > - mknod创建引用设备的节点：关联到主设备号、次设备号，来唯一标识一个内核设备，打开文件后，内核将读写请求交给对应的设备驱动
>
> > 文件：一个底层文件是inode索引节点，可以有多个名字link链接
> >
> > inode：保存有关文件的**元数据**，包括类型（文件、目录、设备），长度，文件内容在磁盘的位置，指向文件的链接数
>
> > fstat：从FD所引用的inode中检索信息，填充一个stat结构体
> >
> > ```c
> > struct stat {
> >     int dev;     // 文件系统的磁盘设备
> >     uint ino;    // Inode编号
> >     short type;  // 文件类型
> >     short nlink; // 指向文件的链接数
> >     uint64 size; // 文件字节数
> > };
> > ```
>
> > link：创建另一个文件名，指向现有文件的inode，通过fstat的结果判定引用的是相同的底层内容：返回相同的inode号（ino），nlink≥2
> >
> > unlink：从文件系统中删除一个，只有文件的链接数nlink为0时，inode和其对应的磁盘空间才会释放
>
> > cd作为shell内置的命令，必须更改shell本身的当前工作目录

# 2.操作系统架构

> > 操作系统的要求：多路复用、隔离、交互
>
> > CPU不单独工作，需要硬件配合，通过硬件的IO接口，CPU可以操作硬件
> >
> > xv6通过qemu来虚拟这些硬件，qemu给CPU提供了一套标准化的支撑硬件，包括RAM、ROM、串口、磁盘

## 2.1抽象系统资源

> > 应用程序间互不信任且存在bug，导致对硬件直接访问有风险，因此需要隔离，禁止应用程序直接访问敏感的硬件资源，通过**资源抽象的接口**来提供服务

## 2.2用户态、内核态、系统调用

> > 用户态=用户模式=目态
> >
> > 核心态=管理模式=管态
>
> > 强隔离：操作系统保证应用程序不能修改读取操作系统的数据结构和指令，也不能访问其他进程的内存
> >
> > 硬件支持：RISC-V有三种CPU可以执行指令的模式：机器模式、管理模式、用户模式
>
> > 机器模式：在MM下执行的指令，具有完全特权，CPU在MM下启动，用于配置计算机
>
> > 管理模式：xv6仅执行很少的几行代码就改为管理模式SM，CPU被允许执行特权指令（启用和禁用中断、读取和写入保存页表地址的satp寄存器）
>
> > 应用程序只能执行用户模式下的指令，称用户空间
> >
> > 管理模式下的软件可以执行特权指令，称内核空间，在内核空间中运行的程序称为内核
>
> > 应用程序想要调用内核函数的程序，必须过渡到内核
> >
> > CPU提供了特殊指令ecall进行模式切换，在内核指定的入口点进入内核，由内核进行参数验证，决定是否提供服务

## 2.3内核组织

> > 宏内核：内核包含所有的主要服务（进程管理、内存管理、文件管理、驱动等），以管理模式运行
> >
> > 优点：性能高、调用直接
> >
> > 缺点：内核大，出错容易导致整个系统崩溃，接口复杂
> >
> > xv6、Linux、Unix
>
> > 微内核：内核只保留最基本的功能（进程调度、内存管理、IPC），其他服务（文件系统、驱动）在用户态运行；操作系统服务作为进程运行，呈现为服务器模式，内核提供IPC机制为不同用户态进程消息传递
> >
> > 优点：内核小、安全性高、易维护
> >
> > 缺点：性能略低，需要多次用户态切换
> >
> > Minix、QNX
>
> > xv6作为宏内核实现，内核接口对应操作系统接口，但由于不提供太多服务，xv6内核比一些微内核还小

## 2.4xv6内核源码

> > 源码的模块间接口定义在def.h
>
> > 分类：
> >
> > 1. 启动与出事故
> > 2. 进程管理
> > 3. 内存管理
> > 4. 文件系统
> > 5. IO与设备驱动
> > 6. 同步机制
> > 7. 系统调用与输出
> > 8. 管道与字符串处理
>
> > | **文件**          | **描述**                                    |
> > | ----------------- | ------------------------------------------- |
> > | **bio.c**         | 文件系统的磁盘块缓存                        |
> > | **console.c**     | 连接到用户的键盘和屏幕                      |
> > | **entry.S**       | 首次启动指令                                |
> > | **exec.c**        | `exec()`系统调用                            |
> > | **file.c**        | 文件描述符支持                              |
> > | **fs.c**          | 文件系统                                    |
> > | **kalloc.c**      | 物理页面分配器                              |
> > | **kernelvec.S**   | 处理来自内核的陷入指令以及计时器中断        |
> > | **log.c**         | 文件系统日志记录以及崩溃修复                |
> > | **main.c**        | 在启动过程中控制其他模块初始化              |
> > | **pipe.c**        | 管道                                        |
> > | **plic.c**        | RISC-V中断控制器                            |
> > | **printf.c**      | 格式化输出到控制台                          |
> > | **proc.c**        | 进程和调度                                  |
> > | **sleeplock.c**   | Locks that yield the CPU                    |
> > | **spinlock.c**    | Locks that don’t yield the CPU.             |
> > | **start.c**       | 早期机器模式启动代码                        |
> > | **string.c**      | 字符串和字节数组库                          |
> > | **swtch.c**       | 线程切换                                    |
> > | **syscall.c**     | Dispatch system calls to handling function. |
> > | **sysfile.c**     | 文件相关的系统调用                          |
> > | **sysproc.c**     | 进程相关的系统调用                          |
> > | **trampoline.S**  | 用于在用户和内核之间切换的汇编代码          |
> > | **trap.c**        | 对陷入指令和中断进行处理并返回的C代码       |
> > | **uart.c**        | 串口控制台设备驱动程序                      |
> > | **virtio_disk.c** | 磁盘设备驱动程序                            |
> > | **vm.c**          | 管理页表和地址空间                          |

## 2.5进程概述

> > xv6使用页表硬件为每个进程提供自己的地址空间，RISC-V页表将虚拟地址转换成物理地址
>
> > xv6进程虚拟地址空间：RISC-V指针64位宽，硬件在页表中查找虚拟地址使用39位，xv6使用38位，最大范围2^38-1=MAXVA
> >
> > - trampoline：用于用户和内核切换
> > - trapframe：映射进程切换到内核
> > - heap：堆
> > - stack：栈
> > - user text and data：用户指令和数据
>
> > xv6内核为每个进程维护很多状态片段，形成proc结构体：页表、内核堆栈、运行状态
>
> > 进程和线程：
> >
> > - 每个进程都有一个执行线程，来执行进程的指令
> > - 一个线程可以挂起后并恢复
> > - 进程切换：内核挂起当前运行的线程，恢复另一个进程的线程
> > - 线程的状态（本地变量、函数调用返回地址）保存在线程栈区
> > - 每个进程有两个栈区：一个用户栈区、一个内核栈区
> > - 进程执行用户指令时，内核栈为空
> > - 进程进入内核，内核代码在进程的内核堆栈上运行，其用户栈仍保存数据只是不活跃
> > - 进程的线程在主动使用他的用户栈和内核栈间交替
>
> > - 一个进程通过ecall指令进行系统调用
> > - 提升硬件特权级别，将PC改为内核定义的入口点，入口点的代码切换到内核栈
> > - 执行实现系统调用的内核指令
> > - 内核调用完成，切换用户态，通过sret指令返回用户空间，降低硬件特权级别
> > - 在系统调用指令结束时，恢复用户指令
>
> > **进程状态：已分配、就绪态、运行态、阻塞态、退出**

## 2.6启动XV6和第一个进程

> > ROM引导、内核启动、硬件初始化、创建第一个进程、运行/init、启动shell、用户可以输入命令
>
> > ### 1. 上电和加载
> >
> > - 硬件上电 → 运行 **ROM 里的引导加载程序**。
> > - 引导程序把 **xv6 内核** 放到物理内存地址 `0x80000000`。
> >
> > ------
> >
> > ### 2. 进入内核入口
> >
> > - CPU 从 `_entry` (entry.S) 开始执行。
> > - `_entry` 设置一个内核栈，然后跳到 `start()` (start.c)。
> >
> > ------
> >
> > ### 3. 机器模式 → 管理模式
> >
> > - `start()` 做一些硬件初始化：关掉虚拟内存、设置中断委托、启动时钟。
> > - 修改寄存器，让 `mret` 返回时跳到 `main()`，并且切换到 **管理模式**（supervisor mode）。
> >
> > ------
> >
> > ### 4. 内核初始化
> >
> > - `main()` 初始化设备（控制台、磁盘、文件系统、进程表等）。
> > - 调用 `userinit()` 创建 **第一个用户进程**。
> >
> > ------
> >
> > ### 5. 第一个进程
> >
> > - 第一个进程运行 `initcode.S`，它只做一件事：调用 `exec("/init")`。
> > - `/init` 程序开始执行：
> >   1. 创建控制台设备文件
> >   2. 打开标准输入/输出/错误 (fd 0/1/2)
> >   3. 启动一个 **shell**
>
> > - risc-v上电，初始化自己，运行一个存储在ROM的引导加载程序
> > - 引导加载程序将xv6内核加载到内核中
> > - 在机器模式下，CPU从_entry开始运行xv6
> > - xv6启动时，页式硬件处于禁止模式，虚拟地址直接映射到物理地址
> > - 加载程序将xv6内核加载到物理地址0x80000000的内存
> > - _entry设置一个栈区，将栈顶指针stack+4096加载到栈顶指针寄存器sp，调用c代码start
> > - start执行一些机器模式下的配置，切换到管理模式
> > - risc-v提供mret进入管理模式，但start在mstatus将原来的运行模式改为管理模式，然后将main函数地址写入mepc寄存器，通过向页表急促起satp写入0在管理模式下禁用虚拟地址转换，将所有中断和异常委托管理模式
> > - start对时钟芯片编程，产生计时器中断
> > - 调用mret返回到管理模式，pc值为main地址
> > - main中初始化设备和子系统
> > - 调用userinit创建第一个进程
> > - 第一个进程执行risc-v指令集写的小程序initcode.S，通过调用exec替换掉当前进程的内存和寄存器，进入内核
> > - 内核完成exec，返回/init进程中的用户空间
> > - init将创建一个新的控制台设备文件，以0、1、2打开，在控制台启动shell

# 3.页表

> > 页表：操作系统为每个进程提供私有地址空间和内存的机制
> >
> > 一般操作：
> >
> > - 隔离不同进程的地址空间，将其服用到单个物理内存上
> >
> > 特殊操作：
> >
> > - 映射相同的内存到不同的地址空间
> > - 用一个未映射的页面保护内核栈区和用户栈区
>
> > 核心作用：地址转换（虚拟地址映射到物理地址）、内存隔离（每个进程有自己的独立页表）、内存保护（提供PET的权限位限制对内存的操作）
>
> > xv6的页表管理逻辑：
> >
> > 内核页表和用户页表：
> >
> > - 内核页表：整个系统只有一套，映射内核的代码、数据、物理内存、IO设备地址等，**状态为内核态可访问U=0**
> > - 用户页表：每个进程一套，包含用户空间（映射进程的代码、堆、栈等U=1）+内核空间（高位虚拟地址，共享内核的映射，确保用户态陷入内核后能访问内核内存）
>
> > **xv6内核缺少一个类似`malloc`可以为小对象提供内存的分配器**，这使得内核无法使用需要动态分配的复杂数据结构。

## 3.1分页硬件

> > RISC-V指令使用虚拟地址，机器的RAM或物理内存是提供物理地址索引，页表为两种地址间的映射建立联系
> >
> > XV6基于SV39运行，只使用64为虚拟地址的低39位，高25位不用
> >
> > RISC-V页表在逻辑上由2^27个页表条目PTE组成，每个PTE包含一个44位的物理页码PPN和一些标志位
> >
> > 分页硬件通过虚拟地址的前27位索引页表，找到该虚拟地址对应的PTE，生成一个56位的物理地址，前44位来自PTE中的PPN，后12位来自虚拟地址
> >
> > 页表使操作系统以2^12=4096字节=4KB（一页）的对齐力度控制虚拟地址到物理地址的转换
>
> > 实际的转换分三步：页表以三级的树形结构存储在物理内存中
> >
> > 树的根是一个4KB的页表，包含512个PTE，每个PTE占8字节，包含该树下一级页表页的物理地址
> >
> > 分页硬件使用27位虚拟地址的前9位在根页表中选择PTE，中间9位在树的下一级中选择PTE，最后9位选择最终的PTE，**如果地址转换中所需要的PTE任何一个不存在，页式硬件引发页面故障异常**
>
> > 三级页表和一级页表对比：
> >
> > - 三级页表结构更节省内存来记录PTE
> > - CPU执行切换会遍历三级结构，必须从CPU中加载三个PTE才能完成地址转换
>
> > TLB：减少从物理内存加载PTE的开销，将页表条目缓存在TLB中
>
> > PTE标志位：PTE_V、PTE_R、PTE_W、PTE_X、PTE_U
>
> > satp：告诉硬件使用页表，保存根页表页的物理地址，每个cpu都有satp

## 3.2内核地址空间

> > 进程页表=用户专属映射+公用内核映射
>
> > ![img](https://xv6.dgs.zone/tranlate_books/book-riscv-rev1/images/c3/p3.png)
>
> > QEMU模拟了计算机，包括从物理地址到0x80000000开始到0x86400000（PHYSTOP）的物理内存
> >
> > 还模拟了IO设备，将设备接口作为内存映射控制器暴露给软件，位于0x80000000下，内核通过读写特殊的物理地址与设备交互
>
> > 内核使用直接映射获取内存，将资源映射到等于物理地址的虚拟地址，简化了读写物理内存的内核代码
>
> > 内核虚拟地址中，非直接映射：
> >
> > - trampoline page：映射在虚拟地址空间的顶端，用户页表也有相同的映射
> > - 内核栈页面：**每个进程有自己的内核栈**，映射到一些高地址区域，在其之下有一个未映射的保护页，其PET是无效的，如果内核栈溢出，会引发异常，触发panic
>
> > 映射权限：
> >
> > - PTE_R、PTE_X：蹦床页面、内核代码页面
> > - PTE_R、PTE_W：其他页面

## 3.3创建一个地址空间

> > pagetable_t：执行risc-v根页表的指针，可以是内核页表，可以是进程页表
> >
> > walk：在页表中找到虚拟地址对应的PTE，如果页表没分配，且参数alloc=1，新分配一页作为子页表
> >
> > mappages：为页表的新映射装载PTE，循环调用walk，填充PTE
> >
> > kvm*：操作内核页表
> >
> > uvm*：操作用户页表
> >
> > copyin、copyout：内核态访问用户地址时做地址翻译
>
> > xv6开启分页/创建页表：
> >
> > - main调用kvminit，使用kvmmake创建内核的页表，直接引用物理地址
> > - **kvmmake首先分配一个物理页保存根页表页**
> > - 调用kvmmap装载内核需要的转换（内核指令、数据、物理内存上限到PHYSTOP），即**恒等映射**
> > - proc_mapstacks**为每个进程分配一个内核栈**，调用kvmmap将每个堆栈映射到KSTACK生成的虚拟地址，为无效的堆栈保护页留出空间
>
> > **内核栈**的用途：
> >
> > - 系统调用处理：用户进程执行ecall使得cpu切换到内核态，为内核代码准备内核栈保存参数、变量、调用帧
> > - 中断、异常处理：内核必须在某个安全的栈中运行中断处理函数
> > - 进程切换：内核决定调度另一个进程时，会在当前进程的内核栈上保存寄存器上下文，方便下次调度恢复
>
> > walk的逻辑：
> >
> > risc-v的39位虚拟地址分为三级9+9+9+12
> >
> > 模拟硬件分页单元，逐级查9位索引，发现某一级PTE无效，查看alloc是否1，进行分配
>
> > 当xv6更换页表时，必须告诉CPU使相应的缓存TLB条目失效，否则会导致对其他进程内存的涂鸦
> >
> > sfence.vma：刷新CPU的TLB
> >
> > 时机：重新加载satp寄存器后、返回用户空间前切换到用户页表的trampoline代码中

## 3.4物理内存分配

> > 内核为页表、用户内存、内核栈、管道缓冲区等分配、释放内存
> >
> > 使用内核末尾到PHYSTOP的物理内存进行运行时分配
> >
> > 一次分配释放4096字节
> >
> > 使用链表将空闲页面记录下来，分配时从链表中删除，释放时添加到链表

## 3.5物理内存分配

> > 每个空闲链表元素是`struct run`，链表受自旋锁spinLock保护
> >
> > main调用kinit初始化分配器，空闲链表保存从内核结束到PHYSTOP之间的每一页
> >
> > kinit调用freerange将内存添加到空闲链表
> >
> > PTE只能引用在4096字节边界上对齐的物理地址，使用PGROUNDUP确保释放对齐的物理地址
> >
> > kfree将内存每个字节设置成1，导致悬空引用，从而崩溃得更快，然后将页面通过头插法加入到空闲链表
> >
> > kalloc删除并返回空闲链表中的第一个元素

## 3.6进程地址空间

> > 进程地址空间分布：
> >
> > - trampoline跳板页：负责在用户态和内核态之间切换时的入口uservec、出口userret
> > - trapframe：保存从用户态到内核态的寄存器快照，ra、sp、gp、tp、a0-a7等
> > - heap
> > - user stack（下面有个保护页page guard，该页没有映射，PTE_V=0，访问将产生页面故障异常）
> > - user text and data
>
> > 1. 不同的进程的页表将用户地址转换成物理内存的不同页面
> > 2. 每个进程看到自己的内存空间都是以0开始的连续虚拟地址，而进程的物理内存可以是不连续的
> > 3. 内核在用户地址空间的顶部映射一个跳板页trampoline

## 3.7sbrk

> > sbrk：进程增长、减少其内存，由growproc实现
> >
> > 根据n的正负，调用uvmalloc（底层是kalloc分配+mappages映射）、uvmdealloc（uvmunmap使用walk查找对应的PTE，使用kfree释放对应的物理内存）
>
> > 页表不仅仅是做地址转换，还是物理页面是否分配的唯一记录

## 3.8exec

> > exec的作用：
> >
> > 替换当前进程的用户地址空间，加载一个新的可执行程序
>
> > 1. 打开文件，检查ELF格式
> > 2. 用proc_pagetable创建新用户页表
> > 3. 加载ELF程序段
> >    1. ELF程序头proghdr描述代码段和数据段
> >    2. 对每个段uvmalloc分配物理内存、从ELF文件读数据写入分配的物理页
> > 4. 分配用户栈
> >    1. 在顶端分配一页作为栈，并分配一个保护页
> >    2. 把argv参数列表复制到栈顶，构造argc和argv
> > 5. 提交新映像，在成功时把进程切换到新页表，释放旧页表

# 4.陷阱指令和系统调用

> > CPU从普通指令转向特权指令的情况：系统调用（ecall）、异常（非法指令，除零异常等）、设备中断（磁盘完成读写请求）
> >
> > 统称为陷阱trap
>
> > 流程：CPU的硬件操作、汇编程序集向量（为了内核代码执行）、陷阱处理程序、系统调用或设备驱动服务例程
> >
> > 1. 强制将控制权转移到内核
> > 2. 内核保存寄存器和其他状态
> > 3. 内核执行适当处理程序
> > 4. 内核恢复保存的状态，并从trap中返回
> > 5. 原始代码恢复执行

## 4.1

## 4.2

## 4.3

## 4.4

## 4.5

## 4.6

## 4.7

# 5.中断和设备驱动

# 6.锁

# 