> 在嵌入式 Linux 系统中，原来的 USB 触摸屏控制板在上电初始化时通过 USB 与开发板通信，内核驱动接收其枚举信息和 HID 数据时触发异常，导致内核 panic。问题的本质是内核态代码直接操作硬件，缺乏像用户空间那样的隔离机制，一旦硬件或驱动异常，就会造成整个系统挂掉。排查发现死机位置集中在 udev 日志打印触摸屏初始化之后。
>
> - 原厂内核镜像不会宕机
> - 在原厂内核基础上，加入了实时补丁，也不会出现宕机
> - 在移植了基于USB的触摸屏控制器驱动后，导致宕机
> - 基于实时补丁的镜像，触摸屏控制器和驱动都是外界设备，方案是直接替换硬件和驱动
> - **Linux设备上电启动，触摸屏驱动在内核probe()阶段（驱动的设备初始化函数，内核发现硬件后会调用这个函数）阻塞，导致udev等待创建设备节点时卡住，系统无法完成启动**

