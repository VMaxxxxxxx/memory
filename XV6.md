1. `xv6`是什么内核架构

   > **宏内核**：将进程管理、文件管理、设备驱动、内存管理等都在一个内核空间中实现，`UNIX`常用，模块之间调用简单效率高，但内核代码庞大，一个模块出错可能会影响整个系统崩溃
   >
   > 微内核：将内核功能尽量精简，只保留最基础的部分（进程间通信、调度、内存管理等），其他服务都在用户态进程中，通过**消息传递**与内核交互，稳定性和拓展性较好，但用户态和内核态切换频繁，性能开销

2. `xv6`的内核源码

   > - 启动与内核初始化
   >
   >   > **`entry.S`**：内核入口（从汇编开始执行），设置栈、跳转到`start`然后跳转到 `main`。
   >   >
   >   > **`start.c`**：内核 C 语言的第一个入口（`main`），负责初始化内核子系统。
   >   >
   >   > **`kernel.ld`**：链接脚本，定义内核在内存中的布局。
   >   >
   >   > **`trampoline.S`**：用户态和内核态切换时的“跳板”代码（切换页表）。
   >   >
   >   > **`kernelvec.S`**：内核的 trap 向量表（中断/异常的入口）。
   >
   > - 进程与调度
   >
   >   > **`proc.c / proc.h`**：进程管理（`fork`, `exit`, `scheduler`, PCB `struct proc`）。
   >   >
   >   > **`swtch.S`**：上下文切换（保存/恢复寄存器）。
   >   >
   >   > **`sysproc.c`**：与进程相关的系统调用实现（`getpid`, `fork`, `exit` 等）。
   >
   > - 内存管理
   >
   >   > **`vm.c`**：虚拟内存管理（页表、映射、页分配）。
   >   >
   >   > **`vmcopyin.c`**：用户态到内核态安全拷贝数据的辅助函数。
   >   >
   >   > **`kalloc.c`**：物理内存分配器（基于空闲链表）。
   >
   > - 系统调用与中断
   >
   >   > **`syscall.c / syscall.h`**：系统调用分发（从系统调用号到函数指针）。
   >   >
   >   > **`sysfile.c`**：与文件系统相关的系统调用（`open`, `read`, `write` 等）。
   >   >
   >   > **`sysproc.c`**：进程相关的系统调用。
   >   >
   >   > **`trap.c`**：中断/异常处理。
   >   >
   >   > **`defs.h`**：函数声明（避免循环依赖）。
   >
   > - 文件系统与`IO`
   >
   >   > **`fs.c / fs.h`**：文件系统核心（`inode`、目录、超级块）。
   >   >
   >   > **`file.c / file.h`**：文件描述符层（管理 `struct file`，抽象设备和普通文件）。
   >   >
   >   > **`log.c`**：日志（写时复制日志，保证文件系统一致性）。
   >   >
   >   > **`bio.c / buf.h`**：缓冲区缓存（block cache，管理磁盘块缓冲）。
   >   >
   >   > **`pipe.c`**：管道实现（进程间通信）。
   >   >
   >   > **`sleeplock.c / sleeplock.h`**：睡眠锁，用于文件系统。
   >   >
   >   > **`spinlock.c / spinlock.h`**：自旋锁。
   >
   > - 设备驱动
   >
   >   > **`uart.c`**：UART 串口驱动（控制台输入输出）。
   >   >
   >   > **`virtio_disk.c / virtio.h`**：VirtIO 磁盘驱动（QEMU 模拟磁盘）。
   >   >
   >   > **`ramdisk.c`**：RAM 磁盘驱动（实验用）。
   >   >
   >   > **`plic.c`**：中断控制器 PLIC 驱动。
   >   >
   >   > **`console.c`**：控制台设备（用 UART 实现）。

3. `xv6`的启动流程

   > ```markdown
   > 上电 / 复位
   >    ↓
   > M 模式 (OpenSBI)
   >    - 初始化硬件
   >    - 进入 S 模式
   >    ↓
   > S 模式 (xv6 内核)
   >    entry.S → start.c → main()
   >    - 初始化子系统 (内存/进程/文件系统/驱动)
   >    - 创建第一个用户进程 init
   >    ↓
   > U 模式
   >    initcode.S (调用 exec("/init"))
   >    ↓
   > 运行 /init
   >    - 启动 shell (sh)
   >    ↓
   > 用户可以输入命令
   > ```
   >
   > **硬件上电 → 进入 M 模式 (Machine mode)**
   >
   > - `RISC-V`上电时，会初始化自己，运行一个存储在只读内存中的引导加载程序
   > - 引导加载程序将`xv6`内核加载到内存上
   >
   > **进入内核入口 → entry.S**
   >
   > - `CPU`从 `kernel/entry.S`开始运行`xv6`，为 xv6 设置好初始执行环境。
   > - `xv6`启动时，页式硬件处于禁用模式，虚拟地址**直接映射**到物理内存
   > - **设置内核栈指针**，这样才能运行`C`代码。
   > - 跳转到 `start.c` ，先执行一些仅在机器模式下允许的**配置**，然后切换到**管理模式**，将 `main()`的地址写入寄存器`empc`将返回地址设为`main`，通过向页表寄存器**`satp`写`0`**来禁止管理模式下的虚拟地址转换，将中断和异常委托给管理模式。
   > - 然后对时钟芯片编程产生定时器中断，调用`mret`返回到管理模式，由于之前将返回值设置为`main`，因此`mret`后面会**返回到`main`函数**，并以管理模式运行
   >
   > **内核初始化 (main.c)**
   >
   > - `main()` 调用一系列**子系统初始化**：
   >   - `consoleinit()`：初始化控制台。
   >   - `printfinit()`：调试打印。
   >   - `kinit()`：物理内存分配器。
   >   - `kvminit()`：内核页表。
   >   - `trapinithart()`：设置中断向量表。
   >   - `plicinit()`：中断控制器初始化。
   >   - `binit()`, `fileinit()`, `iinit()`, `virtio_disk_init()`：文件系统 & 块设备。
   >   - `procinit()`：进程表初始化。
   >
   > **创建第一个进程 (userinit)**
   >
   > - `userinit()` 创建 **第一个用户进程 `init`**。
   > - 给它分配一个进程控制块（PCB）、用户页表。
   > - 加载 `initcode.S`（用户空间的第一段小程序）。
   > - 把进程状态设为 RUNNABLE。
   >
   > **调度器 (scheduler)**
   >
   > - `main()` 最后**调用 `scheduler()`，开始调度循环**。
   > - `scheduler()` 找到第一个可运行进程（就是 `init`），进行上下文切换。
   > - CPU 切换到 U 模式，运行 `initcode.S`。
   >
   > **进入用户态 → initcode.S**
   >
   > - `initcode.S` 是一个很小的用户程序，主要任务是**调用 `exec("/init")`。**
   > - 这样就把真正的用户程序 `/init` 从文件系统加载到内存。
   >
   > **运行 `/init` 用户程序**
   >
   > - `/init` 是一个用户空间 C 程序，它会执行：
   >   - 打开控制台。
   >   - 创建**第一个 shell** (`sh`)。
   > - 至此，系统完全启动，用户可以在 shell 里运行命令。

4. 页表初始化的时机

   > 1. **启动时没有页表，使用物理寻址**
   > 2. 在entry.S阶段，建立一个**临时页表**，只映射内核需要的部分（KERNBASE、trampoline和stack的必要区域）
   > 3. kvminit阶段，建立完整的**内核页表**，替换临时页表
   > 4. 进程创建时，再为每个用户进程建立**用户态页表**
   >
   > > - **satp寄存器**用于存放根页表的物理地址，每个页表项PTE包含下一级页表的物理地址，缺一个就引发页面故障异常
   > >
   > > - 不同的cpu有自己的satp，即可运行不同的进程
   > >
   > > - RISC-V硬件支持
   > >
   > >   > 提供了硬件页表支持，包括多级页表和地址转换机制SV39
   > >
   > > - xv6运行在RISC-V架构的CPU上，并使用SV39分页机制来管理虚拟地址空间
   > >
   > > - **RISC-V指令使用的是`虚拟地址`，机器的`RAM或物理内存`是由物理地址索引的，RISC-V `页表硬件`通过将每个虚拟地址映射到物理地址。**
   > >
   > > - SV39：只使用64位虚拟地址的低39位，高25不用。
   > >
   > > - 逻辑上，RISC-V的页表是由`2^27`个页表项`pte`组成的数组。每个`pte`包含一个44位的物理页码`PPN`和一些标志位。分页硬件利用虚拟地址39位中的27位索引页表，找到虚拟地址对应的一个`pte`，这个`pte`的`ppn`包含44位，再利用虚拟地址剩下的12位页内偏移生成一个56位的物理地址。
   > >
   > > - 页表使`os`能以4096字节的对齐粒度控制虚拟地址到物理地址的转换，即一页
   > >
   > > - 最大虚拟地址空间：`2^39字节=512GB`
   > >
   > > - 最大物理地址空间：`2^56字节`
   > >
   > > - 实际上，转换分成三步，虚拟地址的`L2`用于索引第一级页表(根页表)，`L1`用于索引第二级页表，`L0`用于索引第三级页表(叶子页表)

5. 用户页表和内核页表

   > risc-v的xv6，每个用户进程都有自己独立的用户页表，包含两部分：
   >
   > - 用户空间部分：映射自己的代码、数据、堆、栈，页表项的权限设置为U=1，用户态可以访问
   > - 内核空间部分：并不是把整个内核页表复制一份给用户，而是**每个用户页表都会额外映射到一些固定的内核区**（trampoline page用于用户内核态切换，trapframe保存寄存器现场），但完整的内核映射存在于内核自己的全局页表中，当用户触发陷入时**切换到内核态，才会将satp指向内核页表**，访问整个内核
   > - 具体形式：每个进程有自己独立的1个用户页表 + **一个全局的内核页表**
   >   1. 用户态运行：`satp` 指向该进程的用户页表（只含用户页 + trapframe + trampoline）。
   >   2. 发生系统调用/中断/异常：硬件跳到 `trampoline` 映射的那一页的入口（这页同时在所有用户页表里有统一虚拟地址）。
   >   3. 汇编（trampoline.S）保存用户寄存器到该进程的 `trapframe`（那一页在用户页表中映射但标记成内核可用）。然后软件（在 trampoline 里）切换 `satp` 到 `kernel_pagetable`，再跳进真正的 C 语言内核代码。
   >   4. 内核在 `kernel_pagetable` 下运行（其中映射：内核代码/数据、设备寄存器、kalloc 得到的物理页、各进程的内核栈等）。
   >   5. 返回用户：内核在进入 `usertrapret()` 时恢复用户寄存器前，再次通过 trampoline 把 `satp` 切换回进程的用户页表，然后执行 `sret` 回到用户空间。

6. 物理地址和虚拟地址

   > - `CPU`访问的真实硬件内存地址，没有`MMU`的系统中，程序访问的就是物理地址
   > - `CPU`指令中使用的地址，不直接访问内存，通过`MMU`转换成物理地址，每个进程看到的是独立的虚拟地址空间，由**页表**定义虚拟地址到物理地址的映射
   >   - `RISC-V`使用`Sv39`分页机制，对`64`位宽的地址只使用低`39`位，为每个进程维护一个独立的页表，并且所有进程共享同一个内核页表
   >   - `xv6`进程的地址空间分布：指令、全局变量、栈、堆、**`trapframe`**、**`trampoline`**
   >   - ![image-20250818172202649](C:\Users\19855\AppData\Roaming\Typora\typora-user-images\image-20250818172202649.png)
   >   - 进程状态：**未使用、就绪、运行、阻塞、退出**

7. `xv6`的进程

   > - `xv6`使用页表为每个进程提供自己的地址空间，`RISC-V`页表将虚拟地址转换成物理地址
   > - 每个进程都有一个**执行线程**来执行进程的指令，内核通过不断挂起当前运行的线程，恢复另一个进程的线程来实现调度
   > - 线程的大部分状态（本地变量、函数调用返回地址）存储在线程的栈上
   > - 每个进程有两个栈区：**用户栈、内核栈**；当执行用户指令的时候，内核栈为空；进程陷入内核时，内核代码在进程的内核栈上执行，此时其用户栈中仍然保存有数据，只是不活跃
   > - 进程通过执行`RISC-V`的**`ecall`**指令进行系统调用，该指令能够提升硬件特权级别，将程序计数器更改为内核定义的入口点，入口点的代码切换到内核栈（在内核页表中，每每个进程的用户栈间有保护页），执行实现系统调用的内核指令；
   > - 系统调用完成，内核切换回用户栈，通过**`sret`**指令返回用户空间，降低硬件特权级别，在指令结束时恢复执行用户指令

8. ==**`理解xv6系统调用流程`**==

   > - 用户态调用封装的函数
   >
   >   > 调用的是库函数，被关联在`user/usys.S`中，对应一个系统调用号，并执行一个`ecall`指令
   >
   > - 执行`ecall`，`trap`陷入到内核
   >
   >   > `ecall`指令触发陷入`trap`， `CPU`从用户模式切换到管理模式，硬件会保存当前用户态的`pc`，跳转到`trap`向量`trampoline.S`，保存寄存器到`trapframe`，跳转到`kernel/trap.c/usertrap()`
   >
   > - 内核处理系统调用
   >
   >   > `usertrap`检查`trap`原因，如果是系统调用，调用`syscall()`，会从`a7`寄存器中取出系统调用号，根据系统调用号，在`syscalls[]`这个注册表中找到对应的内核函数
   >
   > - 执行系统调用的具体实现
   >
   >   > 利用一些辅助函数，可以从`trapframe`中取出参数，`argint()、argaddr()`
   >
   > - 返回用户态
   >
   >   > 将系统调用函数返回值写入`a0`寄存器，回到`syscall()`、`usertrap()`、`usertrapret()`，切换到用户页表，将`CPU`状态恢复成用户态，执行`sret`指令，回到用户程序
   >
   > - ```markdown
   >   用户程序: write(fd, "hello", 5)
   >      ↓
   >   usys.S: a7=SYS_write; ecall
   >      ↓
   >   硬件: U-mode → S-mode
   >      ↓
   >   trampoline.S → uservec → usertrap()
   >      ↓
   >   syscall()
   >      ↓
   >   sys_write() in sysfile.c
   >      ↓
   >   内核 write() → 文件系统/驱动
   >      ↓
   >   返回值放在 a0
   >      ↓
   >   usertrapret() → userret → sret
   >      ↓
   >   回到用户态，write() 返回
   >   ```

9. ==**`基于用户态和内核态的数据传递和访问机制`**==

   > - 用户态**传递**参数给内核（系统调用号、系统调用参数）
   >   - `RISC-V`规定，系统调用**参数**放在`a0-a5`寄存器中，系统**调用号**在`a7`
   >   - 在`xv6`内核中，`syscall()`会从`trapframe`中取出这些寄存器，调用**`argint()`、`argaddr()`**得到参数：`argint(0, &fd)取数值参数; argaddr(1, &p);取用户空间地址参数`
   > - 内核**访问、修改**用户态数据（用户态数据在虚拟地址空间中，而内核空间页表不能直接访问）
   >   - 用户传递的**指针**，是用户虚拟地址地址，内核不能直接当做内核地址用，因为用户空间和内核空间的页表不一致，`xv6`通过`argaddr`取出地址，然后**使用`copyin`、`copyout`完成数据搬运**：`copyin(pagetable, dst, srcva, len);//从用户虚拟地址拷贝到内核缓冲区`，`copyout(pagetable, dstva, src, len);//将数据写回用户虚拟地址`
   > - 在系统调用执行结束，把返回值写回`a0`寄存器，用户态函数就能拿到结果

10. ==**`拓展实现trace、sysinfo等系统调用`**==

   > - 系统调用：操作系统通过`CPU`的特权模式将用户态和内核态隔离，保证了用户程序无法直接访问内核资源，规避了误操作和恶意行为，系统调用作为**用户程序和内核交互的桥梁**，运行中的用户程序通过受控的接口向内核请求服务，保证系统的安全性和稳定性
   >
   > - **系统调用机制：通过库函数间接发起系统调用，会触发软件中断或陷阱，引发`CPU`起换到内核态，内核根据系统调用号来确定要执行的服务，完成服务后，将结果或错误码写入用户程序的寄存器中，然后返回到用户态继续执行程序**
   >
   > - 由于内核和用户进程的页表不同，寄存器也不互通，参数无法直接通过c语言参数的形式传递，因此使用`argint()`等系列函数，从进程的`trapframe`中读取用户进程寄存器中的参数
   >
   > - **`trace 系统调用`**：需要记录系统调用号、参数 → 内核打印 → 返回给用户。`
   >
   >   - 对`trace 32 grep hello README`指令，利用`32`设置按位计算的掩码，在执行`grep hello README`时，将掩码对应跟踪的系统调用过程进行输出
   >
   >   - ```c
   >     // trace 系统调用流程
   >     trace.c				// trace(atoi(argv[1])执行系统调用
   >     user/user.h			// 用户态程序函数trace(int mask)声明
   >     user/usys.S			// usys.pl:entry("trace");会生成usys.S，提供trace系统调用的接口，填入a7寄存器一个系统调用号，然后执行ecall陷入内核
   >     kernel/trampoline.S/uservec		// trap向量入口，保存用户上下文，切换内核栈
   >     kernel/trap.c/usertrap()		// 判断trap类型（syscall、外部中断、异常），如果是系统调用，需要调用syscall
   >     kernel/syscall.c	// 到达内核统一syscall()，所有系统调用都会在此处理，进行分发
   >     kernel/syscall.c	// 根据跳板传进来的a7系统调用号，查询syscall.h和syscall[]注册表中对应的内核函数映射关系，调用具体内核函数实现
   >     kernel/sysproc.c	// 处理具体的sys_trace()函数逻辑，使用argint、argaddr获取参数
   >     kernel/syscall.c	// 在a0写入系统调用函数返回值
   >     kernel/trap.c/usertrapret()	// 准备返回用户态，恢复用户态上下文，切换用户页表
   >     kernel/trampoline.S/userret	// 执行sret指令，真正切换到用户态，继续执行用户程序
   >     ```
   >
   > - **`sysinfo 系统调用`**`：需要把内核里的内存使用信息、进程数拷贝到用户态的结构体里。`
   >
   >   - `sinfo(&info);`在用户态调用
   >
   >   - ```c
   >       uint64 dstaddr;		//  获取用户虚拟地址，第一个实参info，然后在函数内形参进行填充
   >       argaddr(0, &dstaddr); 
   >       // 从内核空间拷贝数据到用户空间
   >       if(copyout(myproc()->pagetable, dstaddr, (char*)&info, sizeof info) < 0) return -1;
   >     ```

11. **==`优化 copyin/copyinstr，通过内核页表同步用户空间映射，简化内核访问用户空间数据的流程；`==**

    > - 内核页表：为每个进程创建的独占的内核页表，**并将用户空间映射到内核页表**，使进程在进入内核时，共享与用户页表具有相同映射关系的内核页表，即共享页表
    >
    > - copyin、copyinstr：
    >   
    >   - **copyin**：从用户空间地址 拷贝一块数据到内核空间（用户传给系统调用一个缓冲区，内核要读这个缓冲区里的数据）
    >   - **copyinstr**：从用户空间地址拷贝一个以 `\0` 结尾的字符串到内核（传递文件路径、命令字符串等参数）
    >   - 区别：
    >     - `copyin` 是固定字节数；
    >     - `copyinstr` 会一字节一字节拷，直到遇到 `\0`，保证内核拿到合法字符串。
    >   - 内核用于从进程用户空间拷贝数据的方法，由于内核页表不具有用户空间的映射（用户空间和内核空间是隔离的），通过对用户的虚拟地址经过**软件模拟查找页表**获取数据的具体物理地址，繁琐性能低
    >   - 系统调用是用户程序向操作系统请求服务的唯一方式，而系统调用通常需要携带参数，并且这些参数往往是指针类型，指向的是用户空间的虚拟地址；比如write用户传递一个buf指针，指向用户空间的一段数据，内核要把这段数据写入文件或设备
    >   
    > - 改写：直接在内核态对用户空间的数据使用解引用操作访问，memmove、memcopy、d[i] = s[i]
    >
    > - 简化：避免了模拟页表查询的过程，避免了虚拟地址到物理地址的转换，避免了数据拷贝，直接在内核空间使用CPU硬件机制自动寻址
    >
    > - 具体实现：
    >
    >   - 提供**页表复制辅助函数**，将src页表的一部分映射关系（页表项，并非物理页）拷贝到dst页表
    >   - 提供**映射解除辅助函数**，根据传入参数的比较来确定页表缩减，并非清除物理页，仅解除映射
    >   - 替换copyin、copyinstr的实现（PGROUNDDOWN、walkaddr、memmove），改为直接在内核态用用户的虚拟地址访问数据
    >   - 在进程的同步内核页表中，**动态地同步用户空间的页表项**，如fork、exec、growproc、userinit在内存变化时，及时更新内核页表
    >     - **fork**：为子进程复制父进程的用户页表时，也需要复制一份独占的内核页表
    >     - **exec**：替换进程镜像时，用户页表已经替换，内核页表需要先解除映射，然后重新映射
    >     - **growproc**：在进程的用户页表空间变化时，内核页表也需要相应变化（借助辅助函数）
    >     - **userinit**：在用户内存初始化结束，需要同步程序内存到进程内核页表
    >   - 对内存边界做严格检查，防止用户空间映射到内核空间或引发的整数溢出问题
    >
    > - GPT5
    >
    >   > 一、原始（未优化）版本在做什么？
    >   >
    >   > xv6 RISC‑V 的默认设计：
    >   >
    >   > - 运行在内核态时使用“纯内核页表”（不包含用户地址空间）。
    >   > - 因此**内核无法直接解引用用户传进来的指针**。
    >   > - copyin / copyinstr 的做法：
    >   >   1. 循环处理（按剩余长度或直到遇到 '\0'）。
    >   >   2. 对**每个**还要访问的用户虚拟地址所在页面调用 **walkaddr()**（软件模拟 SV39 三级页表行走）：逐级读取 PTE，检查有效/权限，得出物理页帧地址 (pa)。
    >   >   3. 计算该页还能复制多少字节。
    >   >   4. 使用内核对物理内存的直接映射（kernel_pagetable 里有恒定的物理内存映射）把 (pa + offset) 当作普通内核指针传给 memmove，从而把数据**拷贝到内核缓冲区**。
    >   >   5. 跨页就再走一次。
    >   >
    >   > 注意：这里“软件页表行走”是纯指令开销 + 多次内存访问（3 层 PTE）。硬件的 **TLB/页表缓存**完全没有被利用，因为当前激活的 satp 指向的是内核页表，硬件看不到用户页表结构；walkaddr 只是普通内核代码在“读页表页”。
    >   >
    >   > ------
    >   >
    >   > 二、瓶颈在哪里？
    >   >
    >   > 对一段长度 L 的用户缓冲区，如果跨了 N 个页面：
    >   >
    >   > - 每个页面都要一次 3 层软件 walk（读 3 个 PTE，可能还伴随 cache miss）。
    >   > - 真实要拷的数据（尤其是系统调用小参数、路径字符串）往往很短，甚至只在一个或两个页面里，但仍要完整执行一次软件走页表逻辑。
    >   > - walkaddr 本身包含不少条件判断、位运算、内存引用；对短字符串来说，走页表的指令数可能 > 真正拷字节的指令数。
    >   >
    >   > 换句话说：对“频繁、小块”的复制（copyinstr 读取文件路径、系统调用字符串参数等），软件页表行走成为主要 CPU 开销。
    >   >
    >   > ------
    >   >
    >   > 三、优化想法的本质
    >   >
    >   > 给“每个进程”再准备一份“内核用的页表”（kpagetable）：
    >   >
    >   > - 这份页表里既有原来**全局内核的所有映射**（内核代码/数据、设备、内核栈……），又**“同步”**加入该进程的**用户空间映射**（用户虚拟地址区间 -> 对应物理页）。
    >   > - 在陷入该进程内核态时，不再用全局 kernel_pagetable，而是切换到它的“**混合页表**”。
    >   > - 这样**内核态可以直接解引用用户虚拟地址 (uaddr)**，让硬件完成地址转换，TLB 建立缓存。
    >   > - copyin/copyinstr 可以改写为：尝试直接用 memmove(dst, (void*)uaddr, n)。若访问到不存在/无权限页，硬件触发 page fault（S 态异常），内核在异常处理路径里判断这是“来自 copy 操作的探测”，再返回错误。
    >   >
    >   > => 关键收益：
    >   >
    >   > - 把“软件页表行走 + 手工拼物理地址”替换成“硬件自动翻译 + TLB 缓存”。
    >   > - 从每个页面一次“软件 3 级 walk”降为一次可能的“硬件页表 walk（首次）+ 后续 TLB 命中（同页多字节）”。
    >   > - 简化代码路径（理想情况下 copy 边界检测和 fault 恢复写少量额外逻辑）。
    >   >
    >   > ------
    >   >
    >   > 四、两个点澄清
    >   >
    >   > (1) “避免了虚拟地址空间到物理地址空间的转换”
    >   >
    >   > - 严格来说没有“避免转换”，而是把“软件显式翻译”换成**“硬件隐式翻译 + TLB 缓存”**。转换仍发生，只是成本低得多，并可缓存。
    >   >
    >   > (2) “避免了数据拷贝”
    >   >
    >   > - 是否能“避免拷贝”取决于你是否进一步采用“零拷贝”策略。
    >   > - 单纯把用户页映射进内核，只是让内核可以直接访问用户内存，并不自动取消拷贝逻辑。
    >   > - 如果内核的使用场景（例如仅只读路径字符串）可以接受“直接读用户内存 + 期间被用户修改的竞态风险（TOCTTOU）”，那么你可以不复制，达到真正零拷贝；否则典型内核仍会复制一份到安全缓冲，防止用户在检查完后篡改（安全、完整性考虑）。
    >   > - 所以：性能提升主要来源仍是翻译方式改变，而不是必然的“取消拷贝”。

12. **寄存器**

    > - PC：程序计数器，**指向下一条执行指令的地址**
    > - mode：当前模式的标志位，supervisor或user mode
    > - **SATP**：指向根页表的物理内存地址
    > - **STVEC**：指向内核中处理trap指令的起始地址，即trampoline page的位置
    > - **SEPC**：在trap过程中保存PC的值
    > - SSCRATCH：
    > - 执行trap时，需要做一些状态记录、操作：
    >   - 保存PC和32个用户寄存器的状态，用于后续恢复程序执行
    >   - 将mode改为supervisor mode，便于内核中各种特权指令的执行
    >   - 修改SATP指向内核页表
    >   - 修改堆栈寄存器，指向内核栈，便于内核函数调用
    >   - 跳入内核代码

13. **trap**

    > 1. trap是什么
    >
    >    > 处理器从正常执行过程，遇到异常（内部）、中断（外部）、系统调用等事件，被迫转入内核的特殊处理流程
    >    >
    >    > - usertrap：用户进程运行时发生，由trampoline page的usertrap引导进入usertrap
    >    > - **kerneltrap**：内核代码执行时发生（内核态中断、异常），直接进入kerneltrap处理
    >
    > 2. **trap执行流程**
    >
    >    > 1. 用户态运行代码，发送trap事件（系统调用、中断）
    >    > 2. CPU自动切换到trampoline page，执行trampoline.S里的trap入口代码uservec
    >    > 3. **trampoline.S保存寄存器到trapframe，切换到内核页表**
    >    > 4. 跳转到内核源码的usertrap函数，执行具体处理（判断trap类型处理）
    >    > 5. 处理完成，调用usertrapret，准备返回用户态
    >    > 6. usertrapret设置寄存器、恢复用户页表，调回trampoline page的userret代码
    >    > 7. **userret恢复用户寄存器**，返回用户态，继续从用户程序执行
    >
    > 3. **ecall**的概念
    >
    >    > RISC-V架构的一个特殊指令，代表环境调用，**请求操作系统服务，所有的系统调用最终都是通过ecall指令触发，进入内核态处理**
    >    >
    >    > - > **ecall指令会让PC跳转到用户空间顶部的trampoline page**，这个page包含了内核的trap处理代码，且所有的进程的trampoline page都映射到同一个物理地址，用户进程不能写入（其PTE_U标志位为空），**并不会切换页表**
    >    >
    >    > - ecall将代码从user mode切换到supervisor mode
    >    >
    >    > - **ecall将PC值保存在SEPC寄存器**
    >    > - **ecall将STVEC拷贝到PC**，即trampoline page的位置
    >
    > 4. trampoline page和trampoline.S
    >
    >    > - trampoline page是xv6分配的一页虚拟内存，通常位于虚拟地址空间的最高处，所有的进程页表都有映射
    >    > - trampoline.S：虚拟内存中的专门用于trap时切换上下文的**汇编代码**
    >
    > 5. uservec、usertrap、usertrapret、userret
    >
    >    > - | 步骤        | 主要工作内容                                   |
    >    >   | ----------- | ---------------------------------------------- |
    >    >   | uservec     | **保存寄存器、切换到内核页表**、跳转c代码      |
    >    >   | usertrap    | 处理系统调用、中断、异常，调度                 |
    >    >   | usertrapret | 设置返回用户态的所有状态                       |
    >    >   | userret     | **恢复寄存器、切换回用户页表**、sret返回用户态 |
    >    >   
    >    > - uservec：入口汇编代码（stvec决定），CPU跳转到此，**保存用户寄存器到trapframe**，**切换到内核页表**，跳转到内核源码的usertrap（只有汇编才能操作寄存器和页表，保证内核安全必须保存现场和切换页表）
    >    >
    >    >   > ```c
    >    >   > /*
    >    >   > 	进入 trap：sscratch 里是 trapframe 地址，a0 是用户态 a0。交换后，a0 变成 trapframe 地址，sscratch 变成用户态 a0。
    >    >   > 	trap 处理：用 a0（trapframe）保存所有寄存器，包括 sscratch 里的用户态 a0。
    >    >   > 	trap 返回：再交换回来，a0 恢复为用户态 a0，sscratch 变成 trapframe 地址。
    >    >   > */
    >    >   > csrrw a0, sscratch, a0	// 交换两个寄存器的值
    >    >   > ......
    >    >   > // 完整保存用户态的a0
    >    >   > // 切换到内核栈和内核页表，并刷新页表缓存TLB：sfence.vma
    >    >   > // 恢复必要的内核上下文（hartid、trap入口）
    >    >   > // 跳转到trap处理函数
    >    >   > ```
    >    >
    >    > - usertrap：内核源码，**判断trap类型，处理具体trap事件（系统调用、中断、异常）**，修改trapframe（如epc），调度（yield）、进程管理（exit）、alarm机制等
    >    >
    >    >   > ```c
    >    >   > // 更改STVEC寄存器，指向kernelvec，属于内核空间trap处理代码位置
    >    >   > // 保存PC，因为有可能当程序在内核中，又被进程调度切换，可能因为系统调用，导致SEPC被覆盖
    >    >   > // 根据scause寄存器判断usertrap原因
    >    >   > // 修改trapframe->epc += 4方便恢复程序时，从下一条指令恢复
    >    >   > // 开中断
    >    >   > // 调用syscall函数：从syscall表单，根据trapframe的a7里保存的编号查找对应的系统调用函数，系统调用还有参数，根据trapframe获取，返回值先写入trapframe的a0寄存器，返回时，会被写回a0寄存器
    >    >   > // 调用usertrapret
    >    >   > ```
    >    >
    >    > - usertrapret：内核源码，trap处理完成后，为返回用户态做准备，**设置寄存器、用户页表**，设置返回信息，跳转到跳板页的出口汇编代码userret（只有内核知道如何安全地准备返回用户态的所有状态，必须通过c代码统一设置）
    >    >
    >    >   > ```c
    >    >   > // 关中断，防止被打断，保证原子性
    >    >   > // 设置trap入口（stvec）为trapframe的uservec，下次trap会跳到uservec
    >    >   > // 准备trapframe的内核相关字段，下次trap进入内核恢复
    >    >   > // 设置sstatus、sepc，保证返回后是用户模式，能响应中断
    >    >   > // 准备用户页表的satp
    >    >   > // 跳转到trampoline.S的userret，完成最后的寄存器恢复和页表切换，调用sret
    >    >   > ```
    >    >
    >    > - userret：返回汇编代码，**恢复用户寄存器，切换用户页表，最终调用sret指令**让CPU返回用户态（只有汇编能直接恢复所有的寄存器和切换页表，sret也只能在汇编执行）
    >    >
    >    >   > ```c
    >    >   > // 切换到用户页表，将用户页表的物理地址a1写入satp，刷新TLB
    >    >   > // 从trapframe中取出用户态的a0，写入sscratch，后续可以将sscratch里的a0写入a0，同时把trapframe地址保存到sscratch
    >    >   > // 恢复所有用户寄存器（除了a0）
    >    >   > // 用sscratch里的用户态a0恢复a0寄存器，保存trapframe地址到sscratch，下次进入内核时，sscratch里就是trapframe地址，方便内核保存恢复寄存器
    >    >   > // sret返回用户态，会用sepc恢复PC，用sstatus恢复用户特权级别和中断状态
    >    >   > ```
    >
    > 6. > xv6的supervisor mode即S模式，不能直接访问物理内存，除了M模式，都需要借助虚拟内存+MMU转换来进程内存访问，只是xv6把内核代码/数据、设备寄存器、物理内存等访问的区域都做了 **恒等映射**
    >
    > 7. **陷阱帧trapframe**
    >
    >    > - trapframe是内核为每个进程分配的一个结构体，用于**保存用户寄存器的快照**，当trap发生时，进程要切换到内核态，为了以后安全返回用户态，必须把所有用户寄存器保存下来，方便上下文切换
    >    > - 通常是进程结构的一个指针，指向内核页表空间的虚拟地址，用户态不能直接访问
    >    > - 32个通用寄存器、PC、satp

14. 汇编问题

    > 1. 哪些寄存器保存函数的参数：a0到a7
    > 2. risc-v是大端存储还是小端存储：**小端存储（低有效字节数据保存在内存的低地址）**

15. alarm执行流程

    > - ```shell
    >   用户态: 运行主程序
    >      │
    >      ├─> 时钟中断 → 内核态 usertrap
    >      │      │
    >      │      ├─> 判断 alarm 条件
    >      │      │      ├─> 满足 → 保存 trapframe，设置 epc=handler，标记 goingoff
    >      │      │      └─> 不满足 → 正常返回
    >      │      │
    >      │      └─> yield
    >      │
    >      └─> 返回用户态，执行 handler
    >              │
    >              ├─> handler 逻辑
    >              └─> sigreturn → 内核恢复 trapframe，清除 goingoff
    >                      │
    >                      └─> 返回用户态，继续原代码
    >   ```
    >
    > - 详述：
    >
    > 1. 用户态调用sigalarm，设置alarm
    >    - sigalarm(2, periodic)会触发系统调用，把时钟周期interval和handler传递给内核（argint和argaddr），内核记录**注册**到进程结构体proc相应字段
    > 2. 用户程序正常执行
    >    - 某个用户程序执行的时刻，会触发时钟中断，然后内核检查当前进程是否设置了alarm
    > 3. 时钟中断触发alarm，进入内核态
    >    - 时钟中断发生，开始**trap**，执行usertrap
    >    - 会通过devintr()检查中断类型，判断时钟中断
    >    - 再**判断alarm条件**：alarm设置，alarm倒计时结束，没有重入冲突
    >    - **设置proc的相应字段**，一方面保存当前用户态寄存器状态（陷阱帧trapframe），另一方面为handler执行做准备（重置ticks、设置goingoff），**epc设置成handler**
    >    - yield让出cpu等待下次调用
    > 4. 返回用户态，执行handler
    >    - 再次调度时，使用usertrapret设置好trapframe，**返回用户态，准备执行handler**
    >    - trampoline的userret恢复所有寄存器，切换页表，执行sret回到用户态
    >    - 由于设置了epc返回地址为回调函数，因此是从handler函数的入口开始执行
    > 5. **handler执行，调用sigreturn恢复**
    >    - handler里做自己的逻辑，最后**调用sigreturn**系统调用，通知内核去恢复到alarm之前的状态
    > 6. 内核恢复原trapframe
    >    - sigreturn系统调用进入内核，**使用之前记录的alarm_trapframe恢复trapframe，并设置goingoff清除**
    > 7. 返回用户态，继续执行时钟中断前的用户程序
    >    - 再次通过trampoline返回用户态，**这次的epc是alarm触发之前的pc**，执行用户程序之前的代码

16. **==`分析中断处理流程`==**

    > > 我分析了xv6内核的trap及时钟中断处理流程，理解了trapframe的保存与恢复机制，**在时钟中断分支中插入了alarm功能**，使得用户进程可以周期性地异步执行自定义任务，同时保证了中断与恢复过程中寄存器和执行流的正确性
    >
    > > **xv6的系统调用和中断处理流程是高度统一的**
    > >
    > > - 用户态发生事件（系统调用或中断），系统调用执行ecall，中断由硬件自动触发
    > > - stvec被设置trampoline.S的uservec，进入trap，跳转到这里
    > > - uservec保存寄存器到trapframe，切换内核页表，跳转到trap.c的usertrap统一处理
    > > - trap.c**通过r_scause判断事件类型**（系统调用、设备中断devintr、其他异常），执行syscall或触发alarm判断，最后通过usertrapret准备返回用户态（alarm倒计时触发时，可能会让出cpu，等待下一次调度）
    > > - usertrapret设置好trapframe、页表、sstatus、sepc，跳转到userret
    > > - userret恢复寄存器，切换回用户页表，执行sret返回用户态，继续执行用户代码
    >
    > - 用户程序执行时（处于U-mode），CPU会定期收到硬件时钟的中断信号，是trap的一种途径
    > - trap发生，**CPU会自动切换到S-mode**，通过trampoline.S自动跳转到内核（或者说stvec指定的地址：uservec），内核会进入trap处理流程，保存用户寄存器到trapframe，暂停当前进程执行
    > - 内核进入usertrap，判断trap类型，再**通过devintr()判断中断类型（时钟、IO等）**
    > - 对时钟中断（which_dev == 2）内核会进行调度、计时操作，在添加alarm后，会分析和判断sigalarm是否调用，当alarm满足条件（设置alarm、计时到达、没有正在执行），内核**备份当前陷阱帧**trapframe、修改trapframe的**返回地址epc为回调函数handler**，实现强制跳转，再设置标志位**避免重入**（实现了单纯的时钟中断变成支持用户态定时任务的机制）
    > - handler执行后，调用sigreturn将trapframe内容恢复，继续用户代码的原始执行流

17. **==`基于时钟中断实现alarm系统调用`==**

    > - **时钟中断是硬件定时器（CLINT或PLIC）定期产生**，内核在启动时设置硬件定时器，会定期触发中断
    > - xv6对所有来自用户态的trap（时钟中断、系统调用、异常）都通过trampoline跳到usertrap做统一处理
    > - 在usertrap中会判断中断类型，做具体的执行（**原本对时钟中断只是让出cpu，即yield()**）
    > - 两个系统调用
    > - **sigalarm**：注册alarm
    >   - 初始化时钟周期、配置alarm回调处理函数、设置倒计时
    > - **sigreturn**：从handler返回alarm之前的状态
    >   - 清除重入标志，通过alarm_trapframe恢复用户进程之前的陷阱帧，包括epc即原来的用户进程代码入口

18. **==`支持用户态触发定时任务`==**

    > - 用户态在调用sigalarm的时候，会传入两个参数：时钟周期、回调函数
    > - 这个用户态的回调函数，就代表定时任务，当然在回调函数结束之前，要求调用sigreturn返回中断前的状态

19. **==基于页表和缺页异常处理机制==**

    > - 页面错误和缺页异常
    >   - 页面错误三种：load**加载**页面错误、store**存储**页面错误、**取指令**页面错误
    >   - 页面错误的有效信息：page fault的虚拟内存**地址**，page fault的**原因**类型，引起page fault的**PC值**
    >   - **页面错误**是硬件层面概念，是CPU发现虚拟地址对应的页表无效
    >   - **缺页异常**是操作系统概念，操作系统对页面错误进行处理，如果是由于页不在内存引起，通过**调页机制**解决，即缺页异常**（Lazy Allocation是CPU检测到页表无效，COW是CPU检测到只读页）**
    > - xv6对所有异常的处理机制是：打印错误信息（trap原因、进程号、异常地址），然后kill进程

20. **==实现内存页的Lazy Allocation与Copy-on-Write==**

    > - 传统分配：进程调用sbrk时，内核**立即分配**物理页并建立映射
    >
    > - **Lazy Allocation**：延迟分配用户空间堆内存
    >
    >   > - 当程序申请内存时，操作系统不会立即分配实际的物理内存页，而是先记录申请，当真正访问的该内存时，才在缺页异常中分配物理页，实现**按需分配**，节省资源，减少无意义的内存分配，支持过度分配
    >   > - 懒分配和页面置换：**懒分配**解决分配时机和内存利用率，**页面置换**关注物理内存紧张时的回收
    >   > - 如何避免内存泄露或安全隐患：所有映射都在进程生命周期内维护，释放时彻底清理页表和物理页，**未分配的页表不会引发panic而是直接跳过continue**
    >
    > - **Copy-on-Write**：写时复制
    >
    >   > - 问题：**fork**系统调用将父进程的所有用户空间内存复制到子进程，但是如果父进程很大，则复制可能需要很长时间，且造成浪费，比如**子进程在fork后，立即exec导致其丢弃复制的内存**，而其中的大部分都从未使用过，如果父子进程都使用同一个页面，其中一个有独立的写操作，需要复制
    >   >
    >   > - 解决：copy on write fork，在父子进程实际需要写物理页时，再进行分配和复制物理内存页面
    >   >
    >   >   - 为子进程创建一个页表，用户内存的PTE指向父进程的物理页，修改所有的PTE标记为**不可写**（进程调用fork时，**父子进程共享相同的物理页**，页表标记为只读）
    >   >   - 当某个进程写入时，CPU检测到是**只读页**，触发缺页异常（**页面错误**）
    >   >   - 内核处理时，发现是**写共享页**，分配一个新物理页，把原数据复制过去，修改出错进程的相关PTE指向新页面，标记为可写
    >   >   - 问题：**COW fork使得释放用户内存的物理页变得复杂：给定的物理页可能会被多个进程的页表引用，只有最后一个引用消失，该物理页才应该被释放**
    >   >
    >   > - 使用页表项预留的位做为COW页标识
    >   >
    >   > - 修改**uvmcopy**
    >   >
    >   >   - 删除直接复制内存的逻辑（kalloc、memmove、mappages）
    >   >   - 对父进程的所有PTE，清除PTE_W位，**设置PTE_COW**
    >   >   - 获取当前父进程PTE标志位，将其物理页直接映射到子进程，保持和父进程一样的权限标志
    >   >   - 记录映射的物理页的**引用计数+1**（避免内存泄露，多个进程映射相同的物理页时做记录，调用**kfree时，只对引用计数-1**，减为零才真正释放物理页）
    >   >
    >   > - 当父子进程有尝试执行写操作，出现页面错误被usertrap捕获，修改usertrap实现在修改物理页时，才创建和映射物理页
    >   >
    >   >   - 添加对页面错误的检测，判断当前访问的地址符合**COW页条件**，进行复制内存
    >   >     - `va < p->sz`、`pte = walk(p->pagetable, va, 0) != 0`、`(*pte & PTE_V)`、`*pte & PTE_COW`
    >   >     - 即地址在进程地址内存范围内，地址有映射，地址有效且是COW页
    >   >
    >   > - 复制物理页，创建新的映射
    >   >
    >   >   - 获取虚拟地址的页表项
    >   >   - 获取映射的物理地址
    >   >   - **获取新分配的物理页地址（把原本的物理页引用计数-1，如果原本的物理页的引用计数是1，直接使用这个物理页）**
    >   >   - 修改新的映射（unmunmap、mappages），恢复写权限，清除COW标志
    >   >
    >   > - copyout检测复制的页是否是一个COW页，如果是，则也需要先复制
    >   >
    >   > - **kalloc和kfree**
    >   >
    >   >   - 原本：一个物理页只会在一个进程有映射，kalloc用来分配，kfree用于回收
    >   >
    >   >   - COW：一个物理页可能存在多个进程的映射，要在最后一个映射释放时，真正释放物理页
    >   >
    >   >     - kalloc：分配物理页，引用计数置为1
    >   >
    >   >     - krefpage：**创建物理页的新映射**，引用计数+1，**需要加锁**
    >   >
    >   >     - kcopy_n_deref：将原物理页**引用计数-1，**并将数据复制到新页上，引用计数为1，返回新物理页
    >   >
    >   >     - kfree：**释放物理页的一个映射**，引用计数-1，如果引用计数=0，释放回收物理页
    >   >
    >   >     - > **一个物理页首先被父进程使用kalloc创建，fork后，子进程调用krefpage声明对应的物理页的引用计数+1，当尝试修改页时，kcopy_n_deref将复制要修改的物理页，创建副本，记录解除旧物理页的映射，引用计数-1，最后kfree保证只有所有的引用者都释放了对该物理页的映射时，才真正释放回收该物理页**
    >   >
    >   > - kalloc.c加入宏
    >   >
    >   >   - 获取物理页id
    >   >   - 物理页数上限
    >   >   - 每个物理页的引用计数数组
    >   >   - 数组对应的锁（自旋锁），防止竞态条件引起的内存泄漏
    >   >   - 获取地址对应的物理页引用数
    >   >
    >   > - 初始化引用计数数组的锁
    >   >
    >   > - 修改kalloc将新分配的物理页的引用计数置1，不要加锁（新分配的页没有映射，不会有进程使用）
    >   >
    >   > - 修改kfree当页面引用计数<=0时释放页面（需要加锁）
    >   >
    >   > - 实现krefpage，加锁
    >   >
    >   > - 实现kcopy_n_deref：cow一个新的物理页地址返回（需要加锁）
    >   >
    >   >   - 如果引用计数>1，原引用计数-1，分配一个新页
    >   >   - <=1，不要分配新页，直接返回该物理页
    >   >
    >   > - **为什么要加锁：**
    >   >
    >   >   > 父进程的物理页p，初始引用计数1，fork后，p的计数2，父进程执行写操作，页面异常触发，p引用计数>1，开始写时复制p，此时p的计数是2，由于进程调度，切换到子进程，父进程的cow中断，子进程立即exec，释放所有的旧物理页，p的计数-1变成1，调度到父进程，父进程继续cow，复制完毕，p的计数-1，然后解除对p的映射，此时，p没有被释放回收，且父进程和子进程都丢失了对p的映射，造成内存泄露
    >
    > - 

21. **==优化空闲物理页分配与磁盘缓存管理==**

    > > 页面置换算法和磁盘缓存管理都关注于磁盘之间的数据流动，使用类似的替换策略，但**页面置换**服务于进程虚拟内存管理，**磁盘缓存**服务于文件IO加速
    > >
    > > **xv6没有swap和页面置换**，只有独立的buffer cache用于缓存块管理
    >
    > - 对**物理内存页分配器**kalloc、free和**磁盘块缓存**buffer cache，优化xv6在多核环境下的锁争用，提升并行性能
    > - 原始设计：
    >   - 所有CPU共享一个空闲物理页链表，且用一个全局锁保护kmem.lock，多核下分配、释放都需要竞争锁，争用严重
    >   - 所有缓存块公用一个链表存储，且用全局锁bcache.lock保护，多进程并发读写时，锁争用严重，不能充分并行
    > - 优化方案：
    >   - 为每个CPU分配独立的**空闲页**链表和锁kmem[NCPU]，分配释放时，优先使用自己的CPU队列和锁，只有自己队列没有空闲页时，才窃取其他CPU队列里的页，大部分操作都可以并行，锁争用降低
    >   - 将**缓存块**改为哈希表结构，每个桶有自己的锁bufmap_locks，锁只关注相关的桶，当查找、释放、分配缓存块时，只影响一个桶，允许多桶并发，减少锁争用，为了安全回收缓存块，再添加驱逐锁eviction_lock，驱逐或分配过程可以并行化，驱逐时使用lru策略，回收最近最久未使用的无引用的块

22. **==重构数据结构和锁策略==**

    > - 将单一的kmem结构体，拓展成kmem[NCPU]数组来为每个CPU维护自己的空闲链表和自旋锁
    >
    >   > - 修改kinit()来初始化每个CPU对应的kmem的自旋锁
    >   >
    >   > - 修改在释放内存时，先**关中断**获取cpuid，再**加锁**将释放的页插入到freelist中（头插法），**释放kmem的锁**，重新**开中断**
    >   >
    >   > - 分配物理内存时，也是需要cpuid，因此关中断，加锁（当前cpu），去找空闲页
    >   >
    >   >   - 自己的freelist有空闲页，直接分配
    >   >   - **自己没有，去窃取其他CPU的空闲页（窃取64页）**
    >   >     - 轮询每个cpu
    >   >     - **加对应CPU的锁**，查看人家的freelist状态（是否有空闲页）
    >   >     - 如果有，一直窃取，**头插法**插到自己的freelist中，并移动其他CPU的freelist指针
    >   >     - 直到窃取指定（64）页数后返回
    >   >   - 解锁，开中断，往物理页中写入垃圾数据（memset）
    >   >
    >   > - 有个缺陷：
    >   >
    >   >   > - 一个CPU1在持有自身的锁，然后去偷CPU2的锁，此时CPU2也在持有自身锁的时候，去窃取CPU1的物理页，造成**环路等待和死锁**
    >   >   >
    >   >   > - 释放自己的锁，再去窃取其他CPU的页，把窃取的页的头指针保存起来，再申请本CPU的锁来CPU的页
    >   >   > - 新问题：在释放自己的锁之后，再去窃取其他CPU的页，本CPU的空闲链表内容可能发生变化（被其他分配释放进程修改），只要偷页和插入页的过程都在各自锁的保护下修改链表，不会发生竞态或数据丢失
    >
    > - **bio.c**
    >
    >   > > 把xv6的buffer cache从全局锁重构成哈希分桶，每桶有独立锁，支持并发访问。为了防止并发驱逐时重复创建缓存块，增加了驱逐锁，并采用**双检锁机制**。通过LRU策略有效复用缓存块，保证每个块号只对应唯一的缓存块，大幅降低多核锁争用，提升了并发性能
    >   >
    >   > - 是什么：**基于哈希桶加锁**以及**避免缓存重复创建的并发驱逐策略**，实现对**缓存块**的**锁优化**及**缓存一致性**
    >   >
    >   > - 实现本质：将xv6的块缓存buffer cache**由全局大锁，重构成哈希分桶+细粒度锁**，保证多核并发高效、缓存唯一性和一致性
    >   >
    >   > - 核心结构和原理：
    >   >
    >   >   1. buffer cache基本结构：
    >   >
    >   >      > 用于缓存磁盘块，减少磁盘IO，加速文件访问
    >   >      >
    >   >      > 由多个buf结构体组成的链表
    >   >      >
    >   >      > 用户进程通过**bread、bwrite、brelse**访问和管理缓存块
    >   >
    >   >   2. 原始结构的问题：
    >   >
    >   >      > 全部缓存块由一个链表管理，用一个全局锁bcache.lock守护
    >   >      >
    >   >      > **多进程或多核**访问时，所有操作都**抢同一个锁**，严重影响并发性能
    >   >
    >   >   3. 优化方案：
    >   >
    >   >      > - **哈希分桶**：用**哈希函数**（将**blockno**和**dev**移位、组合、取余）将缓存分块到13个桶，每个桶都有独立锁bufmap_locks[i]
    >   >      > - **驱逐锁**：引用eviction_lock，**在缓存未命中，且需要全局查找、驱逐、复用缓存块时，加上驱逐锁**，防止并发重复创建
    >   >      > - LRU策略：每个buf记录一个lastuse时间戳，驱逐时选择最久未使用且无引用的块
    >   >
    >   > - 主要流程：bget为例
    >   >
    >   >   1. 缓存命中查找（加桶锁）
    >   >
    >   >      > 用哈希找到对应桶，遍历查找目标块是否在缓存中，如果找到，增加引用计数，释放桶锁，返回buf
    >   >
    >   >   2. **缓存未命中**
    >   >
    >   >      > - **释放当前桶锁，获取全局驱逐锁**
    >   >      > - **再次查找桶**，防止在释放锁期间，有其他进程并发插入了重复块**（双检锁策略）**
    >   >
    >   >   3. 全局LRU查找
    >   >
    >   >      > 查找所有桶，选出最近最久未使用，且无引用的块
    >   >      >
    >   >      > 如果找到，**驱逐当前桶，插入到目标桶**，设置新块的字段
    >   >      >
    >   >      > 释放相关锁，返回buf
    >   >
    >   > - **一致性保证：无论并发多少线程，任何时刻每个块号只对应一个缓存块（双检锁+驱逐锁）**
    >   >
    >   > - **死锁防范：遍历桶时，获取桶锁的顺序一致，驱逐时，只持有一个桶锁和驱逐锁，避免环路等待**
    >   >
    >   > - 接口：
    >   >
    >   >   1. brelse：释放buf，对应桶锁加锁后，减引用计数，没有引用时更新时间戳
    >   >   2. bpin、bunpin：手动加减引用，保证原子性

23. **==降低锁争用开销==**

    > - ###### 对kalloc和kfree来说，**每个CPU都有自己的kmem的锁**，只有kalloc在当前CPU没有空闲页时，才会窃取其他CPU的kmem的空闲页，这时需要竞争对方CPU的锁，争用明显降低（引来潜在的环路等待和**死锁**：在请求对方的空闲链表时，先释放自己CPU的锁，去记录窃取到的空闲链表头指针，只要确保在各自的锁保护下去修改链表，就不会发生竞态或丢失数据）

24. risc-v的概念、arm的概念

25. 字长（字）：CPU一次能够自然处理的二进制数据的位数，32位CPU是32位，4字节，64位CPU是64位，8字节

26. 字节：计算机最小可寻址的存储单元，基本上是8

27. xv6运行在哪里（qemu模拟的硬件环境）

28. risc-v几级流水（取值、译码、执行、访存、写回），会不会出现pc减的情况（分支、异常、中断的指令跳转），pc正常情况每次加多少（4或8）

29. 用户页表的地址（proc.h/proc.pagetable）、内核页表的地址（proc.trapframe.kernel_satp）

30. trampoline的作用：存放着一些共享的汇编代码，映射着kernel/trampoline.S，用于进程进出内核时保存恢复寄存器、切换栈、页表等

31. 为什么要合并内核和用户页表，合并不会有冲突吗：mmu只能解析一个页表，当处于内核态时，只能解析内核态页表，若此时想要访问用户态页表（比如传入了指针），没办法通过硬件mmu解析，只能通过软件方式模拟，但性能低于硬件mmu。内核态页表采用直接映射物理内存，物理内存低地址交由硬件统一编址，因此**内核态页表这部分低地址地址没有用到**，可以复用，不会冲突

32. 为什么有虚拟内存（内存隔离、更大的地址空间、内存映射和文件共享、内存管理、交换和页表置换）

33. 为什么可以使所有虚拟内存之和超过物理内存（核心是**缺页中断机制**，实现了写时复制、懒分配、磁盘交换等）

34. 虚拟地址空间大小取决于**cpu的寄存器宽度**和操作系统设计，32位cpu上限2^32=4GB，64位理论上16EB，但一般实现低48位=256TB，或57位，实际可寻址空间远小于理论值，risc-v的sv39是39位虚拟地址，512GB

35. 操作系统内存管理有哪些模块：**进程管理**、**虚拟内存管理**、**内存分配与释放**、页面置换、共享内存、内存映射文件、页面回收与垃圾收集、内存监控与性能优化

36. xv6没有实现页面置换，避免复杂的swap机制，保持内核最小化

37. xv6是如何分配内存的，用户态通过**malloc申请，维护了内存池**，无法提供合适内存时（当空闲链表中没有足够大块满足malloc请求时，通过sbrk向内核请求更多内存，然后把新内存以块的形式加入空闲链表），**调用sbrk向操作系统申请新的拓展堆内存**（在页表中做标记、提供虚拟内存、通过**kalloc**申请物理内存并做**mappages映射**到虚拟内存，返回虚拟内存），**空闲的物理内存通过链表维护**，kalloc在空闲链表上获取内存`user->syscall->proc.c->vm.c:uvmalloc->kalloc->freelist`

38. xv6的栈（xv6为**每个栈分配两页内存**，4KB*2，一页用于实际栈，另一个**防止溢出**，函数调用会在栈上产生栈帧，都保存有==**返回地址、寄存器值、局部变量**==等）

39. xv6不支持多线程，Linux中多线程每个线程都有自己私有的线程栈（线程释放可以回收，也可以设置线程栈池，方便快速分配栈内存，重复使用）

40. 频繁缺页异常影响性能，避免措施（调整swappiness、关闭非必要后台服务、调整页面大小）

41. 页表的作用（地址映射、内存隔离、虚拟内存管理、内存保护）

42. 三级页表对比一级页表的优劣

    > - 单级页表每个进程都需要维护一个**完整的页表**，其大小是取决于虚拟地址空间的大小，而多级页表**按需分配，**仅为实际访问的页面分配页表，大大减小内存开销
    > - 多级页表能够通过**分级索引**，使得页表可以随着进程需求增长，而不是一次性分配整个页表结构，并且还能**减少对大块连续物理内存的需求**，提升内存管理的灵活性
    > - 多级页表需要**多次进行内存访问**，增加了地址转换的开销，但可以通过转换后备缓冲区**TLB**缓存页表项来缓解
    > - 多级页表额外**增加了页表的管理逻辑**，如分配和释放页表页、地址解析算法，使实现复杂度增加

43. 快表的概念：页表的一种缓存，在虚拟地址转换成物理地址时，**优先访问快表，减少访存次数**

44. 快表原理：**局部性原理（时间局部性、空间局部性）**

45. 用户态分配的内存可以直接被内核态访问到吗（**理论上**用户态和内核态程序，访问的都是自己的虚拟地址空间，无法感知其他程序的虚拟地址空间，但内核态程序的虚拟地址空间采用直接映射，虚拟地址就是物理地址，如果知道用户态分配的内存对应的物理地址，内核态程序可以访问用户程序的数据）

46. 缺页异常如果内存不够，发生什么（xv6在缺页时，**触发panic**，其他操作系统会采取措施来获取物理内存（**换出到磁盘、触发内存溢出OOM机制**））

47. 堆和栈的区别（分配方式、管理方式、生命周期、空间大小、访问速度、用途）

48. malloc的内存池找不到合适内存块，怎么做：会先在页表中做虚拟内存分配，然后使用sbrk向操作系统请求提供足量的虚拟内存，操作系统通过kalloc从内核维护的空闲链表上分配物理内存并使用mappages映射到虚拟内存中

49. xv6有进程（独立的虚拟地址空间，独立的用户栈、内核栈、进程状态（runnable、running、sleeping），通过fork、exec、wait、exit管理线程，调度的基本单位），没有线程（模拟线程只能使用fork创建多进程，但这不共享数据）、没有协程

    > 进程（Process）
    >
    > - **最基本的资源管理单位**。
    > - 拥有 **独立的地址空间**（代码段、数据段、堆、栈），操作系统为其分配资源。
    > - **调度者：操作系统内核**。
    > - **上下文切换开销大**：切换进程需要切换页表、刷新 TLB、保存/恢复寄存器等。
    > - **优点**：稳定性高，互相独立，一个进程崩溃不会影响另一个。
    >
    > ------
    >
    > 线程（Thread）
    >
    > - **CPU 调度的基本单位**。
    > - 属于某个进程，**共享该进程的地址空间和资源**（代码、堆、文件描述符），但有独立的 **栈和寄存器**。
    > - **调度者：操作系统内核**。
    > - **上下文切换开销中等**：不需要切页表，但仍需切寄存器和栈指针。
    > - **优点**：通信开销小（共享内存），并发性好。
    > - **缺点**：一个线程崩溃可能导致整个进程崩溃。
    >
    > ------
    >
    > 协程（Coroutine）
    >
    > - **用户态的轻量级线程**（也叫绿色线程、纤程）。
    > - 由用户程序（或运行时库）管理，而不是操作系统。
    > - **调度者：用户态运行时/程序员**。
    > - **上下文切换开销极小**：只切换少量寄存器和栈指针，不需要陷入内核。
    > - **优点**：极高的并发量，特别适合 I/O 密集型（如网络服务）。
    > - **缺点**：不能利用多核（除非配合线程池），崩溃不会自动隔离。

50. xv6系统调用的流程（系统调用是以函数的形式暴露给进程的）

51. **ecall的作用（关中断、切换特权模型、设置寄存器、跳转trampoline.S）**

52. xv6的上下文切换（进程切换调度）包含哪些内容（未保存在栈中的通用寄存器、栈底指针sp）

53. trampoline.S的作用是什么（包含了两个状态切换函数，保存用户态的通用寄存器状态、载入之前保存的内核态通用寄存器状态，切换页表，刷新快表，切换函数栈），其映射到了每个进程虚拟地址空间的相同地址上

54. xv6有哪些锁（**自旋锁spinlock.c（test_and_set原子指令实现）、睡眠锁sleeplocl.c（自旋锁+循环+sleep实现）**），怎么实现的，各自的使用场景是什么（自旋锁使用于**锁持有时间短**的场景、睡眠锁适合锁持有时间长的场景）

55. 如何降低锁竞争（读写锁、无锁数据结构、**锁拆分**）

56. 如何降低锁争用：xv6的所有buffer cache通过一条链表链接，用一个锁来保证多进程访问的安全性，即只能串行访问文件缓存，通过将缓存块散列到哈希表的桶中，每个桶是一个链表，也拥有一个自旋锁，通过锁拆分，降低锁争用

57. 如何处理死锁问题（**预防死锁**（一个线程避免同时获取多个锁、所有线程按固定顺序请求资源、超时机制）、**避免死锁**（银行家算法通过预先分配资源判断是否导致不安全状态决定是否分配资源）、**死锁检测与恢复**（定期检测是否有循环等待、检测到死锁强制剥夺和重新分配资源打破）、并发库工具、良好设计、测试与代码审查）

58. 为什么xv6的单个文件限制是168k，而不是256这种2的n次方（xv6文件系统的inode在索引文件数据时混合了直接和间接方法，有12个直接索引和1个间接索引，每个直接索引指向一个数据块，共直接索引了12kb，间接索引也指向一个数据块，但数据块上存储256个二次索引，每个二次索引指向一个数据块，共间接索引了256kb，计268kb）

59. 为什么操作系统里要设置文件缓存（减少访问磁盘IO的次数，提升访问速度）

60. 段错误的排查解决方式（使用gdb对段错误产生的核心转储文件进行调试，在发生段错误的位置，使用bt查看调用栈，定位产生段错误的函数，再阅读源码解决问题）

61. 哪些情况会产生**段错误**（数组越界、野指针、栈溢出、对只读内存的写、不正确的类型转换、内存映射文件错误）

62. xv6的前十行是汇编指令，进行配置c语言环境，跳转去执行其他初始化操作

63. xv6的SV39页表地址转换

    > - 一个64位的虚拟地址，只用到了低39位，高25位不用，低39位中的低12位作为偏移量，高27位分3级9位标识物理页码
    > - 页表以三级的树形结构存储在物理内存中，数的根是一个4096字节的页表页，包含了512个页表项PTE，每个PTE中包含该树下一级页表页的物理地址，每一个页表页都包含了512个页表项PTE
    > - 分页硬件使用27位的前9位在根页表中选择PTE，中间9位在下一级页表中选择PTE，最后9位选择最终的PTE，最终的PTE里存放的就是物理页地址（44位），拼接上12位的页内偏移，得到物理地址（56位，上限512GB）
    > - **如果三级PTE有任何一个不存在，页式硬件发生缺页中断**，让内核处理
    > - 如果在一级页表目录中只使用条目0，那剩下的条目及其中间页面不分配，三级设计只分配3*4096字节
    > - 使用快表减少从物理内存加载PTE开销，讲页表条目缓存在TLB中
    > - PTE包含标志位，存在、读、写、执行、用户模式运行访问等
    > - 内核必须将根页表页的物理地址写入satp寄存器，每个cpu有自己的satp，使用自己的satp指向的页表转换后续指令生成的所有地址（虚拟地址）

64. 为什么要采用直接映射

    > 内核地址空间中，低地址部分是IO设备区，然后从KERNBASE开始做直接映射（代码段、数据段），空闲物理内存可分配给内核和用户的物理内存，PHYSTOP上面有多个内核栈（每个进程在内核态有一个栈和保护页），最高处是映射了一个trampoline跳板页（用于处理内核和用户态的切换）

65. 内核自己没有独立的全局栈，而是复用每个进程的内核栈

66. 页表的walk（虚拟地址查找PTE）和mappages（为新映射装在PTE）函数

67. copyout（复制数据到用户虚拟地址空间）、copyin（从用户虚拟地址空间复制）

68. 内核末尾到PHYSTOP之间的物理内存用于运行时分配，使用链表的数据结构将空闲页面记录下来