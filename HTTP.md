1. 简历描述

   > - 项目以i.MX6Q数控系统为平台，针对文件浏览和下载需求，采用Reactor实现了基于HTTP的文件服务
   >
   > 1. 基于==Reactor模式==设计了**事件驱动框架**，包括事件循环、事件分发器等核心组件
   > 2. 实现了**线程池**，采用==one loop per thread==模型，将连接分配给**工作线程**，提高并发性能
   > 3. 设计了==HTTP==**请求解析**和**响应生成**模块，支持缓冲区**动态扩容**，实现了浏览器对静态日志文件的请求服务
   > 4. 使用==IO多路复用技术==（**select、poll、epoll**）实现了高并发I/O处理

2. 话术

   > 本项目以i.MX6Q数控系统为平台，针对工业现场和展会的远程联合测试（在现场调试之前先做预判断，方便现场非技术人员提供系统加工程序的日志信息），提供友好的日志文件浏览和下载服务，在简单业务上又引入了主流的性能拓展。
   >
   > 核心采用Reactor模式，构建了事件循环EventLoop和事件分发器Dispatcher，实现了高效的事件驱动框架。
   >
   > 服务器采用one loop per thread的并发模型，将新连接分配给工作线程workThread处理来提升并发性能。
   >
   > 对来自浏览器的HTTP报文使用请求解析HttpRequest和响应模块HttpResponse，在缓冲区Buffer动态扩容机制下，稳定服务于浏览器端的访问。
   >
   > 底层IO处理采用select、poll、epoll多路复用技术，三种接口都实现了，目的在于学习，多路复用保证了高并发下的稳定性和性能。
   >
   > 整个服务批量部署在i.MX6Q代表的A9系列数控系统上，三种多路复用都通过了1000QPS的简单连接测试，满足了甲方需求。

3. 介绍

   > 本项目针对`Linux`下的日志文件友好访问需求，采用主从多线程的`Reactor`模式和`IO`多路复用技术，实现了一个轻量级的高性能`HTTP`服务器。
   >
   > 核心模块：事件循环、调度器、线程池、`HTTP`请求解析与响应生成、缓冲区、`TCP`连接与服务器管理。
   >
   > 核心特性：`Reactor`事件驱动架构（事件循环`+`事件调度）、多线程`one loop per thread`（主线程、工作线程、线程池的配合）、`IO`多路复用（采用`epoll`，但也实现了`select`、`poll`）、`HTTP`请求解析与响应生成、高效缓冲区管理（动态扩容）、高并发性能（`QPS`可达`1000`，延迟控制在`50ms`）

4. ==`Reactor`==模式的核心思想**

   > 本质是**`事件驱动、非阻塞IO`**，主线程只负责监听IO事件（如`socket`可读可写、新连接），一旦有事件发生就分发给对应的处理器（回调函数）处理；IO多路复用可以高效处理大量并发的连接，避免每个连接都t一个线程或进程

5. 事件驱动框架的事件流转流程

   > - 新连接到来：主线程通过`Dispatcher`监听`listen fd`的可读事件，`accept`新连接。
   > - 连接分发：主线程将新连接分配给某个工作线程的事件循环。
   > - 事件注册：工作线程将新连接的`fd`注册到自己的`Dispatcher`，监听可读/可写等事件。
   > - 事件检测：`Dispatcher`（如`epoll`）检测到`fd`有事件发生，通知事件循环。
   > - 事件分发：事件循环遍历活跃`fd`，调用对应`Channel`的回调函数处理事件。
   > - 业务处理：回调函数完成协议解析、数据收发、响应生成等业务逻辑。

6. 事件循环**`EventLoop`**

   > 作为`Reactor`的核心，在`while`中调用`epoll`、`select`、`poll`等系统调用，等待`IO`事件发生
   >
   > 当事件发生时，事件循环会分发事件给注册的回调处理函数

7. 事件分发器`Dispatcher`

   > 负责将事件（某`fd`可读可写）分发给对应的处理逻辑（读取请求等）

8. 线程池`ThreadPool`

   > 通过**预先创建**多个工作线程，避免频繁创建/销毁线程带来的开销，每个工作线程都运行自己的事件循环`EventLoop`，负责处理分配给他的连接上的事件

9. ==`one loop per thread`==模型

   > 每个工作线程有独立的事件循环（**一个线程一个事件循环**），**主线程**只负责监听新连接，接收到新连接后将其分配给工作线程的事件循环，由该线程负责后续的`IO`和事件处理（读写、协议解析、响应），避免了多线程竞争同一个事件循环的锁，提高了多核利用率和并发处理能力

10. 如何区分线程类型

   > 线程池结构体里有主事件循环，能找到对应的线程`ID`，其他工作线程比较`pthread_self`进行判断
   >
   > 线程池管理所有的工作线程，主线程和工作线程的职责和入口函数不同

11. 并发性能

    > `QPS`每秒请求数、并发连接数、延迟，这个受硬件、操作系统、网络影响，单机环境下的`Reactor`线程池模型，可以支持成千上万的并发连接
    >
    > 经过`wrk`测试，启动`4`线程维护`100`个并发连接，当持续向目标服务器发送`HTTP`请求，然后收集响应事件、成功、失败等数据，统计出的`QPS`可达`1000+`，平均延迟`50ms`左右，已经远超设计初衷

12. 请求解析模块

   > 能够从`TCP`流中解析出`HTTP`请求行、请求头、请求体（当然针对日志浏览的请求，没有请求体），而且我们也只是实现了`GET`请求，能够正确提取出方法、`URL`（浏览器期待的文件或目录）、版本、头部

11. 响应生成模块

    > 根据从请求中解析出的`HTTP`的请求数据细节，动态生成并拼接出符合`HTTP`协议的响应，包括状态行、响应头、空行、响应体（在请求文件的时候，填充为文件内容）

12. 缓冲区动态扩容

    > 构造了一个`buffer`缓冲区模块，在应对大文件或长请求的时候支持自动扩容

13. 缓冲区的作用

    > 网络数据基于流，数据可能分次到达或发送，用缓冲区临时存储从`socket`中读取的数据，以及待发送的数据，尽量保证数据的完整性和高效处理
    >
    > 发送的时候，其实是规避了`TCP`粘包的问题

14. 静态日志文件服务

    > 在返回目录结构时，一开始以文件后缀进行了一遍过滤（`.log`、`.txt`、`.log.txt`），我后续取消了这个限制来使这项目更符合通用文件服务

15. `IO`多路复用的作用

    > 传统阻塞`IO`模型每个连接一个线程，资源消耗大，难以支撑高并发，`IO`多路复用允许单线程同时监听多个`socket`的`IO`事件

16. `Dispatcher`负责什么

    > 负责注册、监听、分发`socket`事件（新连接到达、数据可读可写）

17. `epoll`的处理流程

    > 事件注册：当新的`socket`连接建立之后，通过`epoll_ctl`注册到`epoll`实例，监听其读写事件
    >
    > 事件循环：主线程或工作线程调用`epoll_wait`等待事件发生
    >
    > 事件分发：一旦有事件发生，`epoll`返回活跃的`fd`列表，事件循环遍历这些`fd`，然后调用对应的回调函数进行处理（读取`HTTP`请求、发送响应）
    >
    > 高并发处理：由于`epoll`高效，单线程即可管理成千上万个连接，再加上线程池模型，每个线程都能独立高效处理大量连接
    >
    > 封装了三种`IO`多路复用机制（`select`、`poll`、`epoll`），通过统一的`Dispatcher`接口注册和监听`socket`事件。事件循环采用`epoll_wait`高效等待活跃的`fd`，将事件分发给对应的回调函数，实现了单线程高效管理成千上万个并发连接。
    >
    > 主线程：只负责`accept`新连接，不处理具体业务。
    > 工作线程：每个线程有自己的事件循环（`event loop`），用`epoll`等技术管理和处理分配给它的所有连接，当某个连接有事件（如可读/可写）时，`epoll`会通知线程，线程再去处理对应的连接和数据。。
    > 每个工作线程内部是“单线程高并发”，整个服务器是“多线程+每线程高并发”。

18. `Reactor`模型，传统阻塞`IO`服务模型

    > ![img](https://pic3.zhimg.com/v2-f9d7285211b70601aa33a5da7ab04b08_r.jpg)
    >
    > - 网络请求经过：服务器网卡、内核、连接建立、数据读取、业务处理、数据写回；其中`accept`、`read`、`write`都需要操作系统内核提供的系统调用，由内核与网卡进行数据交互，而这些`IO`调用消耗高，因此可对每个连接使用独立的一个线程处理，因此`N`个连接需要`N`个线程资源。
    >
    > - **阻塞`IO`**：用户线程一直阻塞等待网卡数据准备就绪，大部分时间都在等待`IO`事件就绪
    >
    > - **非阻塞`IO`**：如果数据未就绪，用户线程直接返回，应用层轮轮询读取、查询，直到成功读取数据
    >
    > - **`IO`多路复用（感官）**：通过一个阻塞的系统调用（如 `select`、`poll`、`epoll`），**委托内核同时监测多个文件描述符的状态（核心）**（本质是检测`fd`对应的**缓冲区**的可读、可写、异常等）。一旦有文件描述符就绪，系统调用返回，应用程序即可针对这些就绪的文件描述符进行通信操作。这样在单线程或单进程的场景下，也能实现对多个客户端的并发处理。
    >
    > - **事件驱动（大脑）**：等待事件发生（事件循环`eventLoop`），检测到事件后，调用对应（事件分发`dispatcher`）的处理函数（回调函数、`handler`）
    >
    > - **`Reactor`模型**：`IO`复用`+`线程池：通过一个或多个输入同时传递给服务处理器的服务请求的事件驱动处理模式，**核心是事件驱动**，三种形式（单线程模型、多线程模型、主从多线程模型）
    >
    >   - *单线程模型*：一个`reactor`线程用`select`等监听所有事件，就绪后，直接在同一个线程中调用对应的处理器，`IO`操作和业务逻辑都在一个线程，任何耗时操作会阻塞整个服务器
    >
    >   - <img src="https://pic2.zhimg.com/v2-dae5287666bb6dd3d361d4f673a35e0f_r.jpg" alt="img" style="zoom: 33%;" />
    >
    >   - *多线程模型*：一个`reactor`线程负责`IO`多路复用，当某连接有读写事件，`Reactor`负责分发事件，然后交给线程池的工作线程处理业务逻辑，`reactor`仍然是单线程，若连接数多，仍会阻塞
    >
    >   - <img src="https://picx.zhimg.com/v2-1161fb3d6420a5b32b06321ba288aabb_r.jpg" alt="img" style="zoom:28%;" />
    >
    >   - *主从多线程模型*：主`reactor`只负责监听连接请求，把新连接分配给某个从`reactor`，从`reactor`负责监听自家管理的连接的读写事件，并分发给对应的`Handler`，而工作线程由从`reactor`调度来处理具体业务逻辑，主从分离充分利用多核`CPU`，连接分布在多个`Reactor`
    >
    >   - <img src="https://ask.qcloudimg.com/http-save/8495412/16izwrc9gd.jpeg" alt="img" style="zoom:50%;" />
    >
    >   - ```shell
    >     # MainReactor对象通过select监控连接建立事件，收到后，通过accept接收，处理建立连接事件，然后MainReactor分配子线程给SubReactor处理
    >     # SubReactor将连接加入连接队列进行监听，创建Handler处理各种连接事件
    >     # 有新的事件发生，SubReactor会抵用连接对应的Handler进行响应
    >     # Handler通过read读数据，分发给线程池Worker进行业务处理
    >     # 线程池分配独立的线程完成真正的业务处理，将响应结果发给Handler
    >     # Handler收到响应，再send返回给Client
    >     ```
    >
    > - 每个连接都需要独立的线程完成处理完整操作，当并发量比较大，需要创建大量线程；若没有线程可读，当前线程就阻塞在`read`上；都是资源浪费
    >
    > - 解决方案：
    >
    >   1. 基于`IO`复用模型：每个连接共用一个阻塞对象，应用程序只需要在一个阻塞对象上等待，无需阻塞等待所有连接。当某条连接有数据可处理，操作系统通知应用程序，线程从阻塞状态返回
    >
    >   1. 基于线程池复用线程资源：不必再为每个连接创建线程，将连接完成后的业务处理分配给线程进行处理，一个线程可以处理多个连接的业务

19. ==`IO`多路复用==方式的实现细节

    > 1. 共通点
    >
    >    > 都是用一个系统调用来监听多个文件描述符
    >    >
    >    > 委托内核监控`fd`的状态
    >    >
    >    > 当事件发生时，返回给用户态，由用户态去操作就绪的`fd`
    >    >
    >    > 主要区别在于**内核如何管理和返回就绪的`fd`**
    >
    > 2. `select`
    >
    >    > - 原理：`select`通过`fd_set`结构体（用户态传入的**位图数组**）管理所有监听的`fd`，每次调用都要把所有`fd`集合从用户态**拷贝**到内核态（内核遍历，逐个检查状态，然后返回），用户态需要逐个扫描中找出就绪的`fd`，效率低（内存拷贝开销大），`fd`数量也有限（`1024`）
    >    > - 实现流程：
    >    >   1. 初始化`fd_set`（`FD_ZERO`），将所有需要监听的`fd`加入集合（`FD_SET`）
    >    >   2. 循环调用`select(maxfd+1, &readfds, &writefds, NULL, timeout)`等待事件
    >    >   3. 返回后遍历`fd`集合，判断哪些`fd`有事件发生，分发给对应的回调处理
    >    >   4. 事件处理完毕后，继续下一轮循环
    >
    > 3. `poll`
    >
    >    > - 原理：`poll`用一个`pollfd`**结构体数组**（包含`fd`、关心的事件类型）管理所有监听的`fd`，没有`fd`数量限制，但同样内核和用户态每次都要遍历整个数组，效率一般
    >    > - 实现流程：
    >    >   1. 用`pollfd`结构体数组保存所有监听的`fd`及事件类型
    >    >   2. 调用`poll(pollfds, nfds, timeout)`等待事件
    >    >   3. 返回后遍历`pollfd`数组，检查**`revents`字段**，找出有事件的`fd`，分发处理
    >    >   4. 事件处理完毕，更新`pollfd`数组，进入下一轮循环
    >
    > 4. `epoll`
    >
    >    > - 原理：`Linux`高效的`IO`多路复用机制，分两个阶段（注册（`epoll_ctl`把要监听的`fd`注册到内核的**红黑树**中）、等待（`epoll_wait`，内核基于事件通知的回调机制，在`fd`状态变化时，通过**就绪队列**（链表）返回）），因此**支持事件驱动**，内核直接通知的是活跃的`fd`，无需遍历所有的`fd`,支持**水平**（`LT`，只要`fd`就绪，每次`epoll_wait`都会返回）、**边缘**触发（`ET`，只有状态变化时才返回，需要非阻塞`IO`+循环读写）
    >    > - 实现流程：
    >    >   1. 创建`epoll`实例（`epoll_create`），获得`epollfd`
    >    >   2. 用`epoll_ctl`注册、修改、删除需要监听的`fd`及其事件类型
    >    >   3. 调用`epoll_wait(epollfd, events, maxevents, timeout)`等待事件
    >    >   4. 返回后遍历`events`数组，直接获得有事件的`fd`，分发给回调处理
    >    >   5. 事件处理完毕后，继续下一轮循环
    >
    > 5. GPT5
    >
    >    > #### 一、概念定位与总体背景
    >    >
    >    > > - 在高并发网络服务器中，核心任务是同时管理大量处于不同状态的套接字（socket）。I/O多路复用（IO Multiplexing）是一种**让单（或少量）线程/进程同时等待多个文件描述符就绪事件的机制**，相比**一连接一线程/进程**的**阻塞模型**，极大减少了上下文切换与内存开销。
    >    > > - 核心思想：进程**把关注的 FD 列表交给内核**，**内核阻塞等待**事件发生，一旦有一个或多个 FD 就绪，内核返回这些信息，用户态再逐个处理。
    >    >
    >    > #### 二、使用层面
    >    >
    >    > 1. select
    >    >
    >    >    - 接口：`int select(int nfds, fd_set *readfds, fd_set *writefds, fd_set *exceptfds, struct timeval *timeout)`
    >    >
    >    >    - 需要用**位图** fd_set 表示关心的 FD 集合，最大 FD 数常受 FD_SETSIZE（典型 1024）限制（可改宏但会影响性能与兼容）。
    >    >
    >    >    - **每次调用都要重新把 fd_set 从用户态拷贝到内核；select 返回后需要线性扫描 fd_set 找出就绪 FD。**
    >    >
    >    >    - 时间复杂度：O(n) 用户态扫描 + O(n) 内核检查，内核也要遍历全部 FD。
    >    >
    >    >    - 缺点：FD 上限低、重复拷贝、全量扫描。
    >    >
    >    > 2. poll
    >    >
    >    >    - 接口：`int poll(struct pollfd *fds, nfds_t nfds, int timeout)`
    >    >
    >    >    - 取消了位图限制，使用 **pollfd 数组**，可支持更大 FD 值（实际受进程 ulimit 和内存约束）。
    >    >
    >    >    - **每次调用仍要把整个数组传给内核，返回后仍需 O(n) 扫描 pollfd 查看 revents。**
    >    >
    >    >    - 本质仍是“水平触发 + 全量遍历”，改进只是去掉 1024 限制 和 结构更灵活。
    >    >
    >    > 3. epoll
    >    >
    >    >    - 接口族：`epoll_create`、`epoll_ctl(ADD/MOD/DEL)`、`epoll_wait`。
    >    >      - **分离“注册监控集合”和“等待事件”两个阶段：**
    >    >        - epoll_ctl 时把 FD 和关注的事件挂进内核中的数据结构（一次性），**无需每次提交全量列表**。
    >    >        - epoll_wait 时**内核直接返回已就绪的“就绪队列”列表**，不做 O(n) 全表遍历。
    >    >
    >    >    - 时间复杂度：理想情况下接近 O(就绪FD数量)，大幅优化大量 FD 但少量活跃的场景。
    >    >
    >    >    - 支持边缘触发（Edge Trigger, ET）与水平触发（Level Trigger, LT），**默认 LT**。ET 可减少重复通知，降低用户态遍历次数，但开发难度高。
    >    >
    >    >    - 能力：**可与非阻塞 IO 配合，实现高效 Reactor 模式**。
    >    >
    >    > #### 三、底层实现原理（内核视角）
    >    >
    >    > 1. select/poll 内核路径概要
    >    >
    >    >    - 用户传入 FD 集（select 的 fd_set 位图或 poll 的数组）。
    >    >
    >    >    - 内核复制用户数据结构到内核空间。
    >    >
    >    >    - 进入 do_select/do_poll **循环**：
    >    >
    >    >      a. 为**每个 FD** 调用其 file_operations->poll 回调（例如 socket 的 sock_poll），该回调会：
    >    >
    >    >      - 检查当前状态**是否就绪**（如接收缓冲是否非空）。
    >    >      - 若未就绪，把当前进程**添加进等待队列**（wait queue）。
    >    >
    >    >      b. 如果全部都未就绪，当前任务进入可中断睡眠（schedule）。
    >    >
    >    >      c. **被某个底层驱动（如网络协议栈）唤醒后，重新遍历所有 FD 再做一轮检查（水平触发）**。
    >    >
    >    >    - 返回后用户态再遍历一次找出就绪的。
    >    >
    >    > 2. epoll 内核核心数据结构（以 Linux 为例）
    >    >
    >    >    - **epoll 实例对应一个内部对象 struct eventpoll**：
    >    >      a. **红黑树（RB-tree）**：保存所有被关注的 FD（查找 O(log n)）。
    >    >      b. **就绪列表（ready list）**：双向链表（list_head）保存已经被触发的事件。
    >    >      c. **等待队列（wait queue）**存放在 epoll_wait 睡眠的进程。
    >    >
    >    >      - 注册（epoll_ctl ADD）：
    >    >        - 把目标 FD 的 “epitem” 节点插入 RB-tree。
    >    >        - 通过 ep_ptable_queue_proc 把一个回调（**回调函数 + epitem**）挂入目标文件的 wait queue 中。
    >    >
    >    >      - 事件发生：
    >    >        - 例如网络包到达 -> **协议栈把数据放入 socket 接收缓冲 -> 唤醒 wait queue**。
    >    >        - epoll 的回调被调用：检查事件掩码，若满足且 epitem 当前**不在 ready list，则放入 ready list**（避免重复）。
    >    >        - **若有等待在 epoll_wait 的进程则唤醒。**
    >    >
    >    >    - epoll_wait：
    >    >      - 如果 ready list 非空，**直接拷贝**就绪事件到用户数组并可能清理（对 LT 保留/重复触发，对 ET 需要用户态读空缓冲避免丢事件）。
    >    >        - **若 ready list 为空，则把当前进程挂入 epoll 自身等待队列并睡眠，直到有新事件或超时。**
    >    >
    >    > 3. 边缘触发（ET）与水平触发（LT）原理区别
    >    >
    >    >    - LT（默认）：只要资源仍然“可读/可写”，每次 epoll_wait 都会返回该 FD。
    >    >
    >    >    - ET：只在状态从“不可”变为“可”那一瞬间通知一次；如果用户没有把缓冲区读空/写满，就不会再次通知——需要循环 read/write 到返回 EAGAIN。
    >    >
    >    >    - 好处：减少无谓的重复事件投递；坏处：写代码更复杂，容易因一次没读完导致事件“丢”表象（实为用户没继续读导致看不到新通知）。
    >    >
    >    > 4. 为什么 epoll 不再需要反复拷贝大数组
    >    >
    >    >    - 监控集合常驻内核（通过 epoll_ctl 维护），**等待阶段只关注 ready list 的移动和用户空间 output buffer 的写出。**
    >    >
    >    >    - 大规模 FD（比如 10 万）且活跃度低时优势极其明显。
    >    >
    >    > #### 四、性能复杂度与适用场景
    >    >
    >    > 1. select/poll
    >    >
    >    >    - O(n) 扫描不可避免。n 大时性能线性下降；FD 稀疏活跃时浪费明显。
    >    >
    >    > 2. epoll
    >    >
    >    >    - 增删：O(log n)（RB-tree）
    >    >
    >    >    - 事件分发：**接近 O(k)**，k 为就绪 FD 数。
    >    >
    >    >    - 上限：理论上受内核文件描述符限制（/proc/sys/fs/file-max）与进程 RLIMIT_NOFILE。
    >    >
    >    > 3. 高频新增/删除场景
    >    >
    >    >    - epoll_ctl 不是免费：频繁增删 FD（例如短连接极高 churn）时 epoll 的优势会被削弱，仍远好于全表扫描但要考虑分配/回调成本。

20. 一个工作线程、一个事件循环、多个连接的关系

    > - 一个工作线程对应一个事件循环，但一个事件循环可以同时管理和处理多个连接
    > - 事件循环内部通过`IO`多路复用机制，监听和管理所有分配给他的所有`socket fd`

21. `Buffer`中读`socket`的数据，`readv`

    > - 使用分散读的策略，一次性把数据读取到多个不连续的缓冲区（`iovec`数组）中；
    >
    > - 如果数据量小，数据全部落在主`buffer`中；数据量大，主缓冲区写满后，剩下的数据直接落到临时缓冲区（`40KB = 40960bytes`），再`append`到主缓冲区（实现了自动扩容）
    > - 避免多次`read`和多次内存拷贝

22. 常用的一些函数

    > ```c
    > // 把 s 指向的内存区域的前 n 个字节设置为指 c，按字节写
    > void *memset(void *s, int c, size_t n);
    > // 把 src 开始的 n 个字节拷贝到 dest
    > void *memcpy(void *dest, const void *src, size_t n);
    > // 和 memcopy 类似，但支持内存区域重叠
    > void *memmove(void *dest, const void *src, size_t n);
    > // 比较两个内存区域的前 n 个字节
    > int memcmp(const void *s1, const void *s2, size_t n);
    > // 在内存卡 s 的前 n 个字节中查找指 c
    > void *memchr(const void *s, int c, size_t n);
    > // 在一块内存 haystack 中查找子串 needle
    > void *memmem(const void *haystack, size_t haystacklen, const void *needle, size_t needlelen);
    > ```

23. epoll相对select、poll的优势

    > - 对待检测集合的处理，epoll是基于红黑树来管理的，select和poll基于线性的数组、位图Bitmaps、链表等
    > - epoll是基于回调机制，处理效率不会随检测集合改变而降低，而select和poll因为需要遍历整个线性空间，因此效率和集合大小成正比
    > - select和poll存在内核、用户空间的拷贝，而epoll使用了共享内存，更快
    > - select和poll需要对返回的集合进行遍历判断才能确定就绪状态，而epoll可以直接得到就绪的集合
    > - epoll和poll检测的最大文件描述符没有限制，select限制1024，但也可以解除

24. SimpleHTTP框架

    > - main函数负责切换目录，初始化监听的套接字（`socket、setsocketopt、bind、listen`），然后添加到epoll的红黑树中（`epoll_create、epoll_ctl`），启动检测
    > - epoll中检测事件（epoll_wait），检测到就绪的fd进行fd判断，如果是负责监听的fd，创建一个线程（说明有新连接，accept，并加入epoll检测集合，epoll_ctl）；如果不是，就创建一个线程（用于接收HTTP请求数据、解析HTTP请求（判断请求类型和资源路径）、组织数据、发送数据（目录、文件））
    > - 线程参数通过结构体传递（包含fd（操作具体的socket）、epfd（操作epoll）、tid）
    > - 文件通过sendfile和read、send发送，目录通过HTML表格动态生成并发送
    > - 支持URL中文字符解码，文件类型判定，404错误处理等基础功能

25. epoll的水平触发和边沿触发

    > - 水平：默认缺省的方式，支持非阻塞和阻塞socket，只要fd对应的读写缓冲区可读或可写，事件触发，epoll_wait解除阻塞，读事件是被动的（必须有读事件才能判断数据到达，读事件检测必须），写事件是主动的（一般缓冲区都可些，不必检测）
    > - 边沿：高速工作模式，只支持非阻塞socket，当fd就绪时，内核通过epoll通知使用者，但只做一次通知，很大程度减少epoll事件被重复触发的次数，效率比LT高；读缓冲区有数据进入，读事件触发一次，只要没有新数据，读事件就不会再次触发，只通知一次；缓冲区可写时，写事件触发一次，解除epoll_wait的阻塞；
    > - epoll在边沿模式下，套接字要设置成非阻塞模式：如果不设置非阻塞，在一次事件通知后卡死在某个系统调用里，或读写不完全但再也接收不到事件通知，导致程序挂死

1. 什么是Reactor模式

   > Reactor 是一种**事件驱动**的设计模式，核心是将 I/O 事件的**检测**（demultiplexing）与事件**处理**==解耦==。一个或多个事件循环监听文件描述符的事件，一旦就绪，调用注册好的回调（handler）去处理具体逻辑。

2. 如何基于Reactor模式设计事件驱动框架

   > 我在这个项目里落地 Reactor 框架时，先把“事件检测”和“事件处理”彻底拆开。
   >
   > 底层我定义了一套 Dispatcher 抽象，里面是 function pointer：init/add/remove/modify/dispatch/clear，这样 select、poll、epoll 三个实现只要各自填充结构体就能被 EventLoop 统一驱动。EventLoop 本身只做两件事：循环调用 dispatcher->dispatch 等待就绪事件；处理一条线程安全任务队列（新增/修改/删除 Channel）。所有对内核事件表的实际操作都被封装成任务丢进这个队列，保证只在所属事件循环线程里执行，避免多线程直接操作 epoll/select 造成竞态。
   >
   > 单个 fd 我用 Channel 封装：保存 fd、本线程所属的 EventLoop、关注的事件位（读/写）、以及 read/write/destroy 回调指针。为了 O(1) 定位，我维护了一个按 fd 下标的 ChannelMap 动态数组，连接增多时调用 makeMapRoom 扩容，不用红黑树或哈希是因为 fd 在这个场景下相对稠密，小数组访问更快。事件分发时 dispatcher 返回就绪集合后，EventLoop 直接用 fd 下标取 Channel，检查可读/可写位并调用对应回调。
   >
   > 跨线程唤醒我用了 socketpair。主线程或其他工作线程往目标 EventLoop 的写端写一个字节，读端在该 EventLoop 初始化时注册成可读事件；当有新任务（例如新连接 Channel 注册、修改关心事件等）被投递时，先把任务节点挂到队列尾，再写唤醒字节，阻塞在 epoll_wait/select 的 loop 立即被唤醒，随后在 eventLoopProcessTask 阶段顺序消费这些操作。这样不需要信号，也不必频繁轮询。任务队列处理策略是“摘链”——加锁取出整个链表再逐个执行，降低锁持有时间。
   >
   > 线程模型是 one loop per thread。主线程只负责监听和 accept（监听 Channel 也在一个 EventLoop），accept 到的连接先设置非阻塞，然后选一个工作线程的 EventLoop（简单 round-robin），在主线程创建好 Channel 结构但不直接注册，把一个 ADD 任务投递到目标 EventLoop。目标线程被唤醒后在自己的 dispatcher 中 add，后续该连接所有 I/O 事件都在那个线程的回调里完成，避免 fd 在不同线程间迁移。这样主线程压力很小，也不存在“多个线程同时操作同一个 epoll 实例”的问题。
   >
   > 生命周期管理上，Channel 的销毁统一通过任务（DELETE）放回事件循环执行，执行时调用 destroyCallback 做资源回收，防止回调栈里直接 close 造成还在遍历事件数组时 fd 被重用的风险。错误/关闭检测在读回调里处理：读到 0 或致命错误则投递删除任务。为减少频繁调整事件集合的系统调用，我在修改事件时只在关心集发生实际变化时才发起 dispatcher->modify。
   >
   > 这个设计的关键点是：
   >
   > - 用最小的抽象（Dispatcher）屏蔽多路复用差异；
   > - 用 Channel 把事件与回调绑定；
   > - 用任务队列串行化 dispatcher 的所有结构性操作；
   > - 用 socketpair 实现可靠唤醒；
   > - 配合 one loop per thread 把并发扩展出去。
   >
   > > 整个链路保证“就绪检测 → 事件分发 → 业务回调 → 可能的结构变更”始终在事件循环线程内按序执行，线程间只传递任务而不直接共享底层 I/O 状态，从而保持简单和可预测。

3. 

