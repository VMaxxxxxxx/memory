1. `NULL`和`nullptr`的区别

   > **`NULL`**：
   >
   > - 传统 C 宏，通常定义为 `#define NULL 0` 或 `((void*)0)`。
   > - 在 C++ 中本质上就是整数 `0`，可能引发和重载函数、整数混淆的问题。
   >
   > **`nullptr`**（C++11 引入）：
   >
   > - 专门的指针常量，类型是 `std::nullptr_t`，可以隐式转换成任意指针类型，但不能转换成整数类型。
   > - 不会和整数混淆，能更好地区分指针语义。

2. `sizeof`获取变量或类型的大小

   > `sizeof(指针变量)` → **指针本身占的字节数**（和指向类型无关）。
   >
   > `sizeof(*指针变量)` → **指针所指向的对象大小**（和类型有关）。
   >
   > `sizeof(数组名) / sizeof(数组名[0])` → **整个数组大小**（和数组长度、元素类型有关）。
   >
   > `sizeof(类型名)` → **该类型的大小**。
   >
   > - 数组名在大多数表达式里会 **退化为指针**（指向首元素的地址），但在 `sizeof`、`&`、`typeof` 等运算里 **不会退化**。
   > - 把数组传给函数，数组会退化成指针，sizeof结果变化
   >
   > `sizeof(空类)`：1字节，c++要求每个对象有唯一地址

3. 常量指针、指针常量、常量指针常量的区别

   > 作用域、声明周期、初始化、存储位置
   >
   > ```c
   > const int* p;	// 常量指针
   > int const* q;	// 常量指针
   > int* const m;	// 指针常量
   > const int* const x; // 常量指针常量
   > int const* const p;	// 常量指针常量

4. `static`在不同场景下的作用（局部变量、全局变量、全局函数、类静态成员、类静态函数、类静态成员初始化）

   > 在不同位置的核心作用，是为了**延长生命周期、限制作用域、限制链接性**，在类中，强调归属于类而非对象
   >
   > - 静态变量：程序启动时分配内存，保存在全局数据区，默认0，作用域是整个程序声明周期
   > - 静态函数：限制函数的连接性质为**内部链接**，只能在本文件使用
   > - **静态成员变量：所有类的对象共享，必须在类外单独定义**
   > - **静态成员函数：属于类，不属于对象，直接使用类名::函数名调用，不能访问非静态成员变量和非静态成员函数（因为没有this指针）**
   > - 静态局部变量：作用域仅限于代码块，其他与全局静态变量一致

5. `extern`的作用

   > - 声明外部变量或函数：告诉编译器变量或函数已经在别的文件中有定义，此处只引用，不分配内存
   > - 函数默认是extern链接，通常不写extern，主要对全局变量的作用域进行拓展
   > - extern "C"提供C调用的C++接口，实现混合编程（C++支持函数重载，避免在编译的时候给函数名加上类型详细）
   > - 函数声明默认带有extern

6. `const`的作用

   > - 修饰变量，变量只读，不可被修改
   > - 修饰指针（指针常量、常量指针、常量指针常量）
   > - 修饰函数参数（不会修改实参）、返回值（不会修改成员变量）
   > - **修饰引用：`const int a = 10; int& b = a;`非const引用不能绑定到const对象**

7. **强制类型转换**（`static_cast`、`dynamic_cast`、`reinterpret_cast`、`const_cast`）

   > - static_cast：
   >
   >   > - **在编译期检查类型兼容，但没有运行时类型检查**，不保证转换的安全性
   >   > - 编译时类型转换
   >   > - 基本类型转化、类层次间的指针或引用、void*和指针间
   >   > - 向下转换不安全，向上转换安全
   >
   > - dynamic_cast：
   >
   >   > - 类层次间的安全**向下**转换，有运行时检查类型
   >   > - 必须有虚函数表，即类至少有一个虚函数
   >   > - 转换失败会返回nullptr（指针）或抛出异常（引用）
   >
   > - const_cast：去掉cosnt或volatile属性，常量指针或引用**转换成非常量指针或引用**，不改变指向
   >
   > - reinterpret_cast：
   >
   >   > - 任意类型间的**低级转换**，不做检查，不安全
   >   >
   >   > - 指针和整数

8. `struct`和`class`的区别

   > - 相同：如果结构体、类内部没有定义任何构造函数，编译器会生成默认的无参构造函数
   > - 区别：
   >   - struct通常用于组织一组相关数据，class用于封装数据和操作的对象
   >   - struct中默认成员是public，class默认private
   >   - struct默认共有继承，class默认私有继承

9. 函数指针和指针函数的区别

   > - **函数指针：指向函数的指针变量**，存储函数的地址，动态选择要调用的函数，适合于回调
   >
   >   ```c
   >   // 返回类型 (*指针变量名)(参数列表)
   >   int add(int a, int b) {
   >       return a + b;
   >   }
   >   int subtract(int a, int b) {
   >       return a - b;
   >   }
   >   
   >   int main() {
   >       // 定义一个函数指针，指向一个接受两个int参数、返回int的函数
   >       int (*operationPtr)(int, int);
   >   
   >       // 初始化函数指针，使其指向 add 函数, 通过函数指针调用函数
   >       operationPtr = add;	// 函数名就是函数的地址
   >       int result = operationPtr(10, 5);
   >       cout << "Result: " << result << endl;
   >   
   >       // 将函数指针切换到 subtract 函数
   >       operationPtr = subtract;
   >       result = operationPtr(10, 5);
   >       cout << "Result: " << result << endl;
   >       return 0;
   >   }
   >   ```
   >
   > - **指针函数：返回值是指针类型的函数**，用于返回指向某种类型数据的指针
   >
   >   ```c
   >   int* getArray() {
   >       static int arr[5] = {1,2,3,4,5};
   >       return arr;
   >   }
   >       
   >   int* p = getArray(); // p 是指针，指向 arr
   >   ```

10. 函数指针的概念及使用场景

    > - **回调函数**
    > - 函数指针数组实现状态机
    > - 动态加载库
    > - **实现多态**
    > - 作为参数，实现可插拔的函数行为
    > - 首先函数映射表（syscall注册表）

11. 关键字（`const`、`static`、`define`、`typedef`、`inline`、`new`、`malloc`、`constexpr`、`volatile`、`extern`、`++`、`explicit`）

    > - > define宏替换、文本替换，在编译预处理阶段做简单的字符替换，没有类型检查机制，不分配内存，给出的是立即数
    >   >
    >   > ```c
    >   > #define PI 3.1415
    >   > #define SQUARE(x) ((x)*(x))
    >   > ```
    >
    > - > typedef给类型起别名（结构体、函数指针），有对应的数据类型，发生在编译运行阶段，进行类型判断，在静态存储区分配空间，程序运行时，只有一个拷贝
    >   >
    >   > ```c
    >   > typedef unsigned int uint;
    >   > typedef int(*FuncPtr)(int, int);
    >   > ```
    >
    > - > inline建议编译器将函数展开到调用的位置，减少函数调用时压栈、跳转、返回的开销，做类型检查，但编译器有可能会拒绝内联请求
    >   >
    >   > ```c
    >   > inline int add(int a, int b) { return a + b; }
    >   > ```
    >   >
    >   > 内联限制：不能存在任何形式循环、不能存在过多的条件判断、内联函数声明必须在调用前
    >
    > - > const用于定义常量，生效于编译阶段，分配内存，带类型，而define定义宏，是立即数，不带类型
    >
    > - > - new
    >   >   - 内存分配失败，抛出异常bad_alloc，不会返回NULL
    >   >   - 无需指定内存块大小
    >   >   - 是运算符，允许重载
    >   >   - 会调用构造函数完成对象的构造
    >   >   - 从自由存储区上为对象分配内存空间
    >   > - malloc
    >   >   - 内存分配失败，返回NULL
    >   >   - 需要显式指定所需内存的尺寸
    >   >   - 是库函数，不允许重载
    >   >   - 不会调用构造函数
    >   >   - 从堆区动态分配内存
    >
    > - > **constexpr声明编译期常量或编译期可求值的函数，且只读（隐含了const）**，可以修饰变量、函数、构造函数
    >   >
    >   > ```c++
    >   > constexpr int square(int x) { return x * x; } 
    >   > constexpr int val = square(5); // 编译期求值
    >   > 
    >   > int arr[val]; // 编译期确定大小，合法
    >   > ```
    >
    > - > volatile声明的变量，表示其随时可能发生变化，与该变量有关的运算，不要进行编译优化，每次都要从内存中重新装载内容，而不是从寄存器拷贝内容
    >
    > - > extern声明外部变量（类似于include头文件）
    >
    > - > static实现对多个对象之间的数据共享和隐藏，修饰的基本数据类型会做初始化
    >
    > - > ```c
    >   > // 根据参数类型区分前后置++
    >   > self &operator++()
    >   > { 	// ++i
    >   >     node = (linktype)(node->next);
    >   >     return *this;	// 返回自身引用
    >   > }
    >   > self operator++(int)
    >   > {	// i++
    >   >     self tmp = *this;	// 临时变量
    >   >     ++*this;
    >   >     return tmp;		// 返回自身副本
    >   > }
    >   > ```
    >
    > - > explicit用于防止构造函数或转换运算符的隐式类型转换
    >   >
    >   > ```c++
    >   > class Foo {
    >   > public:
    >   >     explicit Foo(int x) { }  // 显式构造函数
    >   > };
    >   > Foo f1(10);   // ✅ 正确，显式调用
    >   > Foo f2 = 10;  // ❌ 错误，隐式转换被禁止
    >   > 
    >   > class Bar {
    >   > public:
    >   >     Bar(int x) { }
    >   > };
    >   > Bar b = 10;   // ✅ 隐式转换允许
    >   > 
    >   > struct A {
    >   >     explicit A(int x, int y) {}
    >   > };
    >   > A a1{1, 2};  // ✅ 正确
    >   > A a2 = {1, 2}; // ❌ 错误，隐式转换被禁止
    >   > ```

12. 数据类型（基本数据类型（字符型、整形、浮点型、布尔型、修饰符、空类型）、派生数据类型（指针、引用、数组、）、用户自定义数据类型（结构体、类、枚举、联合体、类型别名））

    > - 基本数据类型
    >   - 字符型（char一个字节）
    >   - 整形（short至少16位、int至少和short一样长、long至少32位且至少和int一样长、long long至少64位且至少和long一样长）
    >   - 浮点型
    >   - 布尔型
    >   - 带修饰符的（unsigned不存储负数值的整形，增大变量的最大值，数据长度不变）
    >   - 空类型
    > - 派生数据类型
    > - 用户自定义数据类型

13. 指针和引用的区别

    > **指针（pointer）**
    >
    > - 变量，存放的是某个对象的内存地址。
    > - 可以改变指向，允许 `nullptr`。
    > - 使用 `*` 解引用，`->` 访问成员。
    >
    > **引用（reference）**
    >
    > - 已有对象的别名，绑定后与目标共享内存，无独立存储空间，本质上就是“自动解引用的指针”。
    > - 定义时必须绑定对象，不能更改绑定，不能为 `null`。
    > - 使用上和普通变量几乎一样。

14. 静态局部变量、全局变量、局部变量的特点，使用场景

15. `STL`容器有哪些

    - 序列式容器[**`动态数组vector`、`双向链表list`、`双端队列deque`、**`单向链表forward_list`、`定长数组array`]
    - 关联式容器（有序，红黑树）[**`有序唯一键集合set`、`有序唯一键值对集合map`、**`有序非唯一键集合multiset`、`有序非唯一键值对集合multimap`]
    - 无序式**哈希**容器[**`unordered_set`、**`unordered_multiset`、**`unordered_map`、**`unordered_multimap`]

16. `STL`容器各自的特点、使用场景

    > - 顺序容器
    >
    > | 容器           | 底层数据结构 | 特点                                                         |
    > | -------------- | ------------ | ------------------------------------------------------------ |
    > | **`vector`**   | 动态数组     | 动态数组，**随机访问**快，尾部插入快，**动态扩容**，插入慢，**堆上分配** |
    > | **`list`**     | 链表         | 双向链表，任意位置**频繁插入/删除**快，不支持随机访问        |
    > | **`deque`**    | 动态数组     | 双端队列，头尾都能快速插入/删除，**滑动窗口**                |
    > | `array`        |              | 固定大小数组（C++11）、**栈**上分配，性能比vector高一点      |
    > | `forward_list` | 链表         | 单向链表（C++11）                                            |
    >
    > - 关联容器（有序，基于**红黑树**），**插入、查找都是`O(logN)`**
    >
    > | 容器       | 特点                                 |
    > | ---------- | ------------------------------------ |
    > | **`set`**  | 唯一元素自动排序、**去重**           |
    > | `multiset` | 元素允许重复                         |
    > | **`map`**  | 键值对，键唯一，**查找、计数、字典** |
    > | `multimap` | 键值对，键可重复                     |
    >
    > - 无序容器（基于**哈希表**，C++11）
    >
    > | 容器                 | 特点                               |
    > | -------------------- | ---------------------------------- |
    > | **`unordered_set`**  | 无序唯一集合，查找、插入都是`O(1)` |
    > | **`unordered_map`**  | 无序键值对                         |
    > | `unordered_multiset` | 无序可重复集合                     |
    > | `unordered_multimap` | 无序可重复键值对                   |

17. 其他常用结构（**容器适配器[`stack`、`queue`、`priority_queue`]**、标准库的通用数据打包类型[`pair`、`tuple`]）

18. `STL`包含哪些内容（**容器、迭代器、算法、仿函数**、适配器（容器、函数）、分配器）

    > - 容器：存数据，各种数据结构的模板类
    > - 算法：操作数据，基于迭代器提供大量常用算法（查找、修改、排序）
    > - 迭代器：搭桥，容器和算法之间
    > - 仿函数：辅助，函数对象，重载operator()的类或结构体，代替普通函数作为算法参数
    > - 适配器：改接口，对已有的容器、迭代器、仿函数进行封装，改变接口
    > - 空间配置器：管理内存，负责内存空间的配置和管理

19. `vector`是如何保证元素连续存储的，如何实现扩容的

    > - vector使用**动态数组**作为底层数据结构，内部使用**单一的内存块**存储所有元素，并管理这个内存块的大小和容量；当容量不足，会**重新分配**一块更大的内存，把旧的数组中的数据**复制**到新的内存块中
    > - 实现了一个内存分配函数，内存不够时，申请一块**原内存1.5-2倍**大小的新块，做拷贝
    > - 可使用**移动语义**和**直接拷贝**复制对象

20. `vector`的`push_back`和`emplace_back`有什么区别

    > - 都是在vector的末尾添加元素，但
    >
    >   - push_back创建一个元素的**副本**、或**移动**该元素，添加到vector末尾
    >
    >     > 如果参数是一个**右值**，使用移动构造函数移动，如果是**左值**，使用复制构造函数
    >     >
    >     > 在需要**扩容**时，导致所有元素被复制或移动到新的内存位置
    >
    >   - emplace_back在vector末尾**就地构造**元素，避免额外的复制或移动
    >
    >     > 不接受任何直接参数，但**接受构造新元素所需的构造函数参数**
    >     >
    >     > 避免了创建和销毁临时对象的开销，更**高效**
    >     >
    >     > 不会触发vector内存的重新分配
    >     >
    >     > 完美转发参数

21. `list`和`vector`的区别

    > - **底层数据结构**：vector是连续存储的动态数组，list是不连续的内存块组成的双向链表
    > - **随机访问**：vector支持O(1)的随机访问，list不支持，访问需要遍历O(n)
    > - 插入和删除：vector在尾部插入或删除O(1)，中间或头部因为需要移动元素是O(n)，list在任意位置插入或删除是O(1)，但找到**查找位置可能需要O(n)**
    > - 迭代器：vector有**随机迭代器**，支持所有迭代器操作，list是**双向迭代器**，只支持前移或后移
    > - 速度：vector可以根据连续内存的**局部性原理**，结合**缓存**优势，提升访问速度，list节点分布在堆内存各处，每次访问几乎都会导致缓存未命中，速度慢
    > - 适用场景：vector适合支持随机访问、或主要在尾部添加删除元素的，list适合在容器中间频繁插入删除的，不需要随机访问的场景

22. `list`实现元素插入和删除的原理

    > list底层的双向链表，使用指针链接元素的pre和next指针，支持双向遍历，插入和删除就是修改指针

23. `set`和`map`的底层实现有什么区别

    > - set和map都是基于平衡二叉树（通常是红黑树）实现
    > - set**仅存储键值**，即元素本身
    > - 而map存储的是**键值对**，键值是键，值是与键关联的数据

24. `map`、`deque`、`list`的实现原理

    > - map：基于红黑树，保证元素按键的顺序排序，每个节点存储了键值对，通过对键的比较结果进行自平衡，**迭代器在遍历的时候是有序的**
    > - deque：双端队列，基于动态的节点数组，**每个节点存储一部分元素**，并可以动态增加或删除节点，在队列前后端操作元素快速，中间部分可能需要O(N)
    > - list：基于双向链表，使用两个指针，具备了快速插入和删除的能力，无序容器，在迭代器支持下可以实现O(1)的插入和删除

25. 关联式容器`map`、`multimap`、`set`、`multiset`之间有什么区别

    > - 存储的数据：键值对（`std::pair<const Key, T>`），元素
    > - 键的唯一性：是否允许有重复的键或元素
    > - 排序：根据键值或元素值排序，当相同时，**在内部以插入顺序排序**
    > - 访问：是否支持以下标操作符访问，还是只能使用迭代器

26. 在`map`和`set`中查找元素有哪些方法

    > - find，找到特定键的值，返回该元素的迭代器，或者end()迭代器
    > - count，要么是0要么是1
    > - lower_bound，返回不小于给定键的首个元素的迭代器
    > - upper_bound
    > - equal_range

27. `map`和`unordered_map`的区别

    > - 底层数据结构：基于红黑树，排列有序，基于哈希表，元素排列无序

28. 迭代器的作用

    > 提供了一种**容器无关**的统一的方法，来**遍历不同容器的元素**，可以视为容器某个特定位置的指针

29. 迭代器失效的概念、原因、避免措施

    > 迭代器不再指向容器中有效元素的情况（对容器进行了修改，**容器元素被删除erase、容器扩容realloc、容器整体修改swap、clear、assign**等）
    >
    > - 增删时小心扩容，先做空间预分配reserve
    > - erase返回值再用`it = v.erase(it);//返回下一个有效迭代器`

30. `vector`的实现原理

31. `list`的实现原理

32. 介绍智能指针（`shared_ptr`、`unique_ptr`、`weak_ptr`、`auto_ptr`（被移除））

33. 堆和栈的区别

34. 进程地址空间（内存分区）（内核空间、环境变量、命令行参数、栈、共享区（内存映射区）、堆、BSS未初始化全局/静态变量、Data初始化全局/静态变量、常量区（只读数据段）、代码段）

35. 内存泄露的概念，产生原因，分类、避免措施

36. `new`和`malloc`的区别，`delete`和`free`的区别

37. 悬空指针的概念，产生原因，避免措施

38. 野指针和悬浮指针的区别

39. 内存对齐的概念、为什么要考虑内存对齐

    > - 概念：对象在内存中的地址需要为某个字节边界的整数倍，方便CPU访存，对齐地址时最快，未对齐可能产生硬件异常或额外指令
    >
    > - alignof(T)得到类型对齐
    >
    > - **结构体按成员顺序放置，每个成员前可能加padding满足对齐条件（每个成员按照自身类型对齐规则放置，整个结构体按照最大成员对齐，即编译器可能在成员之间或结构体末尾插入空字节，sizeof(struct)向上对齐到该值）**
    >
    > - 可以指定对齐方式：`#pragma pack(n)`控制编译器对齐粒度，`alignas()`显式指定对齐方式
    >
    >   ```c
    >   #pragma pack(1)  // 按1字节对齐
    >   struct B {
    >       char c;
    >       int i;
    >   };
    >   // 大小 = 5（无填充）
    >   #pragma pack()
    >   
    >   struct alignas(16) C {
    >       int x;
    >   };
    >   // 强制对齐到16字节
    >   ```
    >
    > - malloc和new对齐：默认返回的地址通常对齐到最大基本类型的对齐要求

40. 实现内存拷贝函数`char* strcpy(char* strDest, char* strSrc)`

41. 为什么返回的是`strDest`的原始值，而不是`strSrc`

42. 如何实现自重叠（`src <= dst <= src + strlen(src`）的拷贝

43. 实现`String`的构造函数（`char *`）、拷贝构造函数（深拷贝）、赋值构造函数（赋值运算符重载）、析构函数

44. 说一说`this`指针

45. 介绍`C`和`C++`的内存分配方式

46. 计算机中的乱序执行

47. 实现字符串操作函数（`strcpy`、`strlen`、`strcat`、`strcmp`）

48. `C++`面向对象特性（封装、继承、多态）

49. 编译时多态（静态多态：运算符重载、函数重载）和运行时多态（动态多态：虚函数、继承），前者是否能替代后者

50. 如何实现多态（虚函数、虚函数表、重写基类方法、基类类型指向派生类对象、调用虚函数）

51. 虚函数调用的底层机制，虚函数表`vtable`和虚指针`vptr`的概念，动态绑定过程（静态绑定）

52. 如何不使用虚函数实现运行时多态

53. 重载与重写区别以及实现方式，重载能否靠返回值来区分，`override`的作用，不加`virtual`有什么问题

54. 介绍访问修饰符（`public`、`private`、`protected`）的区别

55. 多重继承的概念，存在的问题（菱形继承）

56. 构造函数（默认构造函数、含参构造函数、拷贝构造函数、移动构造函数、委托构造函数、转换构造函数、初始化列表构造函数）

57. 虚函数与纯虚函数的区别

58. 抽象类和纯虚函数的关系

59. 虚析构函数的作用

60. 为什么有虚析构函数，而构造函数不能设置`virtual`

61. 哪些函数不能声明为虚函数

62. 什么是友元函数

63. 什么是内联函数

64. 深拷贝和浅拷贝的区别

65. 什么是运算符重载

66. 左值引用和右值引用的概念、区别

67. `C++`中的`socket`套接字通信过程（服务器端（创建、绑定、监听、接受、收发、关闭）、客户端（创建、设置、连接、收发、关闭））

68. 什么是`socket`（操作系统提供的网络通信端点，一端关联应用程序，一端关联`TCP/IP`协议栈）

69. `socket`相关函数（`socket()`、`bind()`、`listen()`、`accept()`、`connect()`、`send()`、`recv()`、`read()`、`write()`、`close()`）

70. `socket`相关结构体（`sockaddr`、`sockaddr_in`、`in_addr`、`sockaddr_in6`、`in6_addr`、`hostent`）

71. 字节序转换函数（`htons`、`htonl`、`ntohs`、`ntohl`），为什么要转换

72. 为什么服务器端需要两个`socket`

73. `TCP`连接关闭的`shutdown`和`close`有什么区别

74. 如何优化`socket`流程（设置非阻塞`I/O`、多线程处理、设置超时时间、地址复用）

75. `Windows`下使用的`socket`接口`API`是什么

76. `socket`编程模式（阻塞模式与非阻塞模式`IO`的区别），切换函数（`fcntl`、`ioctl`）

77. 如何实现在服务器端同时处理多个客户端的连接请求（多进程多线程模型，`IO`多路复用机制（`select`、`poll`、`epoll`）、异步`IO`模型）

78. `epoll`的触发模式（水平`LT`、边缘`ET`）有什么区别，边缘模式为什么要求非阻塞，`epoll`的底层机制是什么

79. 为什么`select`的`FD`有`1024`限制

80. 如何处理多线程服务器中的竞态条件

81. 网络编程中的粘包和拆包问题处理方案（定长数据、分割标识符、头部标识长度、自定义协议）

82. `std::thread`线程构造函数

83. `RAII`的概念（`resource acquisition is initialization`）

84. `lock_guard`的概念、作用、特点

85. `unique_lock`的概念、作用、特点，与`lock_guard`的区别

86. 如何创建和管理线程，`join`和`detach`有什么区别

87. `std::async`、`std::future`和`std::promise`的作用

88. `std::mutex`的用法

89. 介绍`std::atomic`

    > 提供了一组原子类型和操作的模板库，保证在多线程环境下不会产生数据竞争
    >
    > `a++`和`int a = b`不是线程安全的，因为编译器生成的汇编指令不是原子的
    >
    > ```c++
    > #include <atomic>
    > 
    > std::atomic<int> a{0};
    > std::atomic<bool> flag{false};
    > a.store(10);        // 设置值
    > int v = a.load();   // 读取值
    > a++;                // 原子自增
    > a.fetch_add(5);     // 原子加法，返回原值
    > a.compare_exchange_strong(expected, desired); // CAS操作:原子比较并交换
    > ```

90. 介绍`C++`的多线程同步机制（互斥锁、条件变量、原子操作、信号量）

91. 死锁、读写锁、无锁编程的概念

92. 如何实现一个线程池（一个任务队列+一组工作线程+从任务队列取出任务的机制+同步机制）

93. `C++11`的新特性有哪些（语法的改进、标准库扩充）

94. 介绍智能指针（`shared_ptr`、`unique_ptr`、`weak_ptr`），以及构造智能指针的方法

95. 什么是循环引用

96. 共享指针的实现机制（引用参数、重载机制），线程安全性

97. 自动类型推导`auto`和`decltype`的用法

98. 解释概念：左值、右值、左值引用、右值引用（将亡值、纯右值）、移动语义（`std::move`）、完美转发（`std::forward`）

99. 基于范围的`for`循环的用法（遍历）

100. 列表初始化(赋值、花括号赋值、花括号、圆括号)

101. `lambda`表达式的概念、语法、特点、用途（`STL`算法库）

102. 介绍`lambda`的捕获列表有哪些捕获类型

103. 介绍`C++`中的异常处理机制（`assert`、状态码、`return`、抛出异常（`try`、`catch`、`throw`））

104. 介绍`C++`中抛出异常和匹配异常的原则

105. 构造函数和析构函数中的异常问题

106. 列出常见的异常类型（标准库）

107. 什么是异常安全的代码，如何编写异常安全的代码

108. 命名空间的作用

109. 内联函数的工作原理，与`lambda`表达式的区别

110. 如何在`C++`中实现一个不可变的字符串类

111. 二级指针

     > 1. 修改函数外部的指针
     > 2. 动态创建二维数组
     > 3. 函数参数传递指针数组
     > 4. 修改链表的头阶段
     > 5. 函数返回多个指针（多返回值）
     > 6. 指向函数指针数组

112. malloc的底层原理

     > malloc是一个库函数，自身不直接分配物理内存，而是通过维护空闲链表等数据结构来管理内核分配给进程的堆空间。进程启动时，堆很小，当malloc请求时，首先会在已有的空闲块中查找适合的内存，如果找不到，就想内核申请更多的内存：
     >
     > - 对于中小块内存，glibc通过sbrk等系统调用拓展传统堆，这时候只是在虚拟地址空间中增加一段可用范围，但不会立即分配物理内存页，只是修改了进程的虚拟内存映射表，当触发缺页异常时，内核捕获异常再实际分配物理页并做映射（延迟分配）
     > - 对于较大内存请求，通常大于128K，直接使用mmap在虚拟地址空间中单独映射一块区域，free释放内存时，一般只是把块放回空闲链表，不一定立即归还给内核

113. 哈希表的一些原理

     > - 哈希冲突：使用哈希函数计算时，不同的键映射到了相同的桶下标
     > - 解决方法：
     >   - 链地址法：每个桶存放一个链表，冲突的元素都插到这个链表中，查找时先定位桶，再查找链表
     >   - 开放定址法：继续在哈希数组里找下一个空位置放进去，线性探测、二次探测、双重哈希
     >   - 再哈希：当哈希表的装载因子过大，冲突变多，通过增加桶数量，进行扩容，重新计算每个元素的哈希值，分散冲突

114. 