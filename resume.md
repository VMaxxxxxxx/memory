## 1.实习经历

- 参与 Linux 数控系统开发环境的维护，针对历史文档整理内核、外部库等源码差异，构建补丁集，重建 A9 平 台交叉编译开发环境，负责后续升级包的制作；

  > 开发环境：uboot、kernel、rootfs、QT，主要指的是kernel和QT的源码编译，新的业务要基于已编译的内核进行
  >
  > 历史文档和补丁集：上面的源码都基于开发板厂商提供的，经过很多次迭代，但是没有任何的版本管理，比如内核实时补丁、总线配置、QT库的mqtt（轻量级发布订阅消息协议）、tftp（文件传输协议）等，加一些编译选项、配置选项、改一些屏幕、按键、背光参数，虽然是一些不知所以然的工作，但是在反复中对了解了嵌入式Linux设备的镜像编译、烧录、启动、测试流程。在此过程改动了很多的源码文件，都采用补丁的形式导出了`diff -urN res_path new_path > patch1.diff`，后续`patch -p1 < patch1.diff`，还有就是QT从4.3到5.6.3的库部署，tslib库部署
  >
  > 交叉编译：内核的路径影响着QT编译的路径，每次编译完内核都需要重新编译QT
  >
  > 升级包：uboot、内核镜像、文件系统、启动图片，主要是内核和文件系统

- 负责 A9 平台系统重建后的集成测试，覆盖屏幕参数、UI 界面、IP 通信、SSH 文件传输等基础功能；

  > 屏幕参数：计算`margin、refresh、pixclock`，改结构体成员值，去烧录测试显示区域，校准触摸屏
  >
  > UI界面：对QT的控件、UI做功能测试、输入测试
  >
  > IP通信：ping，改ip，子网掩码
  >
  > SSH：主要面向内部人员的现场调试，scp功能

- 承担数控系统稳定性排查和修复工作，分析系统启动日志，排查宕机原因，制定并落地了包含硬件模块置换、 通信接口验证、驱动部署、自动化测试的系统性解决方案；

  > 通过dmesg系统启动日志文件分析，以及shell的echo，定位原因：**Linux设备上电启动，触摸屏驱动在内核probe()阶段（驱动的设备初始化函数，内核发现硬件后会调用这个函数）阻塞，导致udev等待创建设备节点时卡住，系统无法完成启动**
  >
  > 硬件模块置换：和乙方技术沟通、验证触摸屏控制板存在的问题，尝试新接口的控制板方案，先做双端的（imx6q的串口收发能力、控制板的串口收发能力）通信验证（飞线、DB9接口、使用串口助手），改内核中对应引脚的复用功能使能串口，确认硬件层面的可用性
  >
  > 驱动部署：将软件移植到文件系统，启动时有接口选型，还有QT设备节点选型，通过shell脚本去自动检测当前设备节点、自动校验（按压时有十六进制数据输出）
  >
  > 自动化测试：实验验证成功，交付前要做测试，触摸屏稳定性的测试无非是设备节点的选择、通信验证、校准判断等，还有驱动程序的启动时机

- 对接下游需求，验证并部署 Samba 服务，开发面向日志文件的轻量级服务器，负责提供技术支持。

  > 需求：为用户提供一种最直白最友好的文件访问方式（Windows文件资源管理器、浏览器），尽可能得只需要点点点，不需要借助其他外部软件，即连即用
  >
  > Samba：Samba版本和Linux版本不适配，尝试4.15左右的源码，编译和部署时都无效，找到合适的版本3.4，Samba配置：修改configure选项，修改smb.conf，添加外部接口myshare及路径，自动化testparm、添加并检查用户smbpasswd、pdbedit（专门创建一个用户、组），测试smb状态smbd，检查日志Samba/var，在客户端提供smb1.0配置文档
  >
  > 日志文件服务器：基于阻塞式IO的单线程模型，改进成基于线程池的多线程模型。

## 2.项目经历

## 3.专业技能

