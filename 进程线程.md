- 进程和程序的概念
- 并行和并发
- PCB
- 进程状态
- 进程创建（fork、execl、execlp）
- 进程控制（结束进程exit、孤儿进程（父进程先退出、由init进程领养）、僵尸进程（父进程对于先退出的子进程不管不问，一直运行不退出也不做资源回收：kill -9）、进程回收（阻塞wait、非阻塞waitpid））
- 管道的实质：内核维护的一个环形队列缓冲区
- 使用文件IO操作管道，read、write
- 父子进程间的管道方式通信：子进程拷贝父进程打开的文件描述符
- 匿名管道（pipe、有血缘的父子兄弟爷孙等进程间）和命名管道（mkfifo，进程间有无血缘均适用，通过open操作得到读写管道的fd，一方打开会阻塞）
- 管道重定向到标准输入输出（STDOUT_FILENO）：dup2（）
- 使用fcntl设置管道的阻塞
- 内存映射区的概念
- mmap、munmap：实现进程间通信、实现文件拷贝（不同进程通过指针拷贝memcpy，从一端的文件拷贝到另一个进程打开的文件中去）