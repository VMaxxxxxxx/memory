1. 指针

   - 指向普通对象
   - **常量指针**
   - 指针常量
   - 常量指针常量
   - 函数指针
   - 指向对象成员的指针
   - **this指针：指向类的当前对象的指针常量**

2. 悬空指针

   > 指针指向的内存被free释放

3. 野指针

   > 指针未做初始化

4. 指针和引用的区别

   > - 定义方式不同
   > - 语法不同
   > - 是否允许为空
   > - 可变性
   > - 占用内存地址

5. 引用的实质

   > 引用的本质是变量的别名，在底层实现上，引用通常通过指针来实现，但它本身并不是一个指针。**编译器会在引用被使用时自动将其替换为所绑定变量的地址。**引用不单独分配内存，不占用额外空间，只是在符号表中为变量建立一个别名。引用必须初始化，且绑定后不可更改。

6. 常量指针和指针常量的区别

   > - 定义格式
   > - 含义
   > - 用途（常量指针：函数参数传递；指针常量：绑定硬件地址）

7. 函数指针和指针函数

   > - 定义格式
   > - 本质

8. 参数传递

   > - 值传递（Pass by Value）：
   >   函数调用时将实参的**值复制**一份传递给形参，函数内部对形参的修改不会影响到实参。
   >   本质：形参和实参是两个独立的变量，修改形参不会影响实参，数据安全但效率略低。
   > - 指针传递（Pass by Pointer）：
   >   函数调用时将实参的**地址传递**给形参，函数内部通过指针可以直接操作实参的值。
   >   本质：通过指针访问和修改内存中的原始数据，形参和实参指向同一块内存，修改形参会影响实参。
   > - 引用传递（Pass by Reference）：
   >   函数调用时将实参的变量直接作为形参的**别名**，函数内部对形参的操作就是对实参的操作。
   >   本质：形参是实参的别名，不会产生新的变量，操作形参等同于操作实参，效率高且语法简洁。

9. 全局变量、局部变量、静态全局变量、静态局部变量的区别

   > - 作用域
   >
   >   > 1. 全局变量（Global Variable）：
   >   >    定义在所有函数外部，整个程序范围内可访问，生命周期贯穿程序始终，所有文件都可通过声明extern使用。
   >   > 2. 局部变量（Local Variable）：
   >   >    定义在函数或代码块内部，只在其作用域内有效，函数执行完毕后被销毁，生命周期短，仅限于函数或代码块。
   >   > 3. 静态全局变量（Static Global Variable）：
   >   >    定义在函数外部，加static修饰，只在当前源文件内可访问，其他文件无法通过extern访问，生命周期贯穿程序始终，限制了访问范围。
   >   > 4. 静态局部变量（Static Local Variable）：
   >   >    定义在函数内部，加static修饰，只在当前函数内有效，但生命周期贯穿程序整个运行期，值会被保留，函数多次调用时变量值不会丢失。
   >
   > - 分配空间位置

10. C++作用域

    > - 全局
    > - 局部
    > - 语句
    > - 类
    > - 命名空间
    > - 文件

11. 全局变量定义在头文件中的问题

    > 重复定义

12. 头文件和extern的区别

    > 1. 头文件（Header File）：
    >    头文件通常用于声明函数、变量、结构体等，提供**接口声明**，方便多个源文件共享同一份声明。头文件通过#include指令包含在源文件中，编译时会将头文件内容拷贝到指定位置。
    > 2. extern关键字：
    >    extern用于声明变量或函数的“**外部链接属性**”，告诉编译器该变量或函数在其他文件中定义，只在当前文件做声明，不分配内存。常用于在多个源文件中引用同一个全局变量。
    >
    > - 总结：
    >   头文件用于接口声明和代码复用；extern用于声明外部变量或函数，防止重复定义和链接错误。头文件可以声明多个内容，而extern通常只用于单个变量或函数的外部声明。

13. extern C

    > extern "C" 的作用是用于C++代码中，告诉编译器按照C语言的方式进行函数名修饰（即不进行C++的名字重整），从而保证C++编译器生成的符号名与C语言兼容。这样可以实现C和C++之间的**混合编程**，使C++代码能够调用C语言编写的函数库，或被C语言程序调用。

14. sizeof(1==1)在C和C++的区别

    > - sizeof(int)
    > - sizeof(bool)

15. C和C++的struct区别

    > 1. 成员默认访问权限不同
    >    C语言中，struct的所有成员默认都是public；
    >    C++中，struct成员默认也是public，但C++允许struct中定义成员函数和访问控制（public、private、protected），而class默认是private。
    > 2. 可以包含成员函数
    >    C语言的struct只能包含数据成员，不能有成员函数；
    >    C++的struct不仅可以包含数据成员，还可以包含成员函数、构造函数、析构函数等。
    > 3. 支持继承和多态
    >    C语言不支持结构体继承和多态；
    >    C++中的struct可以与class一样支持继承和多态。
    > 4. 类型定义方式不同
    >    在C语言中，需要使用typedef为struct定义新类型名；
    >    C++中struct定义的类型名就是类型，**无需typedef**。

16. C++中struct和class的区别

    > 1. 默认成员访问权限不同
    >    **struct的成员默认是public（公开），而class的成员默认是private（私有）。**
    > 2. 语法和功能上基本一致
    >    在C++中，struct和class都可以有成员变量、成员函数、构造函数、析构函数、继承和多态等功能，二者在语法和功能上几乎没有差别，主要区别是**默认访问权限和默认继承方式**。
    > 3. 用途习惯不同
    >    struct通常用于表示纯数据结构，不含复杂行为；class习惯上用于表示具有行为和封装的数据类型。

17. struct和union的区别

    > 1. 内存分配方式不同
    >    struct（结构体）：每个成员都有自己的内存空间，总大小是所有成员大小之和（加上对齐）。
    >    union（联合体）：所有成员共享同一块内存空间，总大小等于最大成员的大小。
    > 2. 数据访问方式不同
    >    struct：可以同时访问所有成员，各成员互不影响。
    >    union：同一时刻只能存储一个有效成员的值，修改一个成员会影响覆盖其他成员的值。
    > 3. 使用场景不同
    >    struct：用于保存多个不同类型的数据，各成员独立存在。
    >    union：用于节省空间，多个成员互斥使用，常见于实现多种数据类型的转换等场景。

18. C和C++中的static的区别

    > 1. 在C语言中的static：
    >
    >    - 用于修饰局部变量时，使变量具有静态存储期，生命周期贯穿程序始终，但作用域仅限于所在函数。
    >
    >    - 用于修饰全局变量时，限制变量只能在当前文件访问，其他文件不可通过extern访问（内部链接）。
    >
    >    - 用于修饰函数时，函数只能在当前文件内调用，不能被其他文件访问。
    >
    > 2. 在C++中的static：
    >
    >    - 具有C语言的所有功能，同时可以用于类成员。
    >
    >    - 用于类成员变量/函数时，表示该成员属于整个类，而不是某个对象。静态成员变量所有对象共享，静态成员函数只能访问静态成员。不能将静态成员函数定义为虚函数、const函数、volatile函数
    >    - 静态成员函数不能调用非静态成员变量或函数，因为静态成员函数没有this指针

19. C++静态变量的使用场景

    > - 全局静态变量
    > - 局部静态变量
    > - 类静态成员变量

20. const作用和用法

    > - 限定变量、指针、函数参数为只读，防止被修改，提高代码安全性和可读性
    > - 修饰变量
    > - 修饰指针
    > - 修饰函数参数
    > - **修饰成员变量**：类内声明、定义，在构造函数初始化列表中初始化
    > - **修饰成员函数**：不会修改类成员变量，不能调用非常量成员函数

21. define和const的区别

    > 1. 类型检查：
    >    - `#define` 是预处理指令，用于宏替换，没有类型检查，简单文本替换。
    >    - `const` 是关键字，用于定义常量，有类型检查，属于变量的一种。
    > 2. 作用范围：
    >    - `#define` 在整个文件范围内有效，预处理时直接替换所有出现的地方。
    >    - `const` 受作用域限制，可以用于局部或全局，遵循变量作用域规则。
    > 3. 调试与内存：
    >    - `#define` 宏没有内存分配，不能用调试工具查看。
    >    - `const` 常量有存储空间，可以调试和取地址。
    > 4. 语法与用法：
    >    - `#define PI 3.1415`
    >    - `const float PI = 3.1415;`

22. define和typedef的区别

    > 1. 作用不同
    >
    >    - `#define` 是预处理指令，用于宏定义，做文本替换，可以定义常量或宏函数。
    >    - `typedef` 是关键字，用于类型重命名，为已有的数据类型起别名。
    >
    > 2. 类型检查
    >
    >    - `#define` 只是简单的文本替换，没有类型检查。
    >    - `typedef` 有类型检查，属于编译阶段。
    >
    > 3. 用法示例
    >
    >    - `#define PI 3.1415`
    >    - `typedef int myint;`
    >
    > 4. 功能范围
    >
    >    - `#define` 可用于常量、函数宏等，较为灵活。
    >    - `typedef` 只能用于类型别名，不能定义常量或宏函数。
    >
    > 5. **处理指针时有差别**
    >
    >    > ```c
    >    > #define IntPTR1 int *
    >    > #typedef int * IntPTR2;
    >    > IntPTR1 p1, p2;		// p1: int*, p2: int
    >    > IntPTR2 p3, p4;		// p3: int*, p4: int*
    >    > const IntPTR1 p5;	// 常量指针
    >    > const IntPTR2 p6;	// const约束整个IntPTR2，常量指针
    >    > ```

23. volatile的作用、原子性、对编译器的影响

    > **1. volatile的作用：**
    > volatile用于修饰变量，告诉编译器该变量可能在程序外部被异步修改（如被硬件或其他线程改变），禁止编译器对该变量做优化（如寄存器缓存），每次访问都必须从内存重新读取。
    >
    > **2. volatile与原子性：**
    > volatile**不保证原子性**。它只保证对变量的读写不会被优化或缓存，但不能防止多线程下操作的竞争等问题。例如，`volatile int a = a + 1;` 不是原子操作。
    >
    > **3. 对编译器的影响：**
    >
    > - 告诉编译器禁止对volatile变量做优化，强制每次都从内存读取或写入。
    > - 编译器不会将volatile变量的值缓存在寄存器中。
    > - 可以防止编译器对代码顺序的重新排列（部分场景）。

24. volatile的使用场景，能否结合const

    > - 多个线程都会用到某一变量，且该变量的只随时有可能发生改变
    > - 中断服务程序访问的变量，或并行设备的硬件寄存器的的变量
    > - **可以同时使用，只读的硬件寄存器，如外部输入端口**

25. 为什么析构函数一般是虚函数

    > **为了保证通过基类指针删除派生类对象时，能够正确调用派生类的析构函数，完成对象的完全析构，避免资源泄漏。**
    >
    > - 如果基类的析构函数不是虚函数，**编译器实施静态绑定**，使用基类指针删除派生类对象时，只会调用基类的析构函数，导致派生类资源没有释放，可能造成内存泄漏或其他资源泄漏。
    > - 如果基类的析构函数是虚函数，删除对象时会根据实际对象类型，先调用派生类析构函数，再调用基类析构函数，确保对象正确销毁。
    >
    > ```c++
    > class Base {
    > public:
    >     virtual ~Base() { /* ... */ }
    > };
    > 
    > class Derived : public Base {
    > public:
    >     ~Derived() { /* ... */ }
    > };
    > 
    > Base* p = new Derived;
    > delete p; // 会先调用Derived::~Derived，再调用Base::~Base
    > ```

26. 为什么构造函数不是虚函数

    > **因为对象的构造过程需要确定类型，而虚函数机制依赖于对象已构造完成后的虚函数表（vtable），在对象构造过程中虚函数表还未建立或初始化完成，因此无法实现虚调用。**
    >
    > 1. **虚函数机制依赖于虚函数表（vtable）：**
    >    - 对象的虚函数表是在构造函数执行过程中初始化的。
    >    - 在对象未构造完成前，还没有虚函数表，无法根据实际类型进行虚调用。
    > 2. **对象构造的本质需求：**
    >    - 构造函数的目的是初始化对象本身，确定对象类型。
    >    - 只有在对象构造完成后，才能进行多态和虚函数调用。
    > 3. **语言设计限制：**
    >    - C++标准规定构造函数不能是虚函数。
    >    - 虚函数主要用于解决**运行时多态**，构造过程属于**静态类型确定阶段**。
    >
    > **总结：**
    > 构造函数不是虚函数，是因为在对象构造阶段，虚函数表还没有建立，无法实现虚调用；而且构造函数的职责是初始化对象，不需要多态行为。

27. 什么是内联函数

    > 用inline关键字声明的函数，指在编译时将函数调用处用函数体代码直接替换，从而消除普通函数调用的开销（如**压栈、跳转**），提高程序运行效率。
    >
    > 1. 编译器会尝试将inline函数的调用处替换为函数体代码，实现“直接嵌入”，但是否内联由编译器决定。
    > 2. 适用于函数体短小、频繁调用的函数。
    > 3. 能减少函数调用开销，但不适合复杂、递归或包含循环的大函数。

28. 内联函数的缺点

    > 1. **代码膨胀（增大程序体积）**
    >    多次调用内联函数时，编译器会在每个调用处插入函数代码，导致可执行文件体积增大，尤其是函数体较大或被频繁调用时。
    > 2. **编译时间变长**
    >    由于代码重复插入，编译器需要处理更多代码，编译时间增加。
    > 3. **调试困难**
    >    内联展开后，函数调用变成代码片段，调试时不易跟踪函数调用栈，影响调试体验。
    > 4. 复杂函数不能内联
    >    递归函数、包含循环或过于复杂的函数一般不会被编译器内联，inline只是建议，最终由编译器决定。
    > 5. 影响代码维护
    >    如果在头文件中定义内联函数，头文件改动会导致所有引用该头文件的源文件都需重新编译，降低维护效率。

29. define和inline的区别

    > 1. 作用不同
    >    - `#define` 是预处理指令，用于宏定义，在编译前进行简单的文本替换，可以用于常量、宏函数等。
    >    - `inline` 是C++关键字，用于声明内联函数，建议编译器在调用该函数时进行内联展开，提高效率。
    > 2. 类型和安全性
    >    - `#define` 没有类型检查，仅做文本替换，容易出错（如宏参数优先级问题）。
    >    - `inline` 有类型检查，属于函数，语法规范安全。
    > 3. 用法示例
    >    - `#define MAX(a, b) ((a) > (b) ? (a) : (b))`
    >    - `inline int max(int a, int b) { return a > b ? a : b; }`
    > 4. 调试与作用域
    >    - `#define` 宏没有作用域和调试信息，调试时不能跟踪宏展开。
    >    - `inline` 函数有作用域和调试信息，可以单步调试，易于维护。

30. include时`""`和`<>`的区别

    > - 查找文件的类型（系统头文件或标准库头文件、用户自定义头文件）
    > - 使用习惯

31. void*的概念

    > - 通用的指针类型：无类型指针，用于指向任何类型的指针
    > - 不能直接进行解引用操作，不能直接做指针运算，需要转化为具体指针类型
    > - 用于在不同类型直接进行通用操作，传递指针参数、动态分配内存
    >
    > ```c
    > void* voidPtr = malloc(sizeof(int));
    > int* intPtr = (int*)voidPtr;
    > ```

32. sizeof和strlen的区别

    > 1. **作用不同**
    >
    >    - `sizeof` 运算符，计算数据类型或变量占用的字节数（编译阶段确定）。
    >    - `strlen` 是库函数，用于计算字符串实际长度（即遇到'\0'前的字符数，运行时确定）。
    >
    > 2. **返回值区别**
    >
    >    - `sizeof` 返回类型或对象所占的内存大小（单位：字节），包含所有元素。
    >    - `strlen` 返回字符串内容长度，不包括末尾的'\0'。
    >
    > 3. 使用场景
    >
    >    - `sizeof` 可用于任何数据类型、数组、结构体等。
    >    - `strlen` 只能用于C风格字符串（char数组），且必须以'\0'结尾。
    >
    > 4. 示例
    >
    >    ```c
    >    char str[10] = "abc";
    >    sizeof(str);  // 返回 10
    >    strlen(str);  // 返回 3
    >    ```

33. explicit的概念

    > - 修饰**单参数**构造函数或转换运算符，**防止编译器进行隐式类型转换**
    >
    > ```c++
    > class MyClass {
    > public:
    >     explicit MyClass(int x);  // 只能显式调用
    > };
    > 
    > MyClass obj1 = 10;   // 错误，不能隐式转换
    > MyClass obj2(10);    // 正确，显式调用
    > ```

34. strcpy、memcpy、memmove

    > ### 1. strcpy
    >
    > - 作用：拷贝字符串（以`\0`结尾的字符数组），将源字符串内容复制到目标字符串，包括结尾的`\0`。
    > - 用法：`strcpy(dest, src);`
    > - 要求：目标区域必须足够大且不能重叠；必须用于字符串（`char*`），不能拷贝二进制数据。
    > - 风险：不会检查目标空间大小，容易造成缓冲区溢出。
    >
    > ### 2. memcpy
    >
    > - 作用：拷贝指定长度的内存块，可用于任何类型的数据（不仅仅是字符串）。
    > - 用法：`memcpy(dest, src, n);`
    > - 要求：源和目标内存区域**不能重叠**；拷贝长度由第三个参数决定。
    > - 应用：适合结构体、数组等任意二进制数据的复制。
    >
    > ### 3. memmove
    >
    > - 作用：同样拷贝指定长度的内存块。
    > - 区别于memcpy：**允许源和目标区域重叠**，能安全处理内容重叠的情况。
    > - 用法：`memmove(dest, src, n);`
    > - 原理：先判断源和目的内存区的新的位置，选择拷贝方向（从前往后、从后向前）。

35. memcpy的底层原理

    > - **直接操作内存块的二进制数据，从源地址逐个字节复制数据到目标地址，直到复制完指定的字节数，从不关心数据类型，也不处理字符串结束符**
    > - 按字节复制：
    >   最基本实现就是用循环将源地址的每个字节依次复制到目标地址。
    > - 按字（word）或块（block）复制（优化）：
    >   - 为了提高效率，现代实现会先按较大的单位（**对齐的**，如4字节、8字节、甚至SIMD指令宽度）**批量复制**。
    >   - 剩余不足一个块的字节再用字节复制完成。
    > - 可能的**硬件优化**：
    >   - 利用CPU指令集（如SSE、AVX）进行批量复制，提升性能。
    >   - 某些平台可能采用特定的汇编指令进行优化。
    > - 无重叠区域要求：
    >   `memcpy` 假定源和目标内存区域**不重叠**。如果重叠，应使用`memmove`。
    >
    > ```c++
    > void *memcpy(void *dest, const void *src, size_t n) {
    >     char *d = (char*)dest;
    >     const char *s = (const char*)src;
    >     while (n--) *d++ = *s++;
    >     return dest;
    > }
    > ```

36. strcpy的缺陷

    > 1. **不会检查目标缓冲区大小**
    >    - `strcpy` 只按照源字符串长度复制内容到目标数组，不会验证目标空间是否足够。
    >    - 如果目标空间太小，在连续分配的地址空间中，容易发生**缓冲区溢出**（Buffer Overflow），可能导致其他变量被覆盖、程序崩溃或安全漏洞。
    > 2. **安全风险高**
    >    - 缓冲区溢出可能被恶意利用，造成代码注入、数据损坏等安全问题。
    > 3. 无法处理部分复制需求
    >    - 只能完整复制整个字符串，不能指定只复制部分内容（可以用`strncpy`）。
    > 4. 不适用于二进制数据
    >    - 只能处理以`\0`结尾的字符串，不能用于包含`\0`的二进制数据复制。

37. C++的编译过程

    > - ![img](https://cdn.xiaolincoding.com//picgo/1754903574641-3e0727b1-5007-4cfb-9bf6-0b4a2ea77c3a.png)
    > - 预处理：对源代码进行处理：头文件包含、宏展开、处理条件编译指令（include、define、ifdef）、删除注释，生成一个纯C++源文件代码
    > - 编译：将源代码翻译成.s或.asm汇编代码，汇编做词法分析、语法分析、语义分析、优化，检查代码准确性
    > - 汇编：将汇编代码转换成机器可以执行的目标文件（机器指令），生成与机器平台相关的目标文件（.obj、.o）
    > - 链接：将目标文件和其他必要库文件链接在一起，生成可执行文件，链接器解析目标文件中的符号引用，在其他目标文件或库文件中的符号定义做匹配，地址重定位等

38. 静态链接库和动态链接库的区别

    > - **链接方式**：静态库在编译链接时被完整复制到可执行文件中，动态库只会在可执行文件中包含对库的引用
    > - **文件大小**：静态库使得可执行文件大小增加，动态库不会，只在允许时被动态加载到内存中
    > - 内存占用：静态库允许时随着可执行文件完整加载到内存，占用固定内存空间，动态库运行时动态加载，可以**多进程间共享**，减少内存占用
    > - 可拓展性：动态库的可拓展性好，不修改可执行文件的情况下，直接替换新的动态库，静态库需要重新编译链接

39. 面向对象和面向过程

    > - 面向过程：以过程为中心，主要关注算法和数据的步骤，把问题分解为一个个函数或过程，**数据和操作是分开的**，随着程序复杂化，数据与代码分离导致问题（数据结构发生变化，对于的操作代码也需要接收新的数据操作格式）
    > - 面向对象：以对象为中心，把数据（成员变量）和操作（成员函数）封装在对象中，利用封装、继承和多态，通过对象之间的协作解决问题，有比较好的代码可拓展性及复用性

40. 如何理解C++的面向对象

    > - 对比面向过程
    > - 展开面向对象：面向对象将数据和函数封装到一个类中，并声明其访问级别（public、private、protected），在类对象调用成员是，只需要知道成员函数名、参数列表及返回值类型，隐藏了实现细节，当类的内部成员变量或函数变化时，不影响外部代码的调用

41. 重载、重写（覆盖）和隐藏的区别

    > |      | 重载                                                         | 重写                                                         | 隐藏                                                         |
    > | ---- | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
    > | 概念 | 在同一个作用域内，**函数名相同，参数列表（类型、个数、顺序不同）**，与返回值无关 | 派生类对基类的**`virtual`虚函数**进行重新覆盖实现，**函数签名完全一致**，加上override | 子类中定义与父类同名的成员，**但参数列表不同**，父类同名成员被隐藏 |
    > | 特征 | **编译时多态**，根据参数类型或数量选择对应函数               | 必须是虚函数，**运行时多态**，调用时根据参数实际类型决定调用 | 父类成员需要通过作用域限定符访问，如`ex.Base::func();`       |
    >
    > - 范围
    > - 参数

42. C++的多态概念、如何通过虚函数实现

    > - 概念：同一个接口的操作，作用于不同对象时，表现出不同的行为；主要通过虚函数实现，允许基类指针或引用在运行时决定调用派生类的重写方法
    > - 分类：
    >   - 编译时多态：在编译阶段就确定具体调用的函数或方法，通过函数重载、运算符重载、模板等实现
    >   - **运行时多态：通过虚函数和继承实现，在运行时，根据对象的实际类型，决定调用的函数。每个包含虚函数的类都有一个虚函数表，这个表记录了该类的对象可以调用的虚函数指针，每个对象也有一个隐式的虚函数指针，指向类的虚函数表，在运行时，调用虚函数时，通过虚函数指针，查找正确的函数。**
    > - 实现：
    >   - 定义基类和派生类（多个）
    >   - 创建函数，接收基类指针为参数，传入不同派生类的对象，利用多态性调用不同的派生类方法

43. 函数对象和普通函数的区别

    > - 函数对象（仿函数）：**实现`operator()`的类或结构体**实例，可以像函数一样调用，本质是对象；可以保存状态（成员变量），可以作为模板参数、支持复杂行为，需要创建实例才能调用
    > - 普通函数：`返回类型 函数名(参数)`的形式，只能按照函数签名调用，无状态，没有保存的数据

44. class缺省的函数

    > 没有显示定义：构造函数、析构函数、拷贝构造函数、赋值运算符重载函数，编译器会自动生成

45. 虚函数和纯虚函数

    > - 虚函数：使用virtual声明，允许派生类重写实现，实现运行时多态，可以有函数体，可以被直接调用，可以继承后调用，使用基类指针或引用调用时，实现动态绑定
    > - 纯虚函数：在基类中声明但不提供实现的虚函数，`virtual 返回类型 函数名(...) = 0`，没有函数体，由派生类实现，使当前类称为**抽象类**，作为接口使用，不能实例化，派生类必须实现所有的纯虚函数才能实例化对象
    >   - 设计模式：基类定义算法骨架，将实现步骤延迟到子类中
    >   - 接口定义：创建一个只包含纯虚函数的抽象类
    > - 区别：
    >   - 可以同时出现在一个类中，该类为抽象基类
    >   - 使用方式：直接使用或必须由派生类实现后才能使用
    >   - 定义形式
    >   - 析构函数可以定义为纯虚函数，最好定义为虚函数

46. 虚函数的实现机制

    > > 虚函数通过**虚函数表**实现，**虚函数的地址保存在虚函数表中**，在类对象的内存空间中，保存了指向虚函数表的指针，及**虚表指针**，通过虚表指针可以找到类对应的虚函数表。虚函数表解决了基类和派生类间的继承关系，以及类中成员函数的覆盖问题，当使用基类的指针或引用操作派生类时，这张虚函数表就指明了实际应该调用的函数
    >
    > - 虚函数表
    >   - 存放的内容：虚函数的地址
    >   - 建立的时机：编译阶段
    >   - 保存的位置：虚表指针存放在类对象内存地址的最前面，保证一个偏移量
    > - **虚函数表绑定类，虚表指针绑定对象**，及每个对象的虚函数表一样，但每个对象都有自己的虚表指针，指向类的虚函数表

47. 单继承和多继承

    > - 一个子类是否同时继承多个父类
    > - 是否有二义性（多继承中不同父类有同名成员，需要通过作用域限定，或继承解决）
    > - 虚函数表结构
    >   - 单继承无虚函数覆盖
    >     - 派生类仅继承一个基类，没有重写基类虚函数
    >     - 每个对象只有一个vptr，指向基类vtable
    >   - 单继承有虚函数覆盖
    >     - 派生类继承一个基类，重写了部分或全部虚函数
    >     - 派生类对象有一个vptr，指向派生类自己的vtable，**虚函数表中，重写的虚函数指向派生类的实现，未重写的虚函数指向基类实现**
    >   - 多继承无虚函数覆盖
    >     - 派生类继承多个基类，未重写虚函数
    >     - **每个对象有多个vptr，分别指向各自基类的vtable，每个虚函数表仅包含对应基类的虚函数地址**
    >   - 多继承有虚函数覆盖
    >     - **每个对象有多个vptr，若重写某基类的虚函数，对应的vtable的条目指向派生类的实现，未重写，指向基类实现**

48. 菱形继承

    > - 一个类同时继承两个类，而这两个类又同时继承自同一个基类，形成类型继承
    > - 问题：数据成员重复，方法调用的二义性，重复的基类资源占用额外空间
    > - 解决：虚继承
    >
    > ```c++
    > class A { public: int x; };
    > class B : virtual public A {};
    > class C : virtual public A {};
    > class D : public B, public C {};
    > ```

49. 空类的大小

    > - 1字节：每个对象都必须有唯一的内存地址，隐式分配一个字节，避免多个对象共享地址来区分实例对象，当作为基类时，优化为0（空白基类优化）
    > - **带有虚函数的类大小不为1**：每个对象都有一个虚表指针，指向虚函数表

50. 空白基类优化

    > 基类没有任何成员，在继承时，编译器会避免为其分配额外的内存空间

51. 只含有虚函数的类大小

    > 有虚函数的类对象，都有一个虚函数表指针_vptr，指针大小是4字节（32为机器）

52. 只含有int成员变量的类大小、只含有static成员的类大小

    > 当成员变量int用static修饰后，这个成员会存放在**静态存储区**，不占用类的大小，该类大小从4变成1

53. 左值、右值、左值引用、右值引用

    > ### 左值（Lvalue）
    >
    > - **定义**：表示存储在内存中、**有明确地址的对象**，可以出现在赋值语句的左边。
    >
    > - 例子：变量、数组元素、解引用指针等。
    >
    >   ```C++
    >   int a = 5;
    >   a = 10; // a是左值
    >   ```
    >
    > ### 右值（Rvalue）
    >
    > - **定义**：表示临时值，**没有固定内存地址**，通常是表达式计算结果或字面量，只能出现在赋值语句的右边。
    >
    > - 例子：字面量、临时对象、表达式结果等。
    >
    >   ```c++
    >   int b = a + 1; // a+1是右值
    >   ```
    >
    > ### 左值引用（Lvalue Reference）
    >
    > - **定义**：使用`&`声明，可以绑定到左值（即有地址的对象）。
    >
    > - ```c++
    >   int x = 10;
    >   int &ref = x; // 左值引用
    >   ref = 20;     // 修改x的值
    >   ```
    >
    > ### 右值引用（Rvalue Reference）
    >
    > - **定义**：使用`&&`声明，可以绑定到右值（即临时对象、表达式结果），用于支持移动语义和完美转发，不能绑定到左值
    >
    > - ```c++
    >   int &&rref = a + 1; // 右值引用绑定临时右值
    >   rref = 5;
    >   ```
    >
    > **总结：**
    >
    > - 左值有地址、可被引用和赋值；
    > - 右值是临时值、不可寻址，只能赋给变量；
    > - **左值引用绑定可寻址对象，右值引用绑定临时对象**

54. **如何将左值转换成右值**

    > - 使用`std::move()`将一个左值强制转换成右值，进而通过右值引用使用该值，用于**移动语义**
    >
    > - ```c++
    >   int a = 10;
    >   int &&r = std::move(a);
    >   ```
    >
    > - 转换后，原对象可能会被“置空”或变为有效但未指定的状态
    >
    > - `std::move()`仅改变表达式类型，不改变实际的值或地址。

55. 移动语义

    > - C++引入的一种资源管理方式，通过**右值引用**和**移动构造函数**，来优化对象在内存中的传递和处理，避免不必要的数据复制（传统的拷贝操作对大型对象开销大，需要将对象的所有数据复制到新的对象中）
    >
    > - 原理：**移动语义通过右值引用（`T&&`）和`std::move()`实现。移动构造函数和移动赋值运算符会“窃取”源对象的资源并使源对象进入一种有效但未指定状态。**
    >
    > - ```c++
    >   class MyClass {
    >   public:
    >       MyClass(MyClass&& other) noexcept {
    >           // 转移资源所有权
    >           data = other.data;
    >           other.data = nullptr;
    >       }
    >       MyClass& operator=(MyClass&& other) noexcept {
    >           if (this != &other) {
    >               delete data;
    >               data = other.data;
    >               other.data = nullptr;
    >           }
    >           return *this;
    >       }
    >       int* data;
    >   };
    >   MyClass obj1;
    >   MyClass obj2 = std::move(obj1);
    >   ```

56. 右值引用的作用

    > - 实现移动语义：在不进行深拷贝的情况下，将对象的资源所有权从一个对象转移到另外一个对象，提升代码效率
    > - 用于完美转发：在函数模板中，接收右值引用类型的参数，保持原参数的值类别（左值、右值），将参数传递给另外一个函数

57. 完美转发

    > 完美转发是C++11引入的一种技术，**允许函数模板将参数“原封不动”地传递给另一个函数，无论参数是左值还是右值，都能保持其原始类型和属性**。
    >
    > **实现方式**：  
    >
    > 完美转发依赖于**万能引用**（`T&&`，又称转发引用）和`std::forward<T>()`。  
    >
    > - 万能引用能匹配左值引用和右值引用。
    > - `std::forward<T>()`确保左值被转发为左值，右值被转发为右值。
    >
    > **典型代码示例：**
    > ```cpp
    > template<typename T>
    > void wrapper(T&& arg) {
    >     process(std::forward<T>(arg));
    > }
    > ```
    > - `T&&`是万能引用，`std::forward<T>(arg)`实现完美转发。
    >
    > **优点：**
    > - 保证参数的类型和价值类别不变。
    > - 通常用于泛型代码、工厂函数、容器、回调等场景。
    >
    > **注意事项：**
    > - 必须结合模板参数和`std::forward`使用，否则不能实现“完美”。
    >

58. std::move的实现原理

    > - 利用**折叠原理**，将右值通过T&& 传递，类型保持不变还是右值，左值经T&& 变成普通的左值引用，保证模板可以传递任意实参，类型不变
    > - 然后通过remove_reference移除引用，得到具体原始类型T
    > - **最后通过static_cast<>强制类型转换，返回T&&右值引用**
    >
    > > ```c++
    > > template<typename T>
    > > typename std::remove_reference<T>::type&& move(T&& t) noexcept {
    > >     return static_cast<typename std::remove_reference<T>::type&&>(t);
    > > }
    > > ```
    > >
    > > `std::move` 的本质是用 `static_cast` 把左值转为右值引用类型，激活移动语义，但它本身不做任何资源的“搬运”。

59. 如何判断结构体是否相等，能否用memcmp判断

    > - 重载==运算符实现
    >
    > - ```c++
    >   struct Point {
    >       int x, y;
    >       bool operator==(const Point& other) const {
    >           return x == other.x && y == other.y;
    >       }
    >   };
    >   ```
    >
    > - 不能使用memcmp，因为存在内存对齐，而逐字节比较时，补齐的字节内容随机，产生垃圾数据，且指针和浮点数的成员比较的是地址或特殊编码，另外未初始化的成员导致memcmp不确定

60. 友元函数

    > 可以访问类的私有成员和保护成员。但**不是类的成员函数**，但在类内部通过`friend`关键字声明，赋予其特殊访问权限。
    >
    > **特点**：
    >
    > - 不是类成员，但可访问类的私有和保护成员。
    > - 可以是普通函数、成员函数、类、模板等。
    > - 增强了函数的灵活性，但要谨慎使用，避免破坏封装性。
    >
    > **声明方式**：
    >
    > ```cpp
    > class MyClass {
    > private:
    >     int data;
    >     friend void showData(MyClass& obj); // 声明友元函数
    > };
    > 
    > void showData(MyClass& obj) {
    >     std::cout << obj.data << std::endl; // 可以访问私有成员
    > }
    > ```
    >
    > **使用场景**：
    >
    > - 运算符重载（如`<<`、`>>`等需要访问私有成员但不是成员函数）
    > - 类之间紧密协作需要相互访问私有成员
    >
    > **总结**： 
    > 友元函数通过`friend`声明，允许外部函数访问类的私有和保护成员，常用于运算符重载和类之间的特殊协作。

61. 模板

    > 一种**泛型编程机制**，用来生成**类型安全**的函数和类。模板让代码在**编译时**根据不同类型自动生成对应的函数或类实现，提高代码复用性。
    >
    > **种类**：
    >
    > 1. **函数模板**：用于编写适用于多种数据类型的函数。
    >
    >    > 模板类型参数可以指定返回类型、参数类型、定义函数体内变量声明等，调用一个函数模板时，**编译器用函数实参来推导模板实参**，从而使用实参的类型确定绑定到模板参数的类型
    >
    >    ```c++
    >    template<typename T>
    >    T add(T a, T b) {
    >        return a + b;
    >    }
    >    ```
    >
    > 2. **类模板**：用于编写适用于多种数据类型的类。
    >
    >    > **编译器不能为类模板推断模板参数类型，需要在模板后面的尖括号中指定类型**
    >
    >    ```c++
    >    template<typename T>
    >    class MyArray {
    >    private:
    >        T arr[100];
    >    public:
    >        T& operator[](int idx) { return arr[idx]; }
    >    };
    >    ```
    >
    > **模板参数**：
    >
    > - 类型参数：如`typename T`或`class T`
    > - 非类型参数：如`template<int N>`
    >
    > **使用方法**：
    >
    > - 实例化时指定具体类型或参数
    >
    >   ```c++
    >   int x = add<int>(1, 2);
    >   MyArray<double> arr;
    >   ```
    >
    > **优点**：
    >
    > - 代码复用
    > - 类型安全
    > - 实现泛型容器和算法（如STL库）

62. 函数模板与类模板的区别

    > |                | 函数模板                                 | 类模板                               |
    > | -------------- | ---------------------------------------- | ------------------------------------ |
    > | 定义方式       | `template<typename T> T func(T a, T b)`  | `template<typename T> class MyClass` |
    > | **实例化时机** | 编译器根据调用实参自动推导类型           | 必须在定义对象时显式指定类型         |
    > | **重载**       | 可与普通函数重载，编译器自动匹配最佳版本 | 类模板不能与普通类重载               |
    > | **用途**       | 实现通用算法（如排序、交换等）           | 实现通用数据结构（如容器、数组等）   |
    > | **特化方式**   | 支持函数模板特化和重载                   | 支持类模板特化和**偏特化**           |
    >
    > **简要总结**：  
    >
    > - **函数模板**用于通用算法，参数类型可自动推导，支持重载。
    > - **类模板**用于通用类型，实例化时类型需显式指定，支持特化但不支持重载。

63. 可变参数模板

    > **定义**： 
    > 可变参数模板是C++11引入的一种模板机制，允许模板接受任意数量的参数（类型参数或值参数）。
    >
    > ---
    >
    > **语法格式**：
    > ```cpp
    > template<typename... Args>
    > void func(Args... args) {
    >     // 可以处理任意数量的参数
    > }
    > ```
    > - `typename... Args`：参数包（parameter pack），表示多个类型参数。
    > - `Args... args`：展开参数包。
    >
    > ---
    >
    > **展开方式**：
    > 通常使用**递归**或折叠表达式处理参数包。例如递归打印：
    >
    > ```cpp
    > void print() {} // 递归出口
    > template<typename T, typename... Args>
    > void print(T first, Args... rest) {
    >     std::cout << first << std::endl;
    >     print(rest...); // 展开参数包
    > }
    > ```
    > **应用场景**：
    >
    > - 实现像`printf`、日志、容器初始化等功能。
    > - 泛型编程和元编程。
    >
    > ---
    >
    > **总结**： 
    > 可变参数模板让C++函数和类能够支持任意数量参数，极大增强了模板的灵活性和表达能力，是现代C++泛型编程的重要特性。

64. STL容器

    > - 序列式容器
    > - 关联式容器
    > - 无序关联式容器

65. 